# Build artifacts
.build/
.swiftpm/
DerivedData/
# Stray SwiftPM incremental artifacts (leak into cwd on WSL2)
*.d
*.o
*.swiftdeps
*.swiftdeps~

# IDE / editor
*.xcodeproj/
*.xcworkspace/
.vscode/
.DS_Store

# Caches
.cache/
.omc/

# Note: Package.resolved is NOT ignored — it must be committed for reproducible builds
