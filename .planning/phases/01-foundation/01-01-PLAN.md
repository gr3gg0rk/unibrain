---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Package.swift
  - .gitignore
  - Sources/UnibrainCore/Protocols/LLMSummarizer.swift
  - Sources/UnibrainCore/Protocols/AudioTranscriber.swift
  - Sources/UnibrainCore/Protocols/VisionDescriber.swift
  - Sources/UnibrainCore/Protocols/AudioSynthesizer.swift
  - Sources/UnibrainCore/Errors/ProviderError.swift
  - Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift
  - Sources/UnibrainCore/ModelLoadGate/ModelLease.swift
  - Sources/UnibrainCore/ModelLoadGate/HeavyModelKind.swift
  - Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift
  - Sources/UnibrainCore/Schemas/FrontmatterSchema.swift
  - Sources/UnibrainProviders/.gitkeep
  - Tests/UnibrainCoreTests/ModelLoadGateTests.swift
  - Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift
  - Tests/UnibrainCoreTests/ProviderProtocolTests.swift
  - Tests/UnibrainProvidersTests/.gitkeep
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - DISC-02

must_haves:
  truths:
    - "swift --version reports Swift 6.0.x on WSL2 Linux"
    - "swift build --target UnibrainCore exits 0 on WSL2 Linux"
    - "swift test --test-product UnibrainCoreTests exits 0 with at least one passing test"
    - "Package.swift contains swift-tools-version: 6.0 and .swiftLanguageMode(.v6)"
    - "UnibrainCore has zero Apple-framework imports (DISC-02)"
    - "All four provider protocols compile on Linux (FOUND-02)"
    - "ModelLoadGate actor, ModelLease struct, HeavyModelKind enum, ModelLoadGateError enum compile (FOUND-04 scaffold)"
    - "FrontmatterSchema Codable struct with Yams compiles (FOUND-05 scaffold)"
  artifacts:
    - path: "Package.swift"
      provides: "SPM manifest with UnibrainCore, UnibrainProviders, UnibrainCoreTests, UnibrainProvidersTests targets"
      contains: "swift-tools-version: 6.0"
    - path: ".gitignore"
      provides: "Ignores .build, .swiftpm, DerivedData, .cache, .omc"
      contains: ".build"
    - path: "Sources/UnibrainCore/Protocols/LLMSummarizer.swift"
      provides: "LLMSummarizer protocol with associatedtype Request/Response"
      contains: "protocol LLMSummarizer"
    - path: "Sources/UnibrainCore/Protocols/AudioTranscriber.swift"
      provides: "AudioTranscriber protocol"
      contains: "protocol AudioTranscriber"
    - path: "Sources/UnibrainCore/Protocols/VisionDescriber.swift"
      provides: "VisionDescriber protocol"
      contains: "protocol VisionDescriber"
    - path: "Sources/UnibrainCore/Protocols/AudioSynthesizer.swift"
      provides: "AudioSynthesizer protocol"
      contains: "protocol AudioSynthesizer"
    - path: "Sources/UnibrainCore/Errors/ProviderError.swift"
      provides: "Shared ProviderError enum with six cases per D-16"
      contains: "enum ProviderError"
    - path: "Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift"
      provides: "actor ModelLoadGate with acquire/release API"
      contains: "actor ModelLoadGate"
    - path: "Sources/UnibrainCore/ModelLoadGate/ModelLease.swift"
      provides: "Sendable ModelLease struct"
      contains: "struct ModelLease: Sendable"
    - path: "Sources/UnibrainCore/ModelLoadGate/HeavyModelKind.swift"
      provides: "HeavyModelKind enum with .asr and .llm cases"
      contains: "enum HeavyModelKind"
    - path: "Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift"
      provides: "ModelLoadGateError enum with .busy case"
      contains: "enum ModelLoadGateError"
    - path: "Sources/UnibrainCore/Schemas/FrontmatterSchema.swift"
      provides: "FrontmatterSchema Codable Sendable struct with CodingKeys"
      contains: "struct FrontmatterSchema"
  key_links:
    - from: "Package.swift"
      to: "Sources/UnibrainCore/*"
      via: "SPM target dependency declaration"
      pattern: "\\.target.*name.*UnibrainCore"
    - from: "Sources/UnibrainCore/ModelLoadGate/ModelLease.swift"
      to: "Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift"
      via: "ModelLease stores ModelLoadGate actor reference for release()"
      pattern: "gate.*ModelLoadGate"
---

<objective>
Install Swift 6.0.x toolchain on WSL2, initialize the git repository and .gitignore, create the complete SPM Package.swift with all targets, and scaffold every source file and test file that Phase 1 needs. This is the Wave 0 foundation — every subsequent plan depends on this build succeeding.

Purpose: Every Phase 1 success criterion requires `swift build` and `swift test` to work on WSL2 Linux. Swift is not installed and Package.swift does not exist. This plan removes both blockers (D-07, D-10, D-15..D-18).
Output: Working SPM package that builds green on Linux with stub tests passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Swift 6.0.x toolchain + init git + create Package.swift + .gitignore</name>
  <files>Package.swift, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (sections: Standard Stack, Pitfalls 1-3, Code Examples > Package.swift, Environment Availability)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-05 deployment targets, D-07 three targets, D-10 Yams only)
  </read_first>
  <behavior>
    - Swift 6.0.x (NOT 6.2 which has SwiftPM hang on Ubuntu 24.04) is installed and on PATH
    - Git repo is initialized with branch main
    - .gitignore excludes build artifacts
    - Package.swift compiles with swift package resolve
  </behavior>
  <action>
Install Swift 6.0.x toolchain from swift.org for Ubuntu 24.04 x86_64. Install dependencies: libcurl4, libpython3.10, libxml2, libsqlite3-0 via apt. Download the Swift 6.0.3 tarball from swift.org/install/linux/ubuntu/24_04/, extract to /opt/swift-6.0.3, and symlink /usr/local/bin/swift to the extracted binary. Verify with swift --version that output contains "Swift version 6.0" (NOT 6.2 per RESEARCH.md Pitfall 2).

Initialize git if not already a repo: git init, rename branch to main. Create .gitignore with entries for: .build/, .swiftpm/, DerivedData/, .cache/, .omc/, *.xcodeproj/, Package.resolved (keep this in git actually — NO, do NOT ignore Package.resolved; it should be committed for reproducible builds). Ignore .build, .swiftpm, DerivedData, .DS_Store, .omc only.

Create Package.swift per D-07 with: swift-tools-version: 6.0; platforms .macOS(.v26) and .iOS(.v17) per D-05; products UnibrainCore (library) and UnibrainProviders (library); one dependency .package(url: "https://github.com/jpsim/Yams.git", from: "6.2.2") per D-10; four targets: UnibrainCore (.target with Yams dependency and .swiftLanguageMode(.v6)), UnibrainProviders (.target depending on UnibrainCore with .swiftLanguageMode(.v6)), UnibrainCoreTests (.testTarget depending on UnibrainCore), UnibrainProvidersTests (.testTarget depending on both UnibrainCore and UnibrainProviders). All targets get swiftSettings with .swiftLanguageMode(.v6).

Note on .macOS(.v26): If the Swift 6.0.x toolchain does not recognize .macOS(.v26), try the alternative forms .macOS("26.0") or fall back to .macOS(.v15) to get the build working. Document which form succeeded in the task summary. The deployment target is a build setting, not a runtime requirement — the toolchain version determines what enum values are available.

Run swift package resolve to fetch Yams 6.2.2 and verify the dependency graph resolves without errors. If Yams CYaml module compilation fails per RESEARCH.md Pitfall 3, run swift package resolve again explicitly before swift build.
  </action>
  <verify>
    <automated>swift --version 2>&1 | grep -E 'Swift version 6\.0'</automated>
  </verify>
  <acceptance_criteria>
    - swift --version output contains "Swift version 6.0" (not 6.1, not 6.2)
    - git status shows branch is "main"
    - Package.swift file exists at project root and contains the literal swift-tools-version: 6.0
    - Package.swift contains .package(url: "https://github.com/jpsim/Yams.git", from: "6.2.2")
    - Package.swift contains .swiftLanguageMode(.v6) in at least one target's swiftSettings
    - Package.swift contains .testTarget(name: "UnibrainCoreTests"
    - swift package resolve exits 0 (Yams dependency resolved)
    - .gitignore contains .build and .swiftpm entries
  </acceptance_criteria>
  <done>Swift 6.0.x is on PATH, git is initialized on branch main, Package.swift resolves Yams 6.2.2 successfully, .gitignore is in place.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold all UnibrainCore source files + test stubs</name>
  <files>Sources/UnibrainCore/Protocols/LLMSummarizer.swift, Sources/UnibrainCore/Protocols/AudioTranscriber.swift, Sources/UnibrainCore/Protocols/VisionDescriber.swift, Sources/UnibrainCore/Protocols/AudioSynthesizer.swift, Sources/UnibrainCore/Errors/ProviderError.swift, Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift, Sources/UnibrainCore/ModelLoadGate/ModelLease.swift, Sources/UnibrainCore/ModelLoadGate/HeavyModelKind.swift, Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift, Sources/UnibrainCore/Schemas/FrontmatterSchema.swift, Sources/UnibrainProviders/.gitkeep, Tests/UnibrainCoreTests/ModelLoadGateTests.swift, Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift, Tests/UnibrainCoreTests/ProviderProtocolTests.swift, Tests/UnibrainProvidersTests/.gitkeep</files>
  <read_first>
    - Package.swift (just created in Task 1 — verify target structure)
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples sections: ModelLoadGate, Yams Frontmatter Codable, CI Workflow, Architecture Patterns 1-3)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11..D-14 ModelLoadGate contract, D-15..D-18 provider protocols, D-16 ProviderError cases)
  </read_first>
  <behavior>
    - All four provider protocols compile with associatedtype Request and Response
    - ProviderError has exactly six cases: networkFailure, modelError, rateLimited, invalidResponse, cancelled, underlying
    - ModelLoadGate is an actor with acquire and release methods
    - ModelLease is a Sendable struct
    - HeavyModelKind has .asr and .llm cases
    - FrontmatterSchema is Codable and Sendable with all WRITE-02 fields
    - At least one trivial test in UnibrainCoreTests compiles and passes on Linux
  </behavior>
  <action>
Create all source files under Sources/UnibrainCore/ following the directory structure from RESEARCH.md (Recommended Project Structure section).

Create Sources/UnibrainCore/Protocols/LLMSummarizer.swift: public protocol LLMSummarizer with associatedtype Request, associatedtype Response, func summarize(_ request: Request) async throws -> Response. import Foundation only. Per D-15.

Create Sources/UnibrainCore/Protocols/AudioTranscriber.swift: same shape with func transcribe. Per D-15.

Create Sources/UnibrainCore/Protocols/VisionDescriber.swift: same shape with func describe. Per D-15.

Create Sources/UnibrainCore/Protocols/AudioSynthesizer.swift: same shape with func synthesize. Per D-15.

Create Sources/UnibrainCore/Errors/ProviderError.swift: public enum ProviderError: Error with six cases per D-16: networkFailure(URLRequest, URLError), modelError(String), rateLimited(retryAfter: TimeInterval?), invalidResponse(String), cancelled, underlying(any Error). Make it Sendable if the compiler requires it (URLRequest and URLError are Sendable in Swift 6; any Error is not Sendable so use a different approach if needed — store Error in a non-Sendable way or cast to NSError).

Create Sources/UnibrainCore/ModelLoadGate/HeavyModelKind.swift: public enum HeavyModelKind: String, Sendable { case asr, llm }. Per D-12.

Create Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift: public enum ModelLoadGateError: Error, Sendable { case busy(currentModel: HeavyModelKind?) }. Per D-11.

Create Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift: public actor ModelLoadGate with private var currentModel: HeavyModelKind? = nil, public init(), public func acquire(_ kind: HeavyModelKind) async throws -> ModelLease (deny-on-conflict per D-11: if currentModel exists and differs from kind, throw .busy(currentModel: currentModel); otherwise set currentModel = kind and return ModelLease), public func release(_ kind: HeavyModelKind) async (clear currentModel if it matches kind). Per D-13.

Create Sources/UnibrainCore/ModelLoadGate/ModelLease.swift: public struct ModelLease: Sendable with public let kind: HeavyModelKind, private let gate: ModelLoadGate, init(kind:gate:) (internal access), public func release() async that calls await gate.release(kind). Per D-13, D-14 (caller owns lease until explicit release).

Create Sources/UnibrainCore/Schemas/FrontmatterSchema.swift: public struct FrontmatterSchema: Codable, Sendable with fields per WRITE-02: schemaVersion (Int), course (String), courseName (String), term (String), datetime (Date), durationSeconds (Int), source (String), audioFile (String), tags ([String]), syllabusLink (String?), vectorId (String?), summaryModel (String?). CodingKeys mapping camelCase to snake_case: schema_version, course_name, duration_seconds, audio_file, syllabus_link, vector_id, summary_model. import Foundation and import Yams. Per FOUND-05.

Create Sources/UnibrainProviders/.gitkeep (empty — UnibrainProviders has no concrete code in Phase 1 per D-18).
Create Tests/UnibrainProvidersTests/.gitkeep (empty).

Create Tests/UnibrainCoreTests/ModelLoadGateTests.swift: import Testing, import @testable UnibrainCore. @Suite struct ModelLoadGateTests with one trivial @Test func smoke that creates a gate, acquires .asr, checks lease.kind == .asr, releases. This test MUST pass to prove the test harness works. Additional deny-on-conflict tests are implemented in Plan 02.

Create Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift: import Testing, import @testable UnibrainCore. @Suite struct FrontmatterSchemaTests with one trivial @Test func smoke that creates a FrontmatterSchema with sample values and checks a field. Yams round-trip tests are implemented in Plan 03.

Create Tests/UnibrainCoreTests/ProviderProtocolTests.swift: import Testing, import @testable UnibrainCore. @Suite struct ProviderProtocolTests with one @Test func providerErrorExists that creates a ProviderError.cancelled and checks it is non-nil. This proves the error enum compiles and is accessible.

Run swift build --target UnibrainCore to verify all sources compile. Run swift test --test-product UnibrainCoreTests to verify the test harness works on Linux.
  </action>
  <verify>
    <automated>swift build --target UnibrainCore 2>&1 | tail -5 && swift test --test-product UnibrainCoreTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - swift build --target UnibrainCore exits 0
    - swift test --test-product UnibrainCoreTests exits 0 with at least 3 tests passing (one per suite)
    - Sources/UnibrainCore/Protocols/LLMSummarizer.swift contains "protocol LLMSummarizer"
    - Sources/UnibrainCore/Protocols/AudioTranscriber.swift contains "protocol AudioTranscriber"
    - Sources/UnibrainCore/Protocols/VisionDescriber.swift contains "protocol VisionDescriber"
    - Sources/UnibrainCore/Protocols/AudioSynthesizer.swift contains "protocol AudioSynthesizer"
    - Sources/UnibrainCore/Errors/ProviderError.swift contains all six cases: networkFailure, modelError, rateLimited, invalidResponse, cancelled, underlying
    - Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift contains "actor ModelLoadGate"
    - Sources/UnibrainCore/ModelLoadGate/ModelLease.swift contains "struct ModelLease: Sendable"
    - Sources/UnibrainCore/ModelLoadGate/HeavyModelKind.swift contains "case asr" and "case llm"
    - Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift contains "case busy"
    - Sources/UnibrainCore/Schemas/FrontmatterSchema.swift contains "import Yams" and "struct FrontmatterSchema"
    - No source file under Sources/UnibrainCore/ contains import AVFoundation or import EventKit or import Speech or import SwiftUI (DISC-02)
  </acceptance_criteria>
  <done>All UnibrainCore source files and test stubs compile on Linux. swift build and swift test both succeed. Test harness is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WSL2 filesystem | Swift toolchain tarball extracted from swift.org — supply chain boundary |
| SPM dependency resolution | Yams 6.2.2 pulled from GitHub — external code entering the project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Swift toolchain tarball | medium | mitigate | Download only from swift.org official URL; verify tarball integrity |
| T-01-02 | Tampering | Yams SPM dependency | high | mitigate | Pin to exact version from: "6.2.2" in Package.swift per D-10; verify Package.resolved is committed |
| T-01-03 | Tampering | ModelLoadGate data race | high | mitigate | Swift 6 actor isolation (language-enforced serialization) — actor keyword on ModelLoadGate |
| T-01-04 | Information Disclosure | ProviderError leaking internal state | medium | mitigate | Structured enum cases per D-16, not raw backend strings |
| T-01-SC | Tampering | Swift toolchain install (apt + tarball) | high | mitigate | Install from official swift.org only; verify swift --version matches expected 6.0.x |
</threat_model>

<verification>
- swift --version reports 6.0.x
- swift build --target UnibrainCore exits 0 on Linux
- swift test --test-product UnibrainCoreTests exits 0 on Linux
- Package.swift has correct target structure per D-07
- No Apple-framework imports in UnibrainCore sources (DISC-02)
</verification>

<success_criteria>
- Swift 6.0.x toolchain installed and verified on WSL2
- Package.swift creates UnibrainCore, UnibrainProviders, UnibrainCoreTests, UnibrainProvidersTests targets
- All four provider protocols compile (FOUND-02)
- ModelLoadGate actor + ModelLease struct + HeavyModelKind enum + ModelLoadGateError enum compile (FOUND-04 scaffold)
- FrontmatterSchema with Yams compiles (FOUND-05 scaffold)
- ProviderError enum with six cases compiles (D-16)
- swift build and swift test succeed on Linux (DISC-02, DISC-03)
- UnibrainCore has zero Apple-framework imports
</success_criteria>

<artifacts_produced>
## Artifacts This Phase Produces

This plan creates the foundational artifacts:

**SPM Manifest:**
- Package.swift (swift-tools-version: 6.0, .swiftLanguageMode(.v6), Yams 6.2.2 dep, four targets)

**Provider Protocols (UnibrainCore):**
- LLMSummarizer (associatedtype Request/Response, func summarize)
- AudioTranscriber (associatedtype Request/Response, func transcribe)
- VisionDescriber (associatedtype Request/Response, func describe)
- AudioSynthesizer (associatedtype Request/Response, func synthesize)

**Shared Error Type (UnibrainCore):**
- ProviderError enum: networkFailure, modelError, rateLimited, invalidResponse, cancelled, underlying

**ModelLoadGate System (UnibrainCore):**
- ModelLoadGate actor (acquire, release methods, deny-on-conflict)
- ModelLease struct (Sendable, stores gate reference, release())
- HeavyModelKind enum (.asr, .llm)
- ModelLoadGateError enum (.busy(currentModel:))

**Schema (UnibrainCore):**
- FrontmatterSchema Codable Sendable struct (13 fields, snake_case CodingKeys)

**Test Scaffolding:**
- ModelLoadGateTests (@Suite with smoke test)
- FrontmatterSchemaTests (@Suite with smoke test)
- ProviderProtocolTests (@Suite with ProviderError smoke test)

**Build Configuration:**
- .gitignore (excludes .build, .swiftpm, DerivedData, .omc)
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
