---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - Tests/UnibrainCoreTests/ModelLoadGateTests.swift
  - Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift
  - Sources/UnibrainCore/ModelLoadGate/ModelLease.swift
autonomous: true
requirements:
  - FOUND-04
  - DISC-01

must_haves:
  truths:
    - "ModelLoadGate.acquire(.asr) returns a lease when the gate is free"
    - "ModelLoadGate.acquire(.llm) throws .busy(currentModel: .asr) when ASR is already held (deny-on-conflict per D-11)"
    - "ModelLoadGate.acquire(.asr) succeeds again when ASR is already held (reentrant same-kind per D-11)"
    - "After lease.release(), the gate accepts a different model kind"
    - "ModelLease is Sendable and can be passed across concurrency boundaries safely"
    - "No internal timeout on the lease (D-14) — caller owns it until explicit release"
  artifacts:
    - path: "Tests/UnibrainCoreTests/ModelLoadGateTests.swift"
      provides: "Full ModelLoadGate test suite with acquire/release/deny/reentrant/release-then-acquire tests"
      contains: "@Suite"
    - path: "Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift"
      provides: "actor ModelLoadGate with deny-on-conflict acquire and explicit release"
      contains: "actor ModelLoadGate"
    - path: "Sources/UnibrainCore/ModelLoadGate/ModelLease.swift"
      provides: "Sendable ModelLease struct with async release()"
      contains: "struct ModelLease: Sendable"
  key_links:
    - from: "Tests/UnibrainCoreTests/ModelLoadGateTests.swift"
      to: "Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift"
      via: "@testable import UnibrainCore; calls gate.acquire and gate.release"
      pattern: "gate\\.acquire"
    - from: "Sources/UnibrainCore/ModelLoadGate/ModelLease.swift"
      to: "Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift"
      via: "ModelLease.release() calls gate.release(kind) across actor boundary"
      pattern: "gate\\.release"
---

<objective>
Implement the full ModelLoadGate deny-on-conflict behavior with comprehensive tests, and refine the ModelLease release mechanism. The scaffolded actor from Plan 01 gets its real implementation and test coverage here.

Purpose: The ModelLoadGate is the 8GB RAM discipline enforcer (FOUND-04, DISC-01). Phase 3 ASR loading and Phase 6 LLM summarization both depend on this gate working correctly. The deny-on-conflict contract (D-11) must be proven by tests before any downstream code can trust it.
Output: Fully tested ModelLoadGate actor with five passing tests covering all acquire/release/conflict scenarios.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write comprehensive ModelLoadGate tests (RED phase)</name>
  <files>Tests/UnibrainCoreTests/ModelLoadGateTests.swift</files>
  <read_first>
    - Tests/UnibrainCoreTests/ModelLoadGateTests.swift (current smoke test from Plan 01)
    - Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift (scaffolded actor from Plan 01)
    - Sources/UnibrainCore/ModelLoadGate/ModelLease.swift (scaffolded struct from Plan 01)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11 deny-on-conflict, D-13 acquire/release, D-14 no timeout)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: ModelLoadGate, Code Examples > ModelLoadGate Test)
  </read_first>
  <behavior>
    - Test 1: acquireASRSucceeds — gate.acquire(.asr) returns lease with kind == .asr
    - Test 2: denyOnConflict — gate with .asr held, acquire(.llm) throws ModelLoadGateError.busy(currentModel: .asr)
    - Test 3: reentrantSameKind — gate with .asr held, acquire(.asr) succeeds again (same kind, no conflict)
    - Test 4: releaseAllowsNewModel — after releasing .asr, acquire(.llm) succeeds
    - Test 5: leaseIsSendable — ModelLease can be passed to a detached Task without compiler error
  </behavior>
  <action>
Replace Tests/UnibrainCoreTests/ModelLoadGateTests.swift with a full test suite using Swift Testing (@Test, @Suite, #expect). Write five tests covering the behavior list above.

For denyOnConflict: create a gate, acquire(.asr) to get asrLease, then use await #expect(throws: ModelLoadGateError.self) { _ = try await gate.acquire(.llm) } to assert the throw. Release asrLease after the assertion.

For reentrantSameKind: create a gate, acquire(.asr) twice, verify both leases have kind == .asr. Release both leases.

For releaseAllowsNewModel: create a gate, acquire(.asr), release, then acquire(.llm) which should succeed. Verify llmLease.kind == .llm.

For leaseIsSendable: acquire a lease, pass it to Task.detached { lease in ... } (or use #expect(lease is any Sendable) or just verify the type system accepts it by assigning to a let lease: any Sendable variable). The compiler accepting the code is the test — if ModelLease were not Sendable, Swift 6 would reject the detached Task closure. A simple compilation check: store the lease in a Sendable-typed variable.

Run swift test --filter ModelLoadGateTests. These tests should PASS because the scaffolded actor from Plan 01 already implements the deny-on-conflict logic. If any test fails, the implementation in Task 2 fixes it.
  </action>
  <verify>
    <automated>swift test --test-product UnibrainCoreTests --filter ModelLoadGateTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Tests/UnibrainCoreTests/ModelLoadGateTests.swift contains @Suite("ModelLoadGate") or struct ModelLoadGateTests
    - Test file contains a test named denyOnConflict (or similar) that asserts ModelLoadGateError is thrown
    - Test file contains a test for reentrant same-kind acquire
    - Test file contains a test for release-then-acquire-different-kind
    - Test file contains a test or compile-time check verifying ModelLease Sendable conformance
    - swift test --test-product UnibrainCoreTests --filter ModelLoadGateTests exits 0
    - At least 5 tests run in the ModelLoadGate suite
  </acceptance_criteria>
  <done>Five ModelLoadGate tests written and passing on Linux, covering all D-11..D-14 contract behaviors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify ModelLoadGate implementation matches D-11..D-14 contract</name>
  <files>Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift, Sources/UnibrainCore/ModelLoadGate/ModelLease.swift</files>
  <read_first>
    - Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift (after Task 1 tests)
    - Sources/UnibrainCore/ModelLoadGate/ModelLease.swift
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11 through D-14)
  </read_first>
  <behavior>
    - ModelLoadGate.acquire throws .busy(currentModel:) when a DIFFERENT kind is held
    - ModelLoadGate.acquire succeeds when the SAME kind is held (reentrant)
    - ModelLoadGate.release clears the held model
    - ModelLease.release() calls back to the gate asynchronously
    - No timer, no timeout, no auto-release exists in the code
  </behavior>
  <action>
Read the current ModelLoadGate.swift and ModelLease.swift implementations. Verify the deny-on-conflict logic is correct: acquire checks if currentModel is non-nil AND currentModel != kind before throwing. If any test from Task 1 failed, fix the implementation to make it pass.

Specifically verify:
1. acquire sets currentModel = kind on success (not before the check)
2. release checks currentModel == kind before clearing (prevents a stale lease from clearing a newer model)
3. ModelLease stores a reference to the gate (actor reference, which is Sendable) and release() is async to cross the actor boundary
4. No DispatchQueue, no Timer, no Task.sleep, no auto-release logic exists anywhere in the ModelLoadGate directory

Run swift build --target UnibrainCore to verify clean compilation with Swift 6 strict concurrency. Run swift test --test-product UnibrainCoreTests to verify all tests pass.
  </action>
  <verify>
    <automated>swift build --target UnibrainCore && swift test --test-product UnibrainCoreTests</automated>
  </verify>
  <acceptance_criteria>
    - swift build --target UnibrainCore exits 0 (Swift 6 strict concurrency)
    - swift test --test-product UnibrainCoreTests exits 0 (all tests pass including ModelLoadGate suite)
    - Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift contains "actor ModelLoadGate"
    - Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift contains "throw ModelLoadGateError.busy" in the acquire method
    - Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift does NOT contain "Timer" or "Task.sleep" or "DispatchQueue" (D-14: no internal timeout)
    - Sources/UnibrainCore/ModelLoadGate/ModelLease.swift contains "func release() async"
  </acceptance_criteria>
  <done>ModelLoadGate implementation fully verified against D-11..D-14 contract. All tests green. Swift 6 strict concurrency compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Actor boundary | ModelLoadGate actor serializes all access to currentModel — no external code can read/write it without going through acquire/release |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | ModelLoadGate data race | high | mitigate | Swift 6 actor isolation — all access serialized through actor boundary, verified by strict concurrency compilation |
| T-01-05 | Denial of Service | Stale lease preventing all future model loads | medium | mitigate | release() checks currentModel == kind before clearing; a stale lease cannot clear a different model. Process restart reclaims RAM on crash (D-14). |
</threat_model>

<verification>
- All five ModelLoadGate tests pass on Linux
- swift build --target UnibrainCore exits 0 with Swift 6 strict concurrency
- No manual locking primitives (DispatchQueue, NSLock, semaphore) in ModelLoadGate code
- Deny-on-conflict throws ModelLoadGateError.busy with the correct currentModel value
</verification>

<success_criteria>
- ModelLoadGate enforces one-heavy-model-at-a-time (FOUND-04, DISC-01)
- Deny-on-conflict throws immediately with currentModel info (D-11)
- Same-kind reentrant acquire succeeds (D-11)
- Release clears the gate for a different model
- No internal timeout — caller owns the lease (D-14)
- ModelLease is Sendable and safe to pass across concurrency boundaries (D-13)
</success_criteria>

<artifacts_produced>
## Artifacts This Phase Produces

This plan produces the tested ModelLoadGate system:

**Tested Components:**
- ModelLoadGate actor — deny-on-conflict acquire, reentrant same-kind, explicit release
- ModelLease struct — Sendable, async release() method, stores gate reference

**Test Suite:**
- ModelLoadGateTests: 5 tests covering acquire-free, deny-on-conflict, reentrant, release-then-acquire, Sendable conformance
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
