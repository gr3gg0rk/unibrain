---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift
  - Tests/UnibrainCoreTests/ProviderProtocolTests.swift
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - FOUND-03
  - FOUND-05
  - DISC-03

must_haves:
  truths:
    - "FrontmatterSchema encodes to YAML via Yams and decodes back with all fields preserved (round-trip)"
    - "YAML output contains snake_case keys (schema_version, course_name, duration_seconds, audio_file, syllabus_link, vector_id, summary_model)"
    - "All four provider protocols can be conformed to by mock types in tests"
    - "ProviderError can be thrown and caught with all six cases"
    - "GitHub Actions CI workflow runs on both ubuntu-latest and macos-15"
    - "CI uses actions/cache@v4 for SPM cache and DerivedData cache"
    - "CI Linux job runs only UnibrainCoreTests; macOS job runs full suite"
    - "Pure-logic tests run on WSL2 Linux (DISC-03)"
  artifacts:
    - path: "Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift"
      provides: "Yams round-trip encode/decode tests for FrontmatterSchema"
      contains: "@Suite"
    - path: "Tests/UnibrainCoreTests/ProviderProtocolTests.swift"
      provides: "Mock protocol conformances and ProviderError coverage tests"
      contains: "@Suite"
    - path: ".github/workflows/ci.yml"
      provides: "CI matrix workflow with Linux + macOS jobs, SPM and DerivedData caching"
      contains: "macos-15"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "Package.swift"
      via: "CI runs swift build and swift test against the SPM package"
      pattern: "swift (build|test)"
    - from: "Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift"
      to: "Sources/UnibrainCore/Schemas/FrontmatterSchema.swift"
      via: "@testable import UnibrainCore; Yams encoder/decoder"
      pattern: "FrontmatterSchema"
---

<objective>
Implement Yams round-trip tests for FrontmatterSchema, provider protocol conformance tests, and the GitHub Actions CI workflow. This plan runs in parallel with Plan 02 (no file overlap) and together they complete all Wave 1 implementation work.

Purpose: FrontmatterSchema is the data contract for every lecture note the app writes (FOUND-05). The CI workflow is the only build/test path since there is no Mac in the dev loop (FOUND-03). Pure-logic tests must run on Linux (DISC-03).
Output: Tested FrontmatterSchema with proven Yams round-trip, tested provider protocols, and a CI pipeline that runs on every push.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FrontmatterSchema Yams round-trip tests + provider protocol mock tests</name>
  <files>Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift, Tests/UnibrainCoreTests/ProviderProtocolTests.swift</files>
  <read_first>
    - Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift (smoke test from Plan 01)
    - Tests/UnibrainCoreTests/ProviderProtocolTests.swift (smoke test from Plan 01)
    - Sources/UnibrainCore/Schemas/FrontmatterSchema.swift (Codable struct from Plan 01)
    - Sources/UnibrainCore/Protocols/LLMSummarizer.swift
    - Sources/UnibrainCore/Protocols/AudioTranscriber.swift
    - Sources/UnibrainCore/Protocols/VisionDescriber.swift
    - Sources/UnibrainCore/Protocols/AudioSynthesizer.swift
    - Sources/UnibrainCore/Errors/ProviderError.swift
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples > Yams Frontmatter Codable, Pattern 3: Four Standalone Protocols)
  </read_first>
  <behavior>
    - FrontmatterSchema encodes to YAML string via YAMLEncoder
    - Encoded YAML contains the literal keys: schema_version, course, course_name, term, datetime, duration_seconds, source, audio_file, tags
    - Decoding the encoded YAML back into FrontmatterSchema preserves all field values
    - Nullable fields (syllabus_link, vector_id, summary_model) survive nil round-trip
    - Nullable fields with values survive non-nil round-trip
    - Mock struct conforming to LLMSummarizer compiles and its summarize method is callable
    - Mock struct conforming to AudioTranscriber compiles and its transcribe method is callable
    - Mock struct conforming to VisionDescriber compiles and its describe method is callable
    - Mock struct conforming to AudioSynthesizer compiles and its synthesize method is callable
    - ProviderError.networkFailure, .modelError, .rateLimited, .invalidResponse, .cancelled, .underlying are all constructible
  </behavior>
  <action>
Replace Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift with comprehensive Yams round-trip tests:

Test 1 (roundTripPreservesAllFields): Create a FrontmatterSchema with all 12 fields populated (schemaVersion: 1, course: "CS101", courseName: "Intro to CS", term: "Fall 2026", datetime: a fixed Date, durationSeconds: 3600, source: "lecture", audioFile: "recording.m4a", tags: ["cs", "lecture"], syllabusLink: "https://example.edu/syllabus", vectorId: "vec-001", summaryModel: "llama-3.2-3b"). Encode with YAMLEncoder, decode back, assert every field matches the original.

Test 2 (nullableFieldsSurviveNilRoundTrip): Create a FrontmatterSchema with syllabusLink: nil, vectorId: nil, summaryModel: nil. Round-trip through Yams. Assert the decoded values are nil.

Test 3 (yamlOutputUsesSnakeCaseKeys): Encode a FrontmatterSchema. Assert the resulting YAML string contains the literal substrings "schema_version", "course_name", "duration_seconds", "audio_file". Use #expect(yamlString.contains("schema_version")).

Replace Tests/UnibrainCoreTests/ProviderProtocolTests.swift with mock conformance tests:

Define four private mock structs inside the test file, each conforming to one protocol:
- MockLLMSummarizer: LLMSummarizer with Request = String, Response = String, summarize returns the input reversed (trivial mock)
- MockAudioTranscriber: AudioTranscriber with Request = String, Response = String
- MockVisionDescriber: VisionDescriber with Request = String, Response = String
- MockAudioSynthesizer: AudioSynthesizer with Request = String, Response = String

Test 4 (mockLLMSummarizerConforms): Instantiate MockLLMSummarizer, call summarize("hello"), expect response == "olleh" (or whatever trivial transform).

Test 5-7: Same pattern for the other three protocols (call the method, verify it returns).

Test 8 (providerErrorAllCasesConstructible): Iterate over constructing ProviderError cases: .networkFailure(URLRequest(url: URL(string: "https://example.com")!), URLError(.notConnectedToInternet)), .modelError("test"), .rateLimited(retryAfter: 60), .invalidResponse("bad"), .cancelled, .underlying(NSError(domain: "test", code: 1)). Verify each can be caught in a do/catch block.

Run swift test --test-product UnibrainCoreTests to verify all tests pass.
  </action>
  <verify>
    <automated>swift test --test-product UnibrainCoreTests --filter FrontmatterSchemaTests 2>&1 | tail -10 && swift test --test-product UnibrainCoreTests --filter ProviderProtocolTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift contains at least 3 @Test functions
    - Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift contains a test that encodes and decodes FrontmatterSchema via Yams
    - Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift contains a test asserting YAML output contains snake_case key names
    - Tests/UnibrainCoreTests/ProviderProtocolTests.swift contains mock structs conforming to all four protocols
    - Tests/UnibrainCoreTests/ProviderProtocolTests.swift contains a test constructing all six ProviderError cases
    - swift test --test-product UnibrainCoreTests exits 0 (all tests pass including the new ones)
  </acceptance_criteria>
  <done>FrontmatterSchema round-trips through Yams with snake_case keys preserved. All four provider protocols are conformable. ProviderError has six working cases. All tests green on Linux.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create GitHub Actions CI workflow with matrix + caching</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - Package.swift (from Plan 01 — understand target structure for CI commands)
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples > CI Workflow, Standard Stack > Supporting, Environment Availability)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-02 public repo, D-08 test target split)
  </read_first>
  <behavior>
    - CI triggers on push to main and pull_request to main
    - Linux job runs on ubuntu-latest, installs Swift 6.0.3, runs swift build --target UnibrainCore and swift test --test-product UnibrainCoreTests
    - macOS job runs on macos-15, runs swift build (all targets) and swift test (all tests)
    - Both jobs use actions/checkout@v4
    - macOS job uses actions/cache@v4 for SPM cache with key based on Package.resolved hash
    - macOS job uses actions/cache@v4 for DerivedData cache with key based on Package.swift + Package.resolved hash
    - Linux job uses swift-actions/setup-swift@v2 to install Swift 6.0.3
  </behavior>
  <action>
Create .github/workflows/ci.yml with the following structure (per RESEARCH.md Code Examples > CI Workflow):

Name: CI. Triggers: push to branches [main], pull_request to branches [main].

Two jobs:

Job 1: linux-tests. Name "Linux (UnibrainCore only)". runs-on: ubuntu-latest. Steps: actions/checkout@v4; swift-actions/setup-swift@v2 with swift-version "6.0.3"; run "swift --version"; run "swift package resolve"; run "swift build --target UnibrainCore"; run "swift test --test-product UnibrainCoreTests".

Job 2: macos-tests. Name "macOS (all targets)". runs-on: macos-15. Steps: actions/checkout@v4; SPM cache via actions/cache@v4 with id "spm-cache", path including ~/Library/Caches/org.swift.swiftpm and ~/Library/org.swift.swiftpm, key format ${{ runner.os }}-spm-${{ hashFiles('Package.resolved') }} with restore-keys ${{ runner.os }}-spm-; DerivedData cache via actions/cache@v4 with id "dd-cache", path ~/Library/Developer/Xcode/DerivedData, key format ${{ runner.os }}-dd-${{ hashFiles('Package.resolved', 'Package.swift') }} with restore-keys ${{ runner.os }}-dd-; run "echo \"SPM cache hit: ${{ steps.spm-cache.outputs.cache-hit }}\""; run "echo \"DerivedData cache hit: ${{ steps.dd-cache.outputs.cache-hit }}\""; run "swift build"; run "swift test".

Per D-02, the repo is public so macOS minutes are free. Per D-08, Linux runs only UnibrainCoreTests while macOS runs all.

Do NOT add xcode-select steps or Xcode version pinning — macos-15 ships with Xcode 16.4+ which is sufficient for Swift 6.0 per RESEARCH.md.

After creating the file, validate the YAML is syntactically correct. The workflow will only actually run after the first push to main, but the file must be valid YAML.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml file exists
    - File contains "runs-on: ubuntu-latest"
    - File contains "runs-on: macos-15"
    - File contains "swift-actions/setup-swift" (Linux Swift install)
    - File contains "swift build --target UnibrainCore" (Linux builds core only)
    - File contains "swift test --test-product UnibrainCoreTests" (Linux tests core only)
    - File contains "actions/cache@v4" (caching enabled)
    - File contains "id: spm-cache" or "spm-cache" as a cache step identifier
    - File contains "steps.spm-cache.outputs.cache-hit" (cache-hit output printed for verification)
    - File contains "steps.dd-cache.outputs.cache-hit" (DerivedData cache-hit output printed)
    - File contains "hashFiles('Package.resolved')" or "hashFiles('Package.swift'" (cache key based on manifest)
    - File contains "branches: [main]" (trigger on main)
    - YAML is syntactically valid (parseable)
  </acceptance_criteria>
  <done>CI workflow file exists with Linux + macOS matrix, SPM and DerivedData caching, correct build/test commands per target split. Valid YAML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner | External GitHub-hosted machine running build + test — code executes in untrusted CI environment |
| SPM cache | Cache restored from previous runs — cache poisoning could inject malicious dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Spoofing | CI workflow file tampering | medium | accept | Public repo per D-02; CI file is version-controlled in git; changes reviewed via PR |
| T-01-07 | Tampering | SPM cache poisoning | low | accept | Cache key includes Package.resolved hash; cache can only restore what SPM already resolved legitimately |
| T-01-08 | Information Disclosure | CI logs leaking sensitive data | low | accept | Phase 1 has no secrets, no API keys, no user data; public repo per D-02 |
</threat_model>

<verification>
- FrontmatterSchema round-trips through Yams with all fields preserved
- YAML output uses snake_case keys matching CodingKeys
- All four provider protocols are conformable by mock types
- ProviderError constructs all six enum cases
- CI workflow file is valid YAML with correct matrix structure
- Pure-logic tests pass on Linux (DISC-03)
</verification>

<success_criteria>
- Yams integrates for frontmatter serialization/deserialization with proven round-trip (FOUND-05)
- GitHub Actions CI workflow created with Linux + macOS matrix (FOUND-03)
- CI uses actions/cache@v4 for SPM and DerivedData caching
- Provider protocols proven conformable by mock types in tests
- All tests pass on WSL2 Linux (DISC-03)
</success_criteria>

<artifacts_produced>
## Artifacts This Phase Produces

**Test Suite:**
- FrontmatterSchemaTests: 3 tests (full round-trip, nil-fields round-trip, snake_case YAML key verification)
- ProviderProtocolTests: 5 tests (4 protocol mock conformances + ProviderError all-cases)

**CI Pipeline:**
- .github/workflows/ci.yml: matrix workflow (ubuntu-latest + macos-15), SPM cache, DerivedData cache
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
