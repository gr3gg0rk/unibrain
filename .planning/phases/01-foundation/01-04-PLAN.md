---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
  - 01-03
files_modified:
  - .planning/PROJECT.md
  - UnibrainApp/UnibrainApp.swift
  - UnibrainApp/ContentView.swift
  - .planning/phases/01-foundation/SKELETON.md
autonomous: false
requirements:
  - FOUND-06
  - FOUND-01

user_setup:
  - service: "Apple Developer Program"
    why: "Needed for device builds in Phase 3. Phase 1 documents the decision only."
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "PROJECT.md Key Decisions table contains an Apple Developer Program row with rationale (FOUND-06, D-01)"
    - "PROJECT.md documents MacBook Neo as macOS 26 / A-series / 8GB (D-03, D-06)"
    - "PROJECT.md documents deployment targets as macOS 26 / iOS 17 (D-05)"
    - "PROJECT.md documents the public repo decision (D-02)"
    - "PROJECT.md documents the bundle ID choice"
    - "UnibrainApp Xcode app shell source files exist for macOS CI to compile"
    - "SKELETON.md records the architectural decisions Phase 2-6 will build on"
    - "swift build and swift test both pass locally on Linux after all Phase 1 work"
    - "Full test suite runs green on macOS CI (verified via push)"
    - "CI cache-hit output verified on second run: SPM cache and DerivedData cache both report hits (Phase 1 Success Criterion #3)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated Key Decisions table with Apple Dev, repo visibility, hardware, deployment targets, bundle ID"
      contains: "Apple Developer Program"
    - path: "UnibrainApp/UnibrainApp.swift"
      provides: "SwiftUI app entry point for macOS CI compilation"
      contains: "struct UnibrainApp"
    - path: ".planning/phases/01-foundation/SKELETON.md"
      provides: "Walking Skeleton architectural decision record"
      contains: "Walking Skeleton"
  key_links:
    - from: "UnibrainApp/UnibrainApp.swift"
      to: "Package.swift"
      via: "Xcode app target references local SPM package as dependency"
      pattern: "UnibrainApp"
---

<objective>
Document the Apple Developer Program decision in PROJECT.md (FOUND-06 per D-01), create the Xcode app shell source files that macOS CI will compile, produce the SKELETON.md architectural record, and run the final Phase 1 verification — proving the walking skeleton works end-to-end (build + test on Linux, CI green on macOS).

Purpose: Phase 1 must produce both the architectural bedrock AND the documentation record. The Apple Dev decision (D-01 deferred to Phase 3) is a documentation deliverable. SKELETON.md locks the architectural decisions so Phase 2+ does not re-litigate them. The app shell proves the Xcode target path works on CI.
Output: Updated PROJECT.md, app shell sources, SKELETON.md, and a verified green Phase 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update PROJECT.md Key Decisions + create app shell + SKELETON.md</name>
  <files>.planning/PROJECT.md, UnibrainApp/UnibrainApp.swift, UnibrainApp/ContentView.swift, .planning/phases/01-foundation/SKELETON.md</files>
  <read_first>
    - .planning/PROJECT.md (current state — Key Decisions table, Constraints, Context sections)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 through D-06 for decisions to document)
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples > SwiftUI App Shell, Recommended Project Structure)
    - .planning/phases/01-foundation/01-01-SUMMARY.md (what was built in Wave 0)
    - .planning/phases/01-foundation/01-02-SUMMARY.md (ModelLoadGate results)
    - .planning/phases/01-foundation/01-03-SUMMARY.md (CI + FrontmatterSchema results)
    - Package.swift (to reference exact structure in SKELETON.md)
  </read_first>
  <behavior>
    - PROJECT.md Key Decisions table has a row for Apple Developer Program with "Deferred to Phase 3" status
    - PROJECT.md Key Decisions table has rows for public repo, MacBook Neo hardware, deployment targets, bundle ID
    - PROJECT.md Context section reflects MacBook Neo (not generic "MacBook Air")
    - UnibrainApp.swift contains @main struct UnibrainApp: App with WindowGroup and macOS MenuBarExtra
    - ContentView.swift contains a basic SwiftUI view
    - SKELETON.md records framework, data layer, deployment target, directory layout, and subsequent slice plan
  </behavior>
  <action>
Update .planning/PROJECT.md using Edit (scoped replacements — do NOT rewrite the file). Make the following changes:

1. In the Key Decisions table, add or update these rows:
   - "Apple Developer Program" | "Deferred to Phase 3 — $99/yr paid recommended for TestFlight + crash logs. Not blocking Phase 1 SPM/CI work." | "Deferred (D-01, FOUND-06)"
   - "Public repository" | "Unlimited free macOS CI minutes on GitHub Actions. No proprietary logic, no secrets, single-user." | "Decided (D-02)"
   - "MacBook Neo hardware" | "macOS 26 (Tahoe), A-series chip, 8GB unified memory. Affects ASR strategy (CoreML/ANE may favor WhisperKit over Metal)." | "Confirmed (D-03, D-06)"
   - "Deployment targets" | "macOS 26 (Tahoe) / iOS 17. Unlocks SpeechAnalyzer on macOS; keeps EventKit iOS 17+ API." | "Decided (D-05)"
   - "iPhone/iPad OS versions" | "Unknown — verify with Angelica before Phase 5 iOS capture work. iPad is view/sync surface only in MVP." | "Informational (D-04)"
   - "Bundle ID" | "app.unibrain (subject to change when Apple Dev account is activated in Phase 3)" | "Provisional"
   - "Swift 6 strict concurrency" | "swiftLanguageMode(.v6) on all targets. Greenfield project — start in Swift 6 from day one." | "Decided"

2. In the Context section, update the device description from generic "MacBook Air (Apple Silicon, 8GB unified memory)" to "MacBook Neo (A-series chip, macOS 26 Tahoe, 8GB unified memory)" per D-03.

3. Update the "Last updated" line to the current date.

Create UnibrainApp/ directory with two source files:

UnibrainApp/UnibrainApp.swift: import SwiftUI. @main struct UnibrainApp: App. var body: some Scene with WindowGroup { ContentView() }. Add #if os(macOS) block with MenuBarExtra("Unibrain", systemImage: "brain") containing a Text view "Unibrain — Phase 1 Shell". Close the #endif. This matches RESEARCH.md Code Examples > SwiftUI App Shell.

UnibrainApp/ContentView.swift: import SwiftUI. struct ContentView: View with var body: some View returning VStack containing Text("Unibrain"). font(.title). padding(). This is a placeholder — Phase 3 replaces it with the recording UI.

Note: The UnibrainApp directory is an Xcode project target, NOT part of Package.swift (per D-09). These source files exist for the macOS CI runner to compile via xcodebuild, or for a future Xcode project file to reference. They will not be compiled by swift build.

Create .planning/phases/01-foundation/SKELETON.md using the Walking Skeleton template. Fill in:
- Capability Proven: "Swift Package Manager package with UnibrainCore library builds and tests green on WSL2 Linux; GitHub Actions CI runs the same build + test on macOS with caching."
- Architectural Decisions table: Framework (SwiftUI + SPM), Data layer (files + Yams YAML — no database in MVP), Auth (none — single-user), Deployment target (macOS 26 / iOS 17 via Xcode multiplatform), Directory layout (feature-grouped under Sources/UnibrainCore/ with Protocols/, Errors/, ModelLoadGate/, Schemas/ subdirs).
- Stack Touched in Phase 1: Project scaffold (checked), Routing (N/A — Phase 1 has no routes), Database (N/A — file-based), UI (app shell exists but no interaction), Deployment (CI pipeline, not app store).
- Out of Scope: all Phase 2-6 items (transcription, classification, write-out, summarization, cloud providers, iOS capture).
- Subsequent Slice Plan: Phase 2 (pure pipeline logic on Linux), Phase 3 (macOS record-transcribe-write slice), Phase 4 (calendar-based routing), Phase 5 (iOS capture + onboarding), Phase 6 (gated summary + cloud providers).
  </action>
  <verify>
    <automated>grep -c 'Apple Developer Program' .planning/PROJECT.md && grep -c 'MacBook Neo' .planning/PROJECT.md && test -f UnibrainApp/UnibrainApp.swift && test -f UnibrainApp/ContentView.swift && test -f .planning/phases/01-foundation/SKELETON.md && grep -c 'Walking Skeleton' .planning/phases/01-foundation/SKELETON.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md contains "Apple Developer Program" in the Key Decisions section
    - .planning/PROJECT.md contains "Deferred to Phase 3" or "Deferred (D-01" for the Apple Dev row
    - .planning/PROJECT.md contains "MacBook Neo" (updated from generic MacBook Air)
    - .planning/PROJECT.md contains "Public repository" or "public repo" decision
    - .planning/PROJECT.md contains "macOS 26" and "iOS 17" deployment target reference
    - .planning/PROJECT.md contains "app.unibrain" bundle ID
    - UnibrainApp/UnibrainApp.swift exists and contains "@main" and "struct UnibrainApp" and "App"
    - UnibrainApp/UnibrainApp.swift contains "MenuBarExtra" inside a #if os(macOS) block
    - UnibrainApp/ContentView.swift exists and contains "struct ContentView" and "View"
    - .planning/phases/01-foundation/SKELETON.md exists and contains "Walking Skeleton"
    - SKELETON.md contains Architectural Decisions table with at least 5 rows
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions updated with all D-01..D-06 decisions. App shell source files exist. SKELETON.md records the architectural backbone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 1 walking skeleton — build + test green, CI pushed</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Human verification checkpoint: confirm the Phase 1 walking skeleton is green end-to-end before transitioning to Phase 2.
  </action>
  <what-built>
Phase 1 foundation: Swift 6.0.x SPM package with UnibrainCore (provider protocols, ModelLoadGate actor, FrontmatterSchema, ProviderError), comprehensive Linux-runnable tests, and a GitHub Actions CI workflow with Linux + macOS matrix and caching. The walking skeleton proves: (1) swift build succeeds on Linux, (2) swift test passes on Linux with ModelLoadGate deny-on-conflict tests + Yams round-trip tests + protocol mock tests, (3) CI pipeline runs on push.
  </what-built>
  <how-to-verify>
1. On WSL2, run: swift build --target UnibrainCore — should exit 0
2. On WSL2, run: swift test --test-product UnibrainCoreTests — should show all tests passing (8+ tests across 3 suites)
3. Verify no Apple-framework imports leaked into UnibrainCore: grep -rn "import AVFoundation\|import EventKit\|import Speech\|import SwiftUI" Sources/UnibrainCore/ — should return nothing
4. Push all changes to the main branch on GitHub: git add -A && git commit && git push
5. Open GitHub Actions tab — verify both linux-tests and macos-tests jobs trigger
6. Wait for both jobs to complete — both should be green
7. Push a second trivial commit to verify cache hits on the macOS job. Check the job logs for the echo steps: "SPM cache hit: true" and "DerivedData cache hit: true" must appear in the output. This verifies ROADMAP Phase 1 Success Criterion #3 (caches verified as hits on the second run).
8. Verify PROJECT.md Key Decisions table has the Apple Developer Program row marked "Deferred to Phase 3"
9. Verify SKELETON.md exists and records the architectural decisions
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe what failed</resume-signal>
  <verify>
    <automated>swift build --target UnibrainCore && swift test --test-product UnibrainCoreTests</automated>
    <human-check>GitHub Actions CI both jobs green on push; PROJECT.md and SKELETON.md verified</human-check>
  </verify>
  <done>Human confirms all 9 verification steps pass: Linux build + test green, no Apple imports in UnibrainCore, CI pipeline both jobs green (including second-run cache-hit verification), PROJECT.md and SKELETON.md present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation vs code | PROJECT.md decisions are authoritative — code must conform to documented decisions |
| Local vs CI | Local Linux build is the fast feedback loop; CI macOS build is the verification gate for Apple-only targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Repudiation | Missing decision documentation | medium | mitigate | PROJECT.md Key Decisions table updated with all D-01..D-06 decisions — auditable trail |
| T-01-10 | Tampering | App shell importing non-allowed frameworks | low | accept | App shell is SwiftUI-only in Phase 1; no external imports; verified by CI compilation |
</threat_model>

<verification>
- swift build --target UnibrainCore exits 0 on WSL2 Linux
- swift test --test-product UnibrainCoreTests exits 0 with all tests passing
- No Apple-framework imports in Sources/UnibrainCore/ (DISC-02)
- GitHub Actions CI pipeline runs on push with both Linux and macOS jobs
- PROJECT.md Key Decisions table complete with all Phase 1 decisions
- SKELETON.md records architectural backbone
- UnibrainApp source files exist for future Xcode project
</verification>

<success_criteria>
- Apple Developer Program decision documented in PROJECT.md (FOUND-06, D-01)
- PROJECT.md reflects MacBook Neo hardware and macOS 26 / iOS 17 targets (D-03, D-05, D-06)
- Xcode app shell source files exist (FOUND-01 — multiplatform target structure)
- SKELETON.md locks architectural decisions for Phase 2-6
- Phase 1 walking skeleton verified end-to-end: Linux build + test + CI green
- All 9 phase requirements (FOUND-01..06, DISC-01..03) addressed
</success_criteria>

<artifacts_produced>
## Artifacts This Phase Produces

**Documentation:**
- .planning/PROJECT.md updated with 6+ Key Decisions rows (Apple Dev, public repo, MacBook Neo, deployment targets, bundle ID, Swift 6 concurrency)
- .planning/phases/01-foundation/SKELETON.md (Walking Skeleton architectural decision record)

**App Shell:**
- UnibrainApp/UnibrainApp.swift (@main App with WindowGroup + macOS MenuBarExtra)
- UnibrainApp/ContentView.swift (placeholder SwiftUI view)

**Verification:**
- Human-verified checkpoint confirming build + test + CI all green
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
