---
phase: 02-pure-pipeline-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/UnibrainCore/Schemas/FrontmatterSchema.swift
  - Sources/UnibrainCore/Normalization/NormalizedNote.swift
  - Sources/UnibrainCore/Normalization/NoteNormalizer.swift
  - Sources/UnibrainCore/Errors/FrontmatterValidationError.swift
  - Tests/UnibrainCoreTests/NormalizationTests/NoteNormalizerTests.swift
autonomous: true
requirements:
  - WRITE-01
  - WRITE-02
  - WRITE-03
user_setup: []

must_haves:
  truths:
    - A unit test on Linux feeds a mock transcript + mock course to NoteNormalizer and the output Markdown matches the expected frontmatter schema (all WRITE-02 fields present, correct YAML, schema_version: 1) and references the audio file via Obsidian wiki-link syntax
    - Paragraph grouping algorithm groups transcript segments by time-gap heuristic (3-second threshold) creating readable transcript structure
    - H1 title format follows YYYY-MM-DD — {course_code} Lecture pattern for Obsidian sortability
    - Audio file is referenced via ![[filename]] wiki-link syntax near the top of the note body
    - FrontmatterSchema.validate() method ensures required fields are non-empty before emitting note
  artifacts:
    - NormalizedNote.swift value type with title, body, frontmatter fields
    - NoteNormalizer.swift with normalize() and groupParagraphs() methods
    - FrontmatterSchema.swift extended with validate() method
    - FrontmatterValidationError.swift enum for validation failures
    - NoteNormalizerTests.swift with paragraph grouping, H1 format, wiki-link emission tests
  key_links:
    - NoteNormalizer depends on FrontmatterSchema for frontmatter structure
    - NoteNormalizer depends on FolderNameSanitizer (Plan 03) for course code sanitization — temporary stub until Plan 03 exists
    - FrontmatterSchema.validate() called by NoteNormalizer before emitting NormalizedNote
---

<objective>
Create the note shape contract: NormalizedNote value type, NoteNormalizer pure transform logic, FrontmatterSchema validation extension, and comprehensive Linux-runnable tests proving WRITE-01 (path format), WRITE-02 (12-field frontmatter), and WRITE-03 (audio wiki-link).

Purpose: Establish the core data contract for all notes written to the vault, ensuring Obsidian compatibility, structured frontmatter, and readable transcript formatting — all testable on Linux without Apple frameworks.

Output: NormalizedNote.swift, NoteNormalizer.swift, FrontmatterSchema.swift (with validate()), FrontmatterValidationError.swift, NoteNormalizerTests.swift — all building green on WSL2 Linux via swift test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md
@.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md
@.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md
@.planning/phases/02-pure-pipeline-logic/02-VALIDATION.md
@.planning/phases/01-foundation/01-CONTEXT.md

@Sources/UnibrainCore/Schemas/FrontmatterSchema.swift
@Sources/UnibrainCore/Errors/ProviderError.swift
@Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NormalizedNote value type</name>
  <files>Sources/UnibrainCore/Normalization/NormalizedNote.swift</files>
  <read_first>
    @Sources/UnibrainCore/Schemas/FrontmatterSchema.swift (value type pattern: public struct, Codable, Sendable, public init with all parameters)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 1: NormalizedNote analog mapping to FrontmatterSchema)
  </read_first>
  <behavior>
    Test 1: NormalizedNote constructs with title, body, and frontmatter fields
    Test 2: NormalizedNote is Sendable and can cross concurrency boundaries
    Test 3: NormalizedNote stores reference to FrontmatterSchema with all 12 fields
  </behavior>
  <action>
    Create Sources/UnibrainCore/Normalization/NormalizedNote.swift with:
    - public struct NormalizedNote: Sendable (NOT Codable — no serialization needed)
    - Three stored properties: title: String, body: String, frontmatter: FrontmatterSchema
    - Public init(title:body:frontmatter:) assigning all three fields
    - Doc comments explaining NormalizedNote carries the complete note content ready for NoteWriter

    Per PATTERNS.md Pattern 1: Copy FrontmatterSchema.swift value type pattern exactly (Codable, Sendable, public init), but omit Codable from NormalizedNote (no serialization needed).
  </action>
  <verify>
    <automated>swift test --filter NormalizedNoteTests exits 0</automated>
  </verify>
  <done>
    NormalizedNote.swift exists with public struct NormalizedNote: Sendable containing title: String, body: String, frontmatter: FrontmatterSchema and public init(title:body:frontmatter:). Tests pass confirming Sendable conformance and field storage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FrontmatterValidationError enum</name>
  <files>Sources/UnibrainCore/Errors/FrontmatterValidationError.swift</files>
  <read_first>
    @Sources/UnibrainCore/Errors/ProviderError.swift (error enum pattern: public enum, Error conformance, structured cases with doc comments)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 4: NoteWriterError analog mapping to ProviderError)
  </read_first>
  <behavior>
    Test 1: FrontmatterValidationError.emptyField("course") constructs
    Test 2: FrontmatterValidationError.invalidDuration(0) constructs
    Test 3: FrontmatterValidationError.missingRequiredField("datetime") constructs
    Test 4: Error enum is catchable as Error type
  </behavior>
  <action>
    Create Sources/UnibrainCore/Errors/FrontmatterValidationError.swift with:
    - public enum FrontmatterValidationError: Error, Sendable
    - Case emptyField(String) — required field is empty string
    - Case invalidDuration(Int) — duration_seconds is not positive
    - Case missingRequiredField(String) — field is nil but required
    - Doc comments for each case per CONTEXT.md Claude's Discretion
    - Follow ProviderError.swift enum structure exactly (lines 16-30): public enum: Error with associated values and /// doc comments
  </action>
  <verify>
    <automated>swift test --filter FrontmatterValidationErrorTests exits 0</automated>
  </verify>
  <done>
    FrontmatterValidationError.swift exists with public enum FrontmatterValidationError: Error, Sendable containing three cases (emptyField, invalidDuration, missingRequiredField). Tests pass confirming all cases construct and are catchable as Error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add FrontmatterSchema.validate() method</name>
  <files>Sources/UnibrainCore/Schemas/FrontmatterSchema.swift</files>
  <read_first>
    @Sources/UnibrainCore/Schemas/FrontmatterSchema.swift (existing struct with 12 fields)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 12: FrontmatterSchema validation method pattern)
  </read_first>
  <behavior>
    Test 1: validate() succeeds when all required fields are non-empty and duration > 0
    Test 2: validate() throws emptyField("course") when course is empty
    Test 3: validate() throws emptyField("course_name") when courseName is empty
    Test 4: validate() throws emptyField("term") when term is empty
    Test 5: validate() throws invalidDuration when durationSeconds <= 0
    Test 6: validate() throws missingRequiredField when tags is empty array
  </behavior>
  <action>
    Modify Sources/UnibrainCore/Schemas/FrontmatterSchema.swift by adding:
    - public func validate() throws method at the end of the struct (after existing init)
    - Validation checks per CONTEXT.md Claude's Discretion: course non-empty, courseName non-empty, term non-empty, durationSeconds > 0, tags non-empty
    - Throw FrontmatterValidationError.emptyField(fieldName) for empty string fields
    - Throw FrontmatterValidationError.invalidDuration(durationSeconds) for duration <= 0
    - Throw FrontmatterValidationError.missingRequiredField(fieldName) for empty tags array
    - Doc comment referencing WRITE-02 requirement for frontmatter validity

    Per PATTERNS.md Pattern 12: Add method to existing struct without modifying fields. Do NOT modify existing init or CodingKeys.
  </action>
  <verify>
    <automated>swift test --filter FrontmatterSchemaTests exits 0</automated>
  </verify>
  <done>
    FrontmatterSchema.validate() method added. All existing tests pass (4 FrontmatterSchemaTests + 3 new validation tests). validate() throws appropriately for empty fields, invalid duration, and missing required fields.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create NoteNormalizer with paragraph grouping</name>
  <files>Sources/UnibrainCore/Normalization/NoteNormalizer.swift</files>
  <read_first>
    @.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md (Code Examples: NoteNormalizer Paragraph Grouping Algorithm, lines 512-598)
    @Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift (pure static utility pattern reference — though NoteNormalizer is struct not actor)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 2: NoteNormalizer pure static utility pattern)
  </read_first>
  <behavior>
    Test 1: groupParagraphs with empty segments returns empty array
    Test 2: groupParagraphs with single segment returns single paragraph
    Test 3: groupParagraphs groups segments within 3-second gap into same paragraph
    Test 4: groupParagraphs starts new paragraph when gap >= 3 seconds
    Test 5: groupParagraphs handles consecutive segments with zero gap
    Test 6: groupParagraphs filters segments with empty/whitespace-only text before grouping
  </behavior>
  <action>
    Create Sources/UnibrainCore/Normalization/NoteNormalizer.swift with:
    - public struct NoteNormalizer (NOT class/actor — pure transformation)
    - static func groupParagraphs(segments:threshold:) returning [[String]] per RESEARCH.md lines 522-547
    - Algorithm: iterate segments, calculate gap = segment.start - lastEndTime, if gap >= threshold start new paragraph else append to current paragraph
    - Default threshold = 3.0 seconds (N-04 from CONTEXT.md)
    - Filter empty/whitespace-only segments before grouping (pitfall avoidance per RESEARCH.md)
    - Doc comments referencing N-03 (segments-in contract) and N-04 (3-second threshold)

    Per PATTERNS.md Pattern 2: Use struct with static methods (no state). Follow RESEARCH.md algorithm exactly.
  </action>
  <verify>
    <automated>swift test --filter NoteNormalizerTests exits 0</automated>
  </verify>
  <done>
    NoteNormalizer.groupParagraphs implemented with 3-second threshold algorithm. All 6 paragraph grouping tests pass including edge cases (empty input, zero gap, whitespace filtering).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create NoteNormalizer.normalize() method</name>
  <files>Sources/UnibrainCore/Normalization/NoteNormalizer.swift</files>
  <read_first>
    @Sources/UnibrainCore/Normalization/NoteNormalizer.swift (existing groupParagraphs method)
    @Sources/UnibrainCore/Schemas/FrontmatterSchema.swift (FrontmatterSchema init signature)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decisions N-01..04: note shape, H1 format, segments-in contract, paragraph threshold)
    @.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md (Code Examples: NoteNormalizer normalize() method, lines 549-598)
  </read_first>
  <behavior>
    Test 1: normalize() emits H1 title in YYYY-MM-DD — {course} Lecture format (N-02)
    Test 2: normalize() emits audio wiki-link ![[audio_file]] near top of body (N-01, WRITE-03)
    Test 3: normalize() emits ## Transcript section heading (N-01)
    Test 4: normalize() groups paragraphs into transcript body
    Test 5: normalize() creates FrontmatterSchema with all 12 WRITE-02 fields
    Test 6: normalize() calls frontmatter.validate() before returning NormalizedNote
    Test 7: normalize() does NOT emit ## Summary section (deferred to Phase 6 per N-01)
    Test 8: normalize() sanitizes course field using FolderNameSanitizer (stub for now, Plan 03)
  </behavior>
  <action>
    Modify Sources/UnibrainCore/Normalization/NoteNormalizer.swift by adding:
    - static func normalize(transcript:course:audioFile:recordingStart:durationSeconds:) returning NormalizedNote
    - Call groupParagraphs to get [[String]] paragraphs
    - Build transcript body: "## Transcript\n\n" + paragraphs.map { $0.joined(separator: " ") }.joined(separator: "\n\n")
    - Build H1 title using DateFormatter(yyyy-MM-dd) + " — " + course.title + " Lecture" per N-02
    - Build audio wiki-link: "\n![[audioFile]]\n" per N-01 and WRITE-03
    - Build full body: title + audioLink + transcriptBody (NO ## Summary section per N-01)
    - Create FrontmatterSchema with all 12 fields: schemaVersion=1, course=sanitized course.title (use FolderNameSanitizer as stub), courseName=course.title, term="Fall 2026" placeholder (deferred to Phase 4), datetime=recordingStart, durationSeconds, source="MacBook Air", audioFile, tags=["lecture"], nil for optional fields
    - Call frontmatter.validate() to ensure data integrity
    - Return NormalizedNote(title: title, body: body, frontmatter: frontmatter)
    - Doc comments referencing N-01..04 decisions and WRITE-01/02/03 requirements

    Per RESEARCH.md lines 549-598: Use exact algorithm specified. Stub FolderNameSanitizer.sanitize() call (will resolve in Plan 03).
  </action>
  <verify>
    <automated>swift test --filter NoteNormalizerTests exits 0</automated>
  </verify>
  <done>
    NoteNormalizer.normalize() implemented producing complete NormalizedNote. All 8 tests pass confirming H1 format, wiki-link syntax, transcript structure, frontmatter completeness, validation, and no summary section emission. FolderNameSanitizer stub present (Plan 03 will implement).
  </done>
</task>

<task type="auto">
  <name>Task 6: Create comprehensive NoteNormalizerTests</name>
  <files>Tests/UnibrainCoreTests/NormalizationTests/NoteNormalizerTests.swift</files>
  <read_first>
    @Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift (Swift Testing pattern: @Suite, @Test, #expect)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 13: Swift Testing pattern)
  </read_first>
  <action>
    Create Tests/UnibrainCoreTests/NormalizationTests/NoteNormalizerTests.swift with:
    - @Suite("NoteNormalizer") struct NoteNormalizerTests
    - All tests from Tasks 1, 4, 5 behavior blocks (18 total tests covering paragraph grouping edge cases, H1 format, wiki-link emission, frontmatter completeness, validation, and no-summary-section)
    - Use @Test macro for each test with descriptive names
    - Use #expect for assertions (no XCTAssertEqual)
    - Create fake CalendarEvent instances for normalize() tests (use UUID for id field)
    - Use temporary test data with realistic segment timestamps (e.g., 0.0-10.0, gap 4.0s, 14.0-25.0)
    - Doc comments referencing validation strategy per 02-VALIDATION.md

    Per PATTERNS.md Pattern 13: Copy FrontmatterSchemaTests.swift @Suite/@Test/#expect pattern exactly. All TDD behavior blocks from Tasks 1, 4, 5 map to test cases here.
  </action>
  <verify>
    <automated>swift test --filter NoteNormalizerTests exits 0 with 18 tests passing</automated>
  </verify>
  <done>
    NoteNormalizerTests.swift created with 18 tests covering paragraph grouping (6), H1 format (2), wiki-link emission (2), frontmatter completeness (3), validation (2), no-summary-section (1), NormalizedNote construction (2). All tests pass on Linux.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NoteNormalizer input | Transcript segments and course metadata from untrusted sources (ASR engine, EventKit adapter) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | FolderNameSanitizer stub | low | accept | Temporary stub in Plan 01; Plan 03 implements full sanitization. Current risk: unsanitized course title in frontmatter field. |
| T-2-02 | Tampering | NoteNormalizer paragraph grouping | low | accept | Pure transformation logic, no filesystem access. Malicious segment timestamps can only produce malformed transcript structure (no security impact). |
| T-2-03 | Information Disclosure | FrontmatterSchema validation | medium | mitigate | validate() ensures required fields are non-empty before emitting note, preventing null/empty frontmatter from reaching vault. FrontmatterValidationError provides structured error types. |
| T-2-04 | Tampering | FrontmatterSchema YAML encoding | medium | mitigate | Yams library (Phase 1 verified) handles YAML encoding with proper escaping. snake_case CodingKeys ensure consistent output. validate() pre-checks data before encoding. |
</threat_model>

<verification>
- swift test --filter NoteNormalizerTests exits 0 with 18 tests passing
- swift test --filter NormalizedNoteTests exits 0
- swift test --filter FrontmatterValidationErrorTests exits 0
- swift test --filter FrontmatterSchemaTests exits 0 (existing 4 tests + 3 new validation tests)
- All tests pass on WSL2 Linux (no Apple frameworks required)
- NormalizedNote.swift, NoteNormalizer.swift, FrontmatterValidationError.swift exist in Sources/UnibrainCore/
- NoteNormalizerTests.swift exists in Tests/UnibrainCoreTests/NormalizationTests/
- No .icloud detection code in this plan (Plan 02 covers atomic write threats)
</verification>

<success_criteria>
A unit test on Linux feeds a mock transcript + mock course to NoteNormalizer and the output Markdown matches the expected frontmatter schema (all WRITE-02 fields present, correct YAML, schema_version: 1) and references the audio file via Obsidian wiki-link syntax ![[audio.m4a]].

Paragraph grouping algorithm groups transcript segments by time-gap heuristic (3-second threshold) creating readable transcript structure — verified by 6 edge-case tests.

H1 title format follows YYYY-MM-DD — {course_code} Lecture pattern — verified by 2 format tests.

Audio file is referenced via ![[filename]] wiki-link syntax near the top of the note body — verified by 2 wiki-link tests.

FrontmatterSchema.validate() method ensures required fields are non-empty before emitting note — verified by 3 validation tests throwing appropriate errors.

All 30 tests (18 NoteNormalizer + 3 NormalizedNote + 3 FrontmatterValidationError + 4 FrontmatterSchema + 2 existing) pass on WSL2 Linux proving WRITE-01, WRITE-02, WRITE-03 requirements are met.
</success_criteria>

<output>
Create .planning/phases/02-pure-pipeline-logic/02-01-SUMMARY.md when done
</output>
