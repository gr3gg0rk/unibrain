---
phase: 02-pure-pipeline-logic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/UnibrainCore/Normalization/NoteWriter.swift
  - Sources/UnibrainCore/Errors/NoteWriterError.swift
  - Tests/UnibrainCoreTests/NormalizationTests/NoteWriterTests.swift
autonomous: true
requirements:
  - WRITE-04
  - WRITE-05
  - WRITE-06
user_setup: []

must_haves:
  truths:
    - A unit test on Linux writes a note via TestNoteWriter to a temp directory using FileManager .atomic flag and the file round-trips (read-back content matches)
    - A unit test on Linux simulates a .icloud placeholder file and verifies TestNoteWriter detects and throws NoteWriterError.iCloudPlaceholder (WRITE-05)
    - A unit test on Linux forces a write failure and verifies NoteWriterError is surfaced with structured case (disk full, permission denied, etc.) — no silent swallow (WRITE-06)
    - NoteWriter protocol defines write(_:to:) async throws signature per A-02
    - NoteWriterError enum provides structured error cases (.iCloudPlaceholder, .diskFull, .permissionDenied, .alreadyExists, .directoryCreationFailed, .underlying) per A-04
    - TestNoteWriter creates intermediate directories recursively before writing per A-05
  artifacts:
    - NoteWriter.swift protocol with write(_:to:) signature
    - NoteWriterError.swift enum with 6 structured error cases
    - NoteWriterTests.swift with TestNoteWriter conformance and comprehensive error case tests
  key_links:
    - NoteWriter depends on NormalizedNote from Plan 01 for input type
    - NoteWriter protocol will be conformed by NSFileCoordinatorNoteWriter in Plan 03 (Phase 3)
    - TestNoteWriter uses FileManager .atomic option for cross-platform atomic writes
    - NoteWriterError follows ProviderError pattern from Phase 1
---

<objective>
Create the atomic write contract: NoteWriter protocol, NoteWriterError structured error enum, and TestNoteWriter Linux conformance with comprehensive tests proving WRITE-04 (atomic write), WRITE-05 (.icloud detection), and WRITE-06 (structured error surfacing).

Purpose: Establish the filesystem write abstraction that Platform conformances (Phase 3 NSFileCoordinatorNoteWriter) must satisfy, ensuring atomic writes, iCloud placeholder detection, and clear error propagation — all testable on Linux without Apple frameworks.

Output: NoteWriter.swift protocol, NoteWriterError.swift enum, NoteWriterTests.swift with TestNoteWriter conformance — all building green on WSL2 Linux via swift test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md
@.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md
@.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md
@.planning/phases/02-pure-pipeline-logic/02-VALIDATION.md
@.planning/phases/01-foundation/01-CONTEXT.md

@Sources/UnibrainCore/Errors/ProviderError.swift
@Sources/UnibrainCore/Protocols/AudioTranscriber.swift
@Tests/UnibrainCoreTests/ProviderProtocolTests.swift
@Sources/UnibrainCore/Normalization/NormalizedNote.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NoteWriterError enum</name>
  <files>Sources/UnibrainCore/Errors/NoteWriterError.swift</files>
  <read_first>
    @Sources/UnibrainCore/Errors/ProviderError.swift (error enum pattern: public enum: Error, structured cases with associated values, /// doc comments)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 4: NoteWriterError analog mapping to ProviderError)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decision A-04: NoteWriterError cases)
  </read_first>
  <behavior>
    Test 1: NoteWriterError.iCloudPlaceholder(URL) constructs with URL parameter
    Test 2: NoteWriterError.diskFull constructs
    Test 3: NoteWriterError.permissionDenied(URL) constructs with URL parameter
    Test 4: NoteWriterError.alreadyExists(URL) constructs with URL parameter
    Test 5: NoteWriterError.directoryCreationFailed(URL, underlying:) constructs with URL and Error parameters
    Test 6: NoteWriterError.underlying(any Error) constructs with Error parameter
    Test 7: All six cases are catchable as Error type
  </behavior>
  <action>
    Create Sources/UnibrainCore/Errors/NoteWriterError.swift with:
    - public enum NoteWriterError: Error (NOT Sendable — follows ProviderError pattern which intentionally omits Sendable due to underlying(any Error))
    - Case iCloudPlaceholder(URL) — .icloud placeholder detected per A-03
    - Case diskFull — disk space exhausted
    - Case permissionDenied(URL) — filesystem permission denied per A-04
    - Case alreadyExists(URL) — file already exists per A-04
    - Case directoryCreationFailed(URL, underlying: any Error) — recursive directory creation failed per A-04
    - Case underlying(any Error) — catch-all for filesystem errors per A-04
    - Doc comments for each case explaining when it occurs and referencing WRITE-05 (.icloud) and WRITE-06 (clear errors)
    - Follow ProviderError.swift structure exactly (lines 16-30): enum with associated values and /// doc comments

    Per PATTERNS.md Pattern 4: Copy ProviderError enum pattern exactly. NoteWriterError mirrors ProviderError shape for consistency across the codebase.
  </action>
  <verify>
    <automated>swift test --filter NoteWriterErrorTests exits 0</automated>
  </verify>
  <done>
    NoteWriterError.swift exists with public enum NoteWriterError: Error containing all six cases (iCloudPlaceholder, diskFull, permissionDenied, alreadyExists, directoryCreationFailed, underlying). All 7 tests pass confirming case construction and Error catchability.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NoteWriter protocol</name>
  <files>Sources/UnibrainCore/Normalization/NoteWriter.swift</files>
  <read_first>
    @Sources/UnibrainCore/Protocols/AudioTranscriber.swift (protocol pattern: public protocol with async throws method, doc comments)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 3: NoteWriter protocol analog mapping to AudioTranscriber)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decisions A-01: NoteWriter protocol, A-02: write signature)
  </read_first>
  <behavior>
    Test 1: NoteWriter protocol exists with write(_:to:) signature
    Test 2: Protocol is public and can be conformed in test target
    Test 3: Protocol method is async throws (allows async filesystem operations and error throwing)
  </behavior>
  <action>
    Create Sources/UnibrainCore/Normalization/NoteWriter.swift with:
    - public protocol NoteWriter
    - func write(_ note: NormalizedNote, to destination: URL) async throws
    - Doc comment explaining: Writes NormalizedNote to destination URL atomically (WRITE-04), detects .icloud placeholders (WRITE-05), throws NoteWriterError on failure (WRITE-06), creates intermediate directories recursively (A-05)
    - No associatedtype or default implementation — simple specific signature per A-02
    - Follow AudioTranscriber.swift protocol structure exactly (lines 7-17): public protocol, async throws method, /// doc comment

    Per PATTERNS.md Pattern 3: NoteWriter is simpler than provider protocols — no associatedtype needed. Specific signature: NormalizedNote input, URL destination, void return (success implicit), throws NoteWriterError.
  </action>
  <verify>
    <automated>swift test --filter NoteWriterProtocolTests exits 0</automated>
  </verify>
  <done>
    NoteWriter.swift exists with public protocol NoteWriter containing func write(_:to:) async throws signature. Doc comment references WRITE-04, WRITE-05, WRITE-06, and A-05. Protocol conformance tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create TestNoteWriter conformance</name>
  <files>Tests/UnibrainCoreTests/NormalizationTests/NoteWriterTests.swift</files>
  <read_first>
    @Tests/UnibrainCoreTests/ProviderProtocolTests.swift (mock protocol conformance pattern: private struct MockXXX: Protocol with func implementation)
    @.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md (Code Examples: TestNoteWriter implementation, lines 477-507)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 14: TestNoteWriter pattern)
    @Sources/UnibrainCore/Schemas/FrontmatterSchema.swift (Yams encoding pattern)
  </read_first>
  <behavior>
    Test 1: TestNoteWriter conforms to NoteWriter protocol
    Test 2: write(_:to:) creates intermediate directories recursively
    Test 3: write(_:to:) serializes frontmatter to YAML using Yams
    Test 4: write(_:to:) writes content atomically using FileManager .atomic option
    Test 5: write(_:to:) throws iCloudPlaceholder when destination contains .icloud in path
    Test 6: write(_:to:) throws permissionDenied when FileManager.createDirectory fails
    Test 7: write(_:to:) throws alreadyExists when file exists and .atomic fails
    Test 8: write(_:to:) round-trip test: write then read content matches
  </behavior>
  <action>
    Create Tests/UnibrainCoreTests/NormalizationTests/NoteWriterTests.swift with:
    - @Suite("NoteWriter") struct NoteWriterTests
    - private struct TestNoteWriter: NoteWriter conformance (internal to test file)
    - func write(_ note: NormalizedNote, to destination: URL) async throws implementation:
      - Check destination.pathComponents.contains(".icloud") -> throw NoteWriterError.iCloudPlaceholder(destination) per A-03 and WRITE-05
      - Create directory with FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true) per A-05
      - Serialize frontmatter: let yamlFrontmatter = try YAMLEncoder().encode(note.frontmatter)
      - Build content: "\(yamlFrontmatter)\n\n\(note.body)"
      - Write atomically: try content.write(to: destination, atomically: true, encoding: .utf8) per WRITE-04
    - Tests covering all 8 behavior cases above using @Test macro and #expect assertions
    - Use FileManager.temporaryDirectory for test destinations
    - Use defer blocks to clean up temp files
    - Mock FileManager errors for permission denied and already exists cases (use non-existent directory or create file first)
    - Doc comments referencing validation strategy per 02-VALIDATION.md

    Per PATTERNS.md Pattern 14: TestNoteWriter lives in test file, uses FileManager temp + .atomic write. Follow RESEARCH.md lines 477-507 implementation exactly.
  </action>
  <verify>
    <automated>swift test --filter NoteWriterTests exits 0 with all tests passing</automated>
  </verify>
  <done>
    NoteWriterTests.swift created with TestNoteWriter: NoteWriter conformance. All 8 tests pass proving directory creation, YAML serialization, atomic write, .icloud detection (WRITE-05), error surfacing (WRITE-06), and round-trip consistency (WRITE-04).
  </done>
</task>

<task type="auto">
  <name>Task 4: Create comprehensive NoteWriterError tests</name>
  <files>Tests/UnibrainCoreTests/NormalizationTests/NoteWriterTests.swift</files>
  <read_first>
    @Tests/UnibrainCoreTests/ProviderProtocolTests.swift (ProviderError all-cases test pattern, lines 79-116)
    @Sources/UnibrainCore/Errors/NoteWriterError.swift (all six error cases)
  </read_first>
  <action>
    Modify Tests/UnibrainCoreTests/NormalizationTests/NoteWriterTests.swift by adding:
    - Additional test cases for all six NoteWriterError cases
    - Test: all NoteWriterError cases are constructible and catchable as Error type (analogous to ProviderProtocolTests line 79-116)
    - Test: iCloudPlaceholder case carries URL parameter correctly
    - Test: directoryCreationFailed case carries both URL and underlying Error
    - Test: underlying case carries any Error correctly
    - Use do/catch loops to verify each error case is caught as NoteWriterError (not generic Error)
    - Use #expect(throws: NoteWriterError.self) for structured error assertions
    - Doc comments per Swift Testing pattern

    Per ProviderProtocolTests.swift pattern: Test all enum cases, verify parameterized associated values, confirm Error catchability.
  </action>
  <verify>
    <automated>swift test --filter NoteWriterTests exits 0 with comprehensive error coverage</automated>
  </verify>
  <done>
    NoteWriterTests.swift extended with NoteWriterError case coverage tests. All 6 error cases verified constructible with correct associated values and catchable as Error type. Total test count increases appropriately.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem write | TestNoteWriter writes to temp directories; .icloud placeholder detection prevents silent failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02 | Tampering | .icloud placeholder detection | high | mitigate | TestNoteWriter checks destination.pathComponents.contains(".icloud") and throws NoteWriterError.iCloudPlaceholder per A-03 and WRITE-05. Prevents silent data loss from writing to not-yet-downloaded iCloud files. |
| T-2-05 | Denial of Service | Directory creation failure | medium | mitigate | NoteWriterError.directoryCreationFailed(URL, underlying: any Error) surfaces underlying filesystem error with context per A-04. Caller can retry or surface clear message. |
| T-2-06 | Information Disclosure | Error message leakage | medium | accept | NoteWriterError cases carry structured data (URL, underlying Error) but no sensitive file contents. Error messages are for debugging — acceptable. |
| T-2-07 | Tampering | Atomic write corruption | medium | mitigate | FileManager .atomic option uses POSIX rename(2) which is atomic on Linux/macOS per RESEARCH.md. Prevents partial write corruption if process crashes mid-write. |
</threat_model>

<verification>
- swift test --filter NoteWriterTests exits 0 with all tests passing
- swift test --filter NoteWriterErrorTests exits 0
- swift test --filter NoteWriterProtocolTests exits 0
- All tests pass on WSL2 Linux (no Apple frameworks required)
- NoteWriter.swift protocol exists with write(_:to:) async throws signature
- NoteWriterError.swift enum exists with all 6 structured cases
- NoteWriterTests.swift exists with TestNoteWriter conformance
- TestNoteWriter uses FileManager .atomic option verified in source
- .icloud detection logic verified in TestNoteWriter.write implementation
</verification>

<success_criteria>
A unit test on Linux writes a note via TestNoteWriter to a temp directory using FileManager .atomic flag and the file round-trips (read-back content matches) — verified by atomic write and round-trip tests.

A unit test on Linux simulates a .icloud placeholder file and verifies TestNoteWriter detects and throws NoteWriterError.iCloudPlaceholder — verified by .icloud detection test with path containing ".icloud" component.

A unit test on Linux forces a write failure and verifies NoteWriterError is surfaced with structured case — verified by permission denied, already exists, and directory creation failed tests throwing appropriate NoteWriterError cases.

NoteWriter protocol defines write(_:to:) async throws signature — verified by protocol conformance and signature tests.

NoteWriterError enum provides 6 structured error cases — verified by all-cases construction and catchability tests.

TestNoteWriter creates intermediate directories recursively before writing — verified by directory creation test using FileManager.temporaryDirectory subdirectories.

All tests pass on WSL2 Linux proving WRITE-04, WRITE-05, WRITE-06 requirements are met with pure Foundation (no Apple frameworks).
</success_criteria>

<output>
Create .planning/phases/02-pure-pipeline-logic/02-02-SUMMARY.md when done
</output>
