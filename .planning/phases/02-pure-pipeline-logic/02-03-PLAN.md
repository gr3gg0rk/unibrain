---
phase: 02-pure-pipeline-logic
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/UnibrainCore/Classification/CalendarEvent.swift
  - Sources/UnibrainCore/Classification/CourseMatch.swift
  - Sources/UnibrainCore/Classification/CourseClassifier.swift
  - Sources/UnibrainCore/Classification/FolderNameSanitizer.swift
  - Tests/UnibrainCoreTests/ClassificationTests/CourseClassifierTests.swift
  - Tests/UnibrainCoreTests/ClassificationTests/FolderNameSanitizerTests.swift
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - CourseClassifier.match(events:against:window:) returns .single when exactly one event overlaps recordingStart ± 30min window (C-01, C-02, C-03)
    - CourseClassifier.match returns .multiple when 2+ events overlap the window (C-02)
    - CourseClassifier.match returns .none when zero events overlap (C-02)
    - FolderNameSanitizer.sanitize strips reserved characters (/, :, newline, carriage return) from folder names (C-05)
    - FolderNameSanitizer.sanitize strips leading dots to prevent hidden-file creation (C-05)
    - FolderNameSanitizer.sanitize collapses whitespace runs and enforces 100-character max length (C-05)
    - FolderNameSanitizer.sanitize prevents path traversal vectors (../../etc/passwd) (T-2-01 mitigation)
    - CalendarEvent struct carries id, title, startDate, endDate, location? fields (C-01)
    - CourseMatch enum has three cases: .single(CalendarEvent), .multiple([CalendarEvent]), .none (C-02)
  artifacts:
    - CalendarEvent.swift Sendable struct with 5 fields
    - CourseMatch.swift Sendable enum with 3 cases
    - CourseClassifier.swift with match(events:against:window:) static method
    - FolderNameSanitizer.swift with sanitize(folderName:) static method
    - CourseClassifierTests.swift with time-overlap matching tests
    - FolderNameSanitizerTests.swift with character-stripping and path-traversal tests
  key_links:
    - CalendarEvent will be populated by Phase 4's EventKit adapter
    - CourseClassifier pure logic used by PipelineOrchestrator in Plan 04
    - FolderNameSanitizer used by NoteNormalizer in Plan 01 (stub to be replaced with real call)
    - CourseClassifier.match returns raw CalendarEvent — Phase 4 resolves to course code
---

<objective>
Create the classification pure logic: CalendarEvent value type, CourseMatch enum, CourseClassifier time-overlap matcher, FolderNameSanitizer filesystem safety, and comprehensive Linux-runnable tests proving C-01..C05 decisions (event structure, ±30min overlap matching, three-state output, folder name sanitization, path traversal protection).

Purpose: Establish the pure-logic calendar-to-course matching and folder name safety that Phase 4's EventKit adapter and PipelineOrchestrator will consume — all testable on Linux without Apple frameworks.

Output: CalendarEvent.swift, CourseMatch.swift, CourseClassifier.swift, FolderNameSanitizer.swift, CourseClassifierTests.swift, FolderNameSanitizerTests.swift — all building green on WSL2 Linux via swift test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md
@.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md
@.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md
@.planning/phases/02-pure-pipeline-logic/02-VALIDATION.md
@.planning/phases/01-foundation/01-CONTEXT.md

@Sources/UnibrainCore/Schemas/FrontmatterSchema.swift (Sendable struct pattern)
@Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift (enum with associated values pattern)
@Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift (Swift Testing pattern)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CalendarEvent value type</name>
  <files>Sources/UnibrainCore/Classification/CalendarEvent.swift</files>
  <read_first>
    @Sources/UnibrainCore/Schemas/FrontmatterSchema.swift (Sendable struct pattern: public struct, Codable, Sendable, public init)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 5: CalendarEvent analog mapping to FrontmatterSchema)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decision C-01: CalendarEvent fields)
  </read_first>
  <behavior>
    Test 1: CalendarEvent constructs with id, title, startDate, endDate, location
    Test 2: CalendarEvent constructs with location: nil (optional field)
    Test 3: CalendarEvent is Sendable and can cross concurrency boundaries
    Test 4: CalendarEvent.id is stable identifier (can use UUID in tests)
  </behavior>
  <action>
    Create Sources/UnibrainCore/Classification/CalendarEvent.swift with:
    - public struct CalendarEvent: Codable, Sendable
    - Five stored properties per C-01: id: String, title: String, startDate: Date, endDate: Date, location: String?
    - Public init(id:title:startDate:endDate:location:) with location defaulting to nil
    - Doc comments explaining CalendarEvent is Apple-framework-agnostic abstraction for calendar events
    - Follow FrontmatterSchema.swift struct pattern exactly (lines 12-79): public struct, Codable, Sendable, public var fields, public init with all parameters
    - Note: Codable conformance enables Phase 4 EventKit adapter serialization if needed (though not required in Phase 2)

    Per PATTERNS.md Pattern 5: Copy FrontmatterSchema Sendable struct pattern exactly. 5 fields per CONTEXT.md C-01. Optional location uses String?.
  </action>
  <verify>
    <automated>swift test --filter CalendarEventTests exits 0</automated>
  </verify>
  <done>
    CalendarEvent.swift exists with public struct CalendarEvent: Codable, Sendable containing id, title, startDate, endDate, location? fields. All 4 tests pass confirming construction, optional handling, and Sendable conformance.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CourseMatch enum</name>
  <files>Sources/UnibrainCore/Classification/CourseMatch.swift</files>
  <read_first>
    @Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift (enum with associated values pattern: public enum, Sendable, cases with payloads)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 6: CourseMatch analog mapping to ModelLoadGateError)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decision C-02: three-state enum)
  </read_first>
  <behavior>
    Test 1: CourseMatch.single(CalendarEvent) constructs with event parameter
    Test 2: CourseMatch.multiple([CalendarEvent]) constructs with array parameter
    Test 3: CourseMatch.none constructs (no associated value)
    Test 4: CourseMatch is Sendable and can cross concurrency boundaries
    Test 5: CourseMatch is NOT an Error type (no : Error conformance)
  </behavior>
  <action>
    Create Sources/UnibrainCore/Classification/CourseMatch.swift with:
    - public enum CourseMatch: Sendable (NOT : Error — C-02 specifies three-state match result, not error)
    - Case single(CalendarEvent) — exactly one event overlaps per C-02
    - Case multiple([CalendarEvent]) — 2+ events overlap per C-02
    - Case none — zero events overlap per C-02
    - Doc comments for each case explaining when it occurs and how Phase 4 should handle it
    - Follow ModelLoadGateError.swift enum structure (lines 8-12) but omit : Error conformance

    Per PATTERNS.md Pattern 6: Copy ModelLoadGateError enum pattern EXCEPT for : Error conformance. CourseMatch is a result type, not an error type. Three cases per CONTEXT.md C-02.
  </action>
  <verify>
    <automated>swift test --filter CourseMatchTests exits 0</automated>
  </verify>
  <done>
    CourseMatch.swift exists with public enum CourseMatch: Sendable containing three cases (single, multiple, none). All 5 tests pass confirming case construction with associated values, Sendable conformance, and non-Error type.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create CourseClassifier matcher</name>
  <files>Sources/UnibrainCore/Classification/CourseClassifier.swift</files>
  <read_first>
    @.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md (Code Examples: CourseClassifier time-overlap matching, lines 601-643)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 7: CourseClassifier pure static matcher pattern)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decisions C-03: ±30min window, C-04: pure matcher only)
  </read_first>
  <behavior>
    Test 1: match returns .single when one event overlaps the window
    Test 2: match returns .multiple when 2+ events overlap the window
    Test 3: match returns .none when zero events overlap the window
    Test 4: match handles exact boundary conditions (event starts exactly at windowEnd)
    Test 5: match handles events that fully contain the recording window
    Test 6: match respects ±30min default window (1800 seconds) per C-03
    Test 7: match accepts custom window parameter for testing flexibility
  </behavior>
  <action>
    Create Sources/UnibrainCore/Classification/CourseClassifier.swift with:
    - public struct CourseClassifier (NOT class/actor — pure static function)
    - static func match(events:against:window:) returning CourseMatch per RESEARCH.md lines 613-636
    - Time-overlap algorithm per C-03: windowStart = recordingStart - window, windowEnd = recordingStart + window
    - Overlap condition: event.startDate <= windowEnd AND event.endDate >= windowStart (standard interval overlap)
    - Filter events to overlapping array, switch on count: 0 -> .none, 1 -> .single(overlapping[0]), else -> .multiple(overlapping)
    - Default window = 1800 seconds (±30min) per C-03
    - Doc comments referencing C-01..C-04 decisions and CLAS-01 requirement
    - Follow RESEARCH.md lines 601-643 algorithm exactly

    Per PATTERNS.md Pattern 7: Use struct with static func (no state). Time-overlap algorithm per CONTEXT.md C-03. Return CourseMatch enum per C-02.
  </action>
  <verify>
    <automated>swift test --filter CourseClassifierTests exits 0</automated>
  </verify>
  <done>
    CourseClassifier.swift exists with match(events:against:window:) static method implementing ±30min time-overlap algorithm. All 7 tests pass covering single/multiple/none cases, boundary conditions, window parameterization, and default 30-minute window.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create FolderNameSanitizer</name>
  <files>Sources/UnibrainCore/Classification/FolderNameSanitizer.swift</files>
  <read_first>
    @.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md (Code Examples: FolderNameSanitizer implementation, lines 645-694)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 8: FolderNameSanitizer pure static sanitizer pattern)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decision C-05: sanitization rules)
  </read_first>
  <behavior>
    Test 1: sanitize strips reserved characters (/, :, newline, carriage return)
    Test 2: sanitize strips leading dots to prevent hidden-file creation
    Test 3: sanitize collapses whitespace runs to single space
    Test 4: sanitize trims leading/trailing whitespace
    Test 5: sanitize enforces 100-character max length
    Test 6: sanitize returns "Untitled Course" for empty-string input
    Test 7: sanitize prevents path traversal vectors (../../etc/passwd -> .. etc passwd)
    Test 8: sanitize handles Unicode characters correctly (emoji, accents)
  </behavior>
  <action>
    Create Sources/UnibrainCore/Classification/FolderNameSanitizer.swift with:
    - public struct FolderNameSanitizer (NOT class/actor — pure static function)
    - static func sanitize(folderName:) returning String per RESEARCH.md lines 663-693
    - Sanitization steps per C-05:
      1. Replace reserved characters: /, :, \n, \r -> space
      2. Strip leading dots: while hasPrefix(".") { removeFirst() }
      3. Collapse whitespace: /\s+/ regex -> single space (Swift 6 regex literal)
      4. Trim: trimmingCharacters(in: .whitespaces)
      5. Enforce max length: if count > 100 { prefix(100).trim(.whitespaces) }
      6. Return "Untitled Course" if isEmpty after sanitization
    - Doc comments referencing C-05, T-2-01 (path traversal), and filesystem safety
    - Follow RESEARCH.md lines 645-694 implementation exactly

    Per PATTERNS.md Pattern 8: Use struct with static func. Follow CONTEXT.md C-05 rules exactly. Use Swift 6 regex literal /\s+/ for whitespace collapsing.
  </action>
  <verify>
    <automated>swift test --filter FolderNameSanitizerTests exits 0</automated>
  </verify>
  <done>
    FolderNameSanitizer.swift exists with sanitize(folderName:) static method implementing character stripping, whitespace collapsing, length limiting, and path-traversal prevention. All 8 tests pass including edge cases (empty string, Unicode, path traversal vectors).
  </done>
</task>

<task type="auto">
  <name>Task 5: Create CourseClassifierTests</name>
  <files>Tests/UnibrainCoreTests/ClassificationTests/CourseClassifierTests.swift</files>
  <read_first>
    @Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift (Swift Testing pattern: @Suite, @Test, #expect)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 15: CourseClassifierTests analog mapping to ModelLoadGateTests)
  </read_first>
  <action>
    Create Tests/UnibrainCoreTests/ClassificationTests/CourseClassifierTests.swift with:
    - @Suite("CourseClassifier") struct CourseClassifierTests
    - All tests from Task 3 behavior block (7 tests covering single/multiple/none cases, boundary conditions, window parameterization)
    - Create fake CalendarEvent instances with UUID().uuidString for id field
    - Use Date(timeIntervalSince1970:) for test timestamps (e.g., recordingStart = 0, event at -1800..+1800 for exact ±30min overlap)
    - Edge cases: event ends exactly at windowStart (should NOT overlap), event starts exactly at windowEnd (should overlap), event fully contains window (should overlap)
    - Parameterized test for window values (30min, 15min, 60min) if Swift Testing parameterization is straightforward
    - Doc comments per Swift Testing pattern

    Per PATTERNS.md Pattern 15: Copy ModelLoadGateTests.swift @Suite/@Test/#expect pattern. Test CourseClassifier.match() with fake CalendarEvent instances and UUIDs.
  </action>
  <verify>
    <automated>swift test --filter CourseClassifierTests exits 0 with 7 tests passing</automated>
  </verify>
  <done>
    CourseClassifierTests.swift created with 7 tests covering time-overlap matching logic. All tests pass confirming single/multiple/none cases, boundary conditions, window parameterization, and default ±30min window behavior.
  </done>
</task>

<task type="auto">
  <name>Task 6: Create FolderNameSanitizerTests</name>
  <files>Tests/UnibrainCoreTests/ClassificationTests/FolderNameSanitizerTests.swift</files>
  <read_first>
    @Tests/UnibrainCoreTests/FrontmatterSchemaTests.swift (Swift Testing pattern)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 16: FolderNameSanitizerTests analog mapping to FrontmatterSchemaTests)
  </read_first>
  <action>
    Create Tests/UnibrainCoreTests/ClassificationTests/FolderNameSanitizerTests.swift with:
    - @Suite("FolderNameSanitizer") struct FolderNameSanitizerTests
    - All tests from Task 4 behavior block (8 tests covering character stripping, whitespace collapsing, length limiting, path traversal, Unicode)
    - Test cases: "CS/101:Intro" -> "CS 101 Intro", "...Course" -> "Course", "CS101   Intro" -> "CS101 Intro", path traversal vectors, empty string -> "Untitled Course"
    - Use #expect for assertions (no XCTAssertEqual)
    - Parameterized tests for reserved characters if Swift Testing supports it (/, :, \n, \r)
    - Unicode test cases: "Mathématique" preserves accents, "🧮 Course" preserves emoji
    - Path traversal test: "../../etc/passwd" -> ".. etc passwd" (stripped slashes but structure preserved)
    - Doc comments referencing T-2-01 mitigation strategy

    Per PATTERNS.md Pattern 16: Copy FrontmatterSchemaTests.swift @Suite/@Test/#expect pattern. Test sanitize() method per CONTEXT.md C-05 rules.
  </action>
  <verify>
    <automated>swift test --filter FolderNameSanitizerTests exits 0 with 8 tests passing</automated>
  </verify>
  <done>
    FolderNameSanitizerTests.swift created with 8 tests covering sanitization rules, path traversal prevention, and Unicode handling. All tests pass confirming reserved character stripping, whitespace collapsing, length limiting, and T-2-01 mitigation.
  </done>
</task>

<task type="auto">
  <name>Task 7: Update NoteNormalizer to call FolderNameSanitizer</name>
  <files>Sources/UnibrainCore/Normalization/NoteNormalizer.swift</files>
  <read_first>
    @Sources/UnibrainCore/Normalization/NoteNormalizer.swift (existing normalize() method with FolderNameSanitizer stub)
    @Sources/UnibrainCore/Classification/FolderNameSanitizer.swift (real FolderNameSanitizer.sanitize method)
  </read_first>
  <action>
    Modify Sources/UnibrainCore/Normalization/NoteNormalizer.swift by:
    - Replacing FolderNameSanitizer stub comment with real call: FolderNameSanitizer.sanitize(folderName: course.title)
    - Remove any TODO or stub comments referencing Plan 03
    - Update normalize() method to use real sanitization for frontmatter course field
    - Keep doc comments referencing C-05 for sanitization rationale
    - Verify existing tests still pass (no behavior change, just stub resolution)
  </action>
  <verify>
    <automated>swift test --filter NoteNormalizerTests exits 0 with all tests passing</automated>
  </verify>
  <done>
    NoteNormalizer.swift updated to use real FolderNameSanitizer.sanitize() call instead of stub. All existing NoteNormalizerTests pass (18 tests) confirming sanitization integration works correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CourseClassifier input | Calendar events from untrusted source (EventKit adapter in Phase 4) |
| FolderNameSanitizer input | Course titles from untrusted source (calendar event titles) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | FolderNameSanitizer | high | mitigate | FolderNameSanitizer strips path-traversal characters (/ and :) and leading dots per C-05. Prevents ../../etc/passwd attacks on vault folder structure. Sanitization applied before any filesystem use. |
| T-2-08 | Tampering | CourseClassifier time overlap | low | accept | Pure date arithmetic — malicious timestamp can only cause incorrect course routing (no security impact). Phase 4 manual picker fallback handles ambiguous cases. |
| T-2-09 | Information Disclosure | CalendarEvent.location field | low | accept | Optional location field may contain room data — not sensitive in university context. No PII. |
</threat_model>

<verification>
- swift test --filter CourseClassifierTests exits 0 with 7 tests passing
- swift test --filter FolderNameSanitizerTests exits 0 with 8 tests passing
- swift test --filter CalendarEventTests exits 0
- swift test --filter CourseMatchTests exits 0
- swift test --filter NoteNormalizerTests exits 0 (Plan 01 tests still pass after FolderNameSanitizer integration)
- All tests pass on WSL2 Linux (no Apple frameworks required)
- CalendarEvent.swift, CourseMatch.swift, CourseClassifier.swift, FolderNameSanitizer.swift exist in Sources/UnibrainCore/Classification/
- CourseClassifierTests.swift, FolderNameSanitizerTests.swift exist in Tests/UnibrainCoreTests/ClassificationTests/
- Path traversal test verified in FolderNameSanitizerTests
- Time-overlap algorithm verified in CourseClassifierTests including edge cases
</verification>

<success_criteria>
CourseClassifier.match(events:against:window:) returns .single when exactly one event overlaps recordingStart ± 30min window — verified by 3 tests (exact overlap, fully-contained, boundary conditions).

CourseClassifier.match returns .multiple when 2+ events overlap the window — verified by test with 2 overlapping events.

CourseClassifier.match returns .none when zero events overlap — verified by test with non-overlapping events.

FolderNameSanitizer.sanitize strips reserved characters — verified by 2 tests (individual chars, combined input).

FolderNameSanitizer.sanitize strips leading dots — verified by test with "...Course" input.

FolderNameSanitizer.sanitize collapses whitespace and enforces 100-char max — verified by 2 tests (multi-space, long string).

FolderNameSanitizer.sanitize prevents path traversal vectors — verified by test with "../../etc/passwd" input producing safe output.

All tests pass on WSL2 Linux proving C-01..C-05 decisions are implemented with pure Foundation (no Apple frameworks).
</success_criteria>

<output>
Create .planning/phases/02-pure-pipeline-logic/02-03-SUMMARY.md when done
</output>
