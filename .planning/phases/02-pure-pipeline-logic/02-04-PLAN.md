---
phase: 02-pure-pipeline-logic
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - Sources/UnibrainCore/Pipeline/PipelineState.swift
  - Sources/UnibrainCore/Pipeline/PipelineInputs.swift
  - Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift
  - Sources/UnibrainCore/Pipeline/PipelineError.swift
  - Tests/UnibrainCoreTests/PipelineTests/PipelineOrchestratorTests.swift
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - PipelineOrchestrator actor enforces 8-state lifecycle (idle → transcribing → classifying → normalizing → writing → completed) per O-01
    - PipelineOrchestrator.run(inputs:) throws PipelineError.alreadyRunning when called concurrently per O-02
    - PipelineOrchestrator supports cooperative cancellation via cancel() method transitioning to .cancelled state per O-04
    - PipelineOrchestrator fail-fast model: any error transitions to .failed(error) terminal state per O-03
    - PipelineState enum has 8 cases: idle, transcribing, classifying, normalizing, writing, completed, failed(any Error), cancelled per O-01
    - PipelineInputs value type carries recordingURL, recordingStart, recordingEnd, durationSeconds, source, events per O-05
    - PipelineOrchestrator isolates state via Swift 6 actor (data-race-safe by language guarantee) per O-02
  artifacts:
    - PipelineState.swift enum with 8 cases
    - PipelineInputs.swift Sendable struct with 6 fields
    - PipelineOrchestrator.swift actor with run(inputs:), cancel(), currentState members
    - PipelineError.swift enum for orchestrator-internal errors
    - PipelineOrchestratorTests.swift with 8-state transition tests and concurrent-run rejection tests
  key_links:
    - PipelineOrchestrator depends on NoteNormalizer from Plan 01 for normalize() call
    - PipelineOrchestrator depends on NoteWriter from Plan 02 for write() call
    - PipelineOrchestrator depends on CourseClassifier from Plan 03 for match() call
    - PipelineOrchestrator follows ModelLoadGate actor pattern from Phase 1 (O-02 references D-11..14)
    - PipelineOrchestrator will be injected with AudioTranscriber from Phase 3 for transcribe() call
---

<objective>
Create the orchestrator integration: PipelineState enum, PipelineInputs value type, PipelineError enum, PipelineOrchestrator actor with 8-state machine, cooperative cancellation, and fail-fast error handling — with comprehensive Linux-runnable tests proving O-01..O-05 decisions (8-state lifecycle, concurrent-run rejection, cooperative cancellation, fail-fast model, injected dependencies).

Purpose: Establish the central coordinator that Phase 3's platform code will drive, orchestrating all pure-logic components from Plans 01-03 with mock ASR dependency — all testable on Linux without Apple frameworks.

Output: PipelineState.swift, PipelineInputs.swift, PipelineOrchestrator.swift, PipelineError.swift, PipelineOrchestratorTests.swift — all building green on WSL2 Linux via swift test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md
@.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md
@.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md
@.planning/phases/02-pure-pipeline-logic/02-VALIDATION.md
@.planning/phases/01-foundation/01-CONTEXT.md

@Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift (actor isolation pattern)
@Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift (enum with associated values pattern)
@Sources/UnibrainCore/Protocols/AudioTranscriber.swift (provider protocol pattern)
@Sources/UnibrainCore/Normalization/NormalizedNote.swift (Plan 01 output)
@Sources/UnibrainCore/Normalization/NoteWriter.swift (Plan 02 output)
@Sources/UnibrainCore/Classification/CourseClassifier.swift (Plan 03 output)
@Tests/UnibrainCoreTests/ModelLoadGateTests.swift (actor test pattern)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PipelineState enum</name>
  <files>Sources/UnibrainCore/Pipeline/PipelineState.swift</files>
  <read_first>
    @Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift (enum with associated values pattern: public enum, Sendable, cases with payloads)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 9: PipelineState analog mapping to ModelLoadGateError)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decision O-01: 8-state lifecycle)
  </read_first>
  <behavior>
    Test 1: PipelineState.idle constructs
    Test 2: PipelineState.transcribing constructs
    Test 3: PipelineState.classifying constructs
    Test 4: PipelineState.normalizing constructs
    Test 5: PipelineState.writing constructs
    Test 6: PipelineState.completed constructs
    Test 7: PipelineState.failed(Error) constructs with Error parameter
    Test 8: PipelineState.cancelled constructs
    Test 9: PipelineState is Sendable (including .failed(any Error) case using @unchecked)
  </behavior>
  <action>
    Create Sources/UnibrainCore/Pipeline/PipelineState.swift with:
    - public enum PipelineState: Sendable (NOT : Error — state type, not error type)
    - 8 cases per O-01: idle, transcribing, classifying, normalizing, writing, completed, failed(any Error), cancelled
    - .failed(any Error) case uses @unchecked Sendable per RESEARCH.md (any Error is not Sendable but state enum must be)
    - Doc comments for each state explaining when it occurs and what UI should render
    - Follow ModelLoadGateError.swift enum structure (lines 8-12) but add @unchecked Sendable for .failed case

    Per PATTERNS.md Pattern 9: Copy ModelLoadGateError enum pattern. 8 states per CONTEXT.md O-01. Use @unchecked Sendable for .failed(any Error) per RESEARCH.md.
  </action>
  <verify>
    <automated>swift test --filter PipelineStateTests exits 0</automated>
  </verify>
  <done>
    PipelineState.swift exists with public enum PipelineState: Sendable containing all 8 cases. All 9 tests pass confirming case construction, associated values, and Sendable conformance including @unchecked .failed case.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PipelineInputs value type</name>
  <files>Sources/UnibrainCore/Pipeline/PipelineInputs.swift</files>
  <read_first>
    @Sources/UnibrainCore/Schemas/FrontmatterSchema.swift (Sendable struct pattern: public struct, Sendable, public init)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 10: PipelineInputs analog mapping to FrontmatterSchema)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decision O-05: 6 input fields)
  </read_first>
  <behavior>
    Test 1: PipelineInputs constructs with recordingURL, recordingStart, recordingEnd, durationSeconds, source, events
    Test 2: PipelineInputs is Sendable and can cross concurrency boundaries
    Test 3: PipelineInputs.events field carries [CalendarEvent] from CourseClassifier
  </behavior>
  <action>
    Create Sources/UnibrainCore/Pipeline/PipelineInputs.swift with:
    - public struct PipelineInputs: Sendable (NOT Codable — no serialization needed)
    - 6 stored properties per O-05: recordingURL: URL, recordingStart: Date, recordingEnd: Date, durationSeconds: Int, source: String, events: [CalendarEvent]
    - Public init(recordingURL:recordingStart:recordingEnd:durationSeconds:source:events:) assigning all 6 fields
    - Doc comments explaining PipelineInputs carries all data needed for a single pipeline run
    - Follow FrontmatterSchema.swift struct pattern exactly (lines 53-79): public struct, Sendable, public var fields, public init with all parameters
    - Note: Omit Codable (not needed for in-memory value type)

    Per PATTERNS.md Pattern 10: Copy FrontmatterSchema Sendable struct pattern. 6 fields per CONTEXT.md O-05. Sendable conformance for crossing actor boundaries.
  </action>
  <verify>
    <automated>swift test --filter PipelineInputsTests exits 0</automated>
  </verify>
  <done>
    PipelineInputs.swift exists with public struct PipelineInputs: Sendable containing all 6 required fields. All 3 tests pass confirming construction, Sendable conformance, and CalendarEvent array storage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create PipelineError enum</name>
  <files>Sources/UnibrainCore/Pipeline/PipelineError.swift</files>
  <read_first>
    @Sources/UnibrainCore/ModelLoadGate/ModelLoadGateError.swift (enum with associated values pattern: public enum: Error, Sendable, cases with payloads)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decision O-02: concurrent-run rejection, Claude's Discretion: PipelineError enum)
  </read_first>
  <behavior>
    Test 1: PipelineError.alreadyRunning constructs
    Test 2: PipelineError.invalidInputs constructs
    Test 3: PipelineError.cancelled constructs
    Test 4: PipelineError is Sendable and can cross concurrency boundaries
    Test 5: PipelineError is catchable as Error type
  </behavior>
  <action>
    Create Sources/UnibrainCore/Pipeline/PipelineError.swift with:
    - public enum PipelineError: Error, Sendable
    - Case alreadyRunning — thrown when run() called while state != .idle per O-02
    - Case invalidInputs — thrown when PipelineInputs validation fails
    - Case cancelled — thrown when pipeline is cancelled (can be thrown from run() or surfaced from CancellationError)
    - Doc comments for each case explaining when it occurs
    - Follow ModelLoadGateError.swift enum structure exactly (lines 8-12): public enum: Error, Sendable, case with associated values
    - Keep enum small (3 cases) — per CONTEXT.md Claude's Discretion

    Per ModelLoadGateError.swift pattern: public enum: Error, Sendable. PipelineError distinct from NoteWriterError (orchestrator-internal vs filesystem errors).
  </action>
  <verify>
    <automated>swift test --filter PipelineErrorTests exits 0</automated>
  </verify>
  <done>
    PipelineError.swift exists with public enum PipelineError: Error, Sendable containing 3 cases (alreadyRunning, invalidInputs, cancelled). All 5 tests pass confirming case construction, Sendable conformance, and Error catchability.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create PipelineOrchestrator actor</name>
  <files>Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift</files>
  <read_first>
    @Sources/UnibrainCore/ModelLoadGate/ModelLoadGate.swift (actor isolation pattern: public actor, private var state, acquire/release pattern)
    @.planning/phases/02-pure-pipeline-logic/02-RESEARCH.md (Code Examples: PipelineOrchestrator implementation, lines 418-473)
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 11: PipelineOrchestrator analog mapping to ModelLoadGate)
    @.planning/phases/02-pure-pipeline-logic/02-CONTEXT.md (Decisions O-01..05: 8-state, actor isolation, fail-fast, cooperative cancellation, inputs + dependencies)
  </read_first>
  <behavior>
    Test 1: PipelineOrchestrator.run(inputs:) starts in .idle state and transitions through all stages
    Test 2: PipelineOrchestrator.run(inputs:) throws .alreadyRunning when called concurrently (actor isolation serializes this)
    Test 3: PipelineOrchestrator.cancel() transitions to .cancelled and stops pipeline (cooperative cancellation)
    Test 4: PipelineOrchestrator fail-fast: any error in a stage transitions to .failed(error) terminal state
    Test 5: PipelineOrchestrator currentState is readable as computed property returning private state
    Test 6: PipelineOrchestrator calls CourseClassifier.match during classifying stage
    Test 7: PipelineOrchestrator calls NoteNormalizer.normalize during normalizing stage
    Test 8: PipelineOrchestrator calls NoteWriter.write during writing stage
  </behavior>
  <action>
    Create Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift with:
    - public actor PipelineOrchestrator per O-02
    - Private stored properties: private var state: PipelineState = .idle, private var activeTask: Task<Void, Error>?
    - Constructor injected dependencies per O-05: private let transcriber: any AudioTranscriber, private let writer: any NoteWriter
    - public init(transcriber:writer:) stores injected dependencies
    - public func run(inputs: PipelineInputs) async throws:
      - Synchronous guard: guard case .idle = state else { throw PipelineError.alreadyRunning }
      - Set state = .transcribing, create Task with all stages
      - Inside Task: try Task.checkCancellation() before each stage (cooperative per O-04)
      - Stage 1 (transcribing): let transcript = try await transcriber.transcribe(inputs.recordingURL)
      - Stage 2 (classifying): state = .classifying, let match = CourseClassifier.match(events: inputs.events, against: inputs.recordingStart, window: 1800)
      - Stage 3 (normalizing): state = .normalizing, let note = NoteNormalizer.normalize(transcript: segments, course: matched event, audioFile: inputs.recordingURL.lastPathComponent, ...)
      - Stage 4 (writing): state = .writing, try await writer.write(note, to: destinationURL)
      - Final: state = .completed, try await activeTask?.value
      - Catch block: state = .failed(error), throw error (fail-fast per O-03)
    - public func cancel() async: activeTask?.cancel(), state = .cancelled per O-04
    - public var currentState: PipelineState { return state } per O-02
    - Doc comments referencing O-01..05 decisions and RESEARCH.md patterns
    - Follow ModelLoadGate.swift actor pattern (lines 17-51) AND RESEARCH.md lines 418-473 implementation

    Per PATTERNS.md Pattern 11: Copy ModelLoadGate actor isolation pattern exactly. Store Task in actor state so cancel() can access it. Synchronous guard check for concurrent-run rejection. Call Task.checkCancellation() before each async stage.
  </action>
  <verify>
    <automated>swift test --filter PipelineOrchestratorTests exits 0</automated>
  </verify>
  <done>
    PipelineOrchestrator.swift exists with public actor implementing 8-state machine. All 8 tests pass confirming state transitions, concurrent-run rejection, cooperative cancellation, fail-fast error handling, currentState readability, and calls to CourseClassifier/NoteNormalizer/NoteWriter.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create comprehensive PipelineOrchestratorTests</name>
  <files>Tests/UnibrainCoreTests/PipelineTests/PipelineOrchestratorTests.swift</files>
  <read_first>
    @Tests/UnibrainCoreTests/ModelLoadGateTests.swift (actor test pattern: @Suite, @Test, async/await, #expect(throws:))
    @.planning/phases/02-pure-pipeline-logic/02-PATTERNS.md (Pattern 17: PipelineOrchestratorTests analog mapping to ModelLoadGateTests)
    @Tests/UnibrainCoreTests/ProviderProtocolTests.swift (mock protocol conformance pattern for MockAudioTranscriber)
  </read_first>
  <action>
    Create Tests/UnibrainCoreTests/PipelineTests/PipelineOrchestratorTests.swift with:
    - @Suite("PipelineOrchestrator") struct PipelineOrchestratorTests
    - Mock dependencies: private struct MockAudioTranscriber: AudioTranscriber, private struct MockNoteWriter: NoteWriter (use TestNoteWriter from Plan 02)
    - All tests from Task 4 behavior block (8 tests covering state transitions, concurrent-run rejection, cancellation, fail-fast, currentState, integration with CourseClassifier/NoteNormalizer/NoteWriter)
    - State transition test: run(inputs:) transitions through all 8 states (idle -> transcribing -> classifying -> normalizing -> writing -> completed)
    - Concurrent-run test: call run() twice without awaiting first -> second throws .alreadyRunning
    - Cancellation test: start run(), call cancel() immediately, await throws CancellationError or completes with .cancelled state
    - Fail-fast test: mock transcriber throws error, verify state = .failed(error)
    - Integration tests: mock CourseClassifier returns .single, NoteNormalizer returns valid note, NoteWriter succeeds
    - Use @Test macro with async functions for async tests
    - Use #expect(throws: PipelineError.self) for structured error assertions
    - Use #expect(orchestrator.currentState == .expectedState) for state assertions
    - Doc comments per Swift Testing pattern

    Per PATTERNS.md Pattern 17: Copy ModelLoadGateTests.swift @Suite/@Test/#expect pattern. Mock dependencies follow ProviderProtocolTests.swift pattern. Test 8-state transitions per CONTEXT.md O-01.
  </action>
  <verify>
    <automated>swift test --filter PipelineOrchestratorTests exits 0 with all tests passing</automated>
  </verify>
  <done>
    PipelineOrchestratorTests.swift created with comprehensive tests covering 8-state transitions, concurrent-run rejection, cooperative cancellation, fail-fast error handling, and integration with CourseClassifier/NoteNormalizer/NoteWriter. All tests pass on Linux with mock dependencies.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PipelineInputs | Recording metadata and calendar events from untrusted sources (capture session, EventKit adapter) |
| Orchestrator dependencies | Mock AudioTranscriber in tests, real conformances in Phase 3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-10 | Tampering | PipelineOrchestrator actor isolation | medium | mitigate | Swift 6 actor isolation serializes all state access by language guarantee per O-02. Concurrent-run rejection enforced by synchronous guard at run() entry. No data races possible. |
| T-2-11 | Denial of Service | PipelineOrchestrator fail-fast | low | accept | Fail-fast model (O-03) means errors terminate pipeline immediately — no retry loops that could consume resources. Caller decides retry policy. |
| T-2-12 | Information Disclosure | PipelineState.error payload | low | accept | .failed(any Error) carries error for debugging. Errors are thrown from dependencies (NoteWriter, CourseClassifier) which have structured error types. No sensitive data in error payloads. |
</threat_model>

<verification>
- swift test --filter PipelineOrchestratorTests exits 0 with all tests passing
- swift test --filter PipelineStateTests exits 0
- swift test --filter PipelineInputsTests exits 0
- swift test --filter PipelineErrorTests exits 0
- All tests pass on WSL2 Linux (no Apple frameworks required)
- PipelineState.swift, PipelineInputs.swift, PipelineOrchestrator.swift, PipelineError.swift exist in Sources/UnibrainCore/Pipeline/
- PipelineOrchestratorTests.swift exists in Tests/UnibrainCoreTests/PipelineTests/
- Actor isolation verified in PipelineOrchestrator implementation
- Cooperative cancellation verified via Task.checkCancellation() calls
- Fail-fast error handling verified via .failed state transition tests
- Concurrent-run rejection verified via .alreadyRunning throw tests
</verification>

<success_criteria>
PipelineOrchestrator actor enforces 8-state lifecycle (idle → transcribing → classifying → normalizing → writing → completed) — verified by state transition test showing all 8 states in order.

PipelineOrchestrator.run(inputs:) throws PipelineError.alreadyRunning when called concurrently — verified by test calling run() twice without awaiting first, second call throws .alreadyRunning.

PipelineOrchestrator supports cooperative cancellation via cancel() method — verified by test calling cancel() during run and confirming .cancelled state (or CancellationError thrown).

PipelineOrchestrator fail-fast model: any error transitions to .failed(error) terminal state — verified by test with mock transcriber throwing error, state transitions to .failed(error) and error re-throws.

PipelineState enum has 8 cases — verified by construction tests for all cases including .failed(any Error).

PipelineInputs value type carries all 6 required fields — verified by construction and Sendable tests.

All tests pass on WSL2 Linux proving O-01..O-05 decisions are implemented with Swift 6 actor isolation and cooperative cancellation patterns.
</success_criteria>

<output>
Create .planning/phases/02-pure-pipeline-logic/02-04-SUMMARY.md when done
</output>
