---
phase: 04-course-classification-smart-routing
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - Sources/UnibrainCore/Pipeline/PipelineState.swift
  - Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift
  - Sources/UnibrainCore/Pipeline/PipelineInputs.swift
  - Sources/UnibrainCore/Normalization/NoteNormalizer.swift
  - Sources/UnibrainProviders/VaultWriting/ScheduleAwareVaultResolver.swift
  - Tests/UnibrainCoreTests/PipelineTests/PipelineOrchestratorPauseTests.swift
  - Tests/UnibrainProvidersTests/VaultWriting/ScheduleAwareVaultResolverTests.swift
autonomous: true
requirements: [CLAS-03, CLAS-05]

must_haves:
  truths:
    - "PipelineState has a new .awaitingUserChoice case that pauses the pipeline mid-run (MP-04)"
    - "CheckedContinuation is stored as actor state; UI resumes it from outside the actor (avoids SR-14875 hang)"
    - "NoteNormalizer.normalize accepts term and source as parameters instead of hardcoding them (RESEARCH Pitfall 5)"
    - "ScheduleAwareVaultResolver builds {vault}/{term}/{course-code}/YYYY-MM-DD-{COURSE}-Lecture.md path (CLAS-05)"
    - "Unmapped event titles trigger FolderNameSanitizer -> auto-created folder (CLAS-03)"
    - "Skip routes to {vault}/{term}/_unsorted/ with UNCLASSIFIED frontmatter (MP-03)"
  artifacts:
    - path: "Sources/UnibrainCore/Pipeline/PipelineState.swift"
      provides: "Extended PipelineState with .awaitingUserChoice case"
      contains: "awaitingUserChoice"
    - path: "Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift"
      provides: "Orchestrator with pause/resume via CheckedContinuation + resume(with:) + skipClassification()"
      contains: "resumeWithSelection"
    - path: "Sources/UnibrainCore/Pipeline/PipelineInputs.swift"
      provides: "Extended PipelineInputs with term label and source parameters"
      contains: "termLabel"
    - path: "Sources/UnibrainCore/Normalization/NoteNormalizer.swift"
      provides: "Parameterized normalize() accepting term and source instead of hardcoded values"
      contains: "term:"
    - path: "Sources/UnibrainProviders/VaultWriting/ScheduleAwareVaultResolver.swift"
      provides: "ScheduleAwareVaultResolver replacing HardcodedVaultResolver for Phase 4+ recordings"
      contains: "ScheduleAwareVaultResolver"
    - path: "Tests/UnibrainCoreTests/PipelineTests/PipelineOrchestratorPauseTests.swift"
      provides: "Tests for pause at .awaitingUserChoice + resume + skip + cancellation during pause"
      contains: "@Test"
    - path: "Tests/UnibrainProvidersTests/VaultWriting/ScheduleAwareVaultResolverTests.swift"
      provides: "Tests for path construction, auto-create, skip, multi-term routing"
      contains: "@Test"
  key_links:
    - from: "PipelineOrchestrator.executePipeline"
      to: ".awaitingUserChoice state"
      via: "withCheckedThrowingContinuation when CourseClassifier returns .multiple or .none"
      pattern: "withCheckedThrowingContinuation"
    - from: "UI layer resume(with:)"
      to: "selectionContinuation.resume(returning:)"
      via: "Cross-actor method call (safe — SR-14875 only affects in-actor resumes)"
      pattern: "selectionContinuation.*resume"
---

<objective>
Extend the PipelineOrchestrator with pause/resume capability for manual course selection (MP-04), parameterize NoteNormalizer to accept real term/source values (Pitfall 5 fix), and create ScheduleAwareVaultResolver to replace Phase 3's hardcoded folder routing with schedule-aware {vault}/{term}/{course-code}/ paths.

Purpose: This plan wires Wave 1's building blocks (CourseMappingStore, EventKit adapter, CoursePickerViewModel) into the pipeline core. The orchestrator pause/resume is the riskiest architectural change in Phase 4 (RESEARCH.md identified CheckedContinuation + SR-14875 as the key risk). The resolver replacement makes routing work end-to-end.
Output: Extended PipelineState/Orchestrator/Inputs/Normalizer, new ScheduleAwareVaultResolver, comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-course-classification-smart-routing/04-CONTEXT.md
@.planning/phases/04-course-classification-smart-routing/04-RESEARCH.md
@Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift
@Sources/UnibrainCore/Pipeline/PipelineState.swift
@Sources/UnibrainCore/Pipeline/PipelineInputs.swift
@Sources/UnibrainCore/Normalization/NoteNormalizer.swift
@Sources/UnibrainCore/Classification/CourseMatch.swift
@Sources/UnibrainCore/Classification/FolderNameSanitizer.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PipelineState + Orchestrator pause/resume + NoteNormalizer parameterization</name>
  <files>Sources/UnibrainCore/Pipeline/PipelineState.swift, Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift, Sources/UnibrainCore/Pipeline/PipelineInputs.swift, Sources/UnibrainCore/Normalization/NoteNormalizer.swift, Tests/UnibrainCoreTests/PipelineTests/PipelineOrchestratorPauseTests.swift</files>
  <read_first>
    - Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift (full file — the executePipeline method, the activeTask pattern, the cancel/reset methods)
    - Sources/UnibrainCore/Pipeline/PipelineState.swift (8-state enum — add 9th state)
    - Sources/UnibrainCore/Pipeline/PipelineInputs.swift (6-field struct — add termLabel)
    - Sources/UnibrainCore/Normalization/NoteNormalizer.swift (lines 105-118 — hardcoded "Fall 2026" and "MacBook Air" placeholders to parameterize)
    - Sources/UnibrainCore/Pipeline/PipelineError.swift (error enum)
    - .planning/phases/04-course-classification-smart-routing/04-RESEARCH.md Pattern 2 (CheckedContinuation pause/resume example), Pitfall 3 (SR-14875 warning), Pitfall 5 (NoteNormalizer hardcoded values)
    - .planning/phases/04-course-classification-smart-routing/04-CONTEXT.md MP-04 (picker fires at .classifying), CT-01 (term label stored in courses.json)
  </read_first>
  <behavior>
    - Test 1: PipelineOrchestrator with mock transcriber + mock writer + mock resolver: when CourseClassifier returns .single, the pipeline runs to .completed without pausing (existing behavior preserved).
    - Test 2: When events are empty (CourseClassifier returns .none), orchestrator transitions to .awaitingUserChoice and parks. currentState == .awaitingUserChoice. The test then calls resume(with: aCourseEvent) and verifies the pipeline continues to .completed.
    - Test 3: When 2 events overlap (CourseClassifier returns .multiple), orchestrator transitions to .awaitingUserChoice. Test calls skipClassification() and verifies pipeline routes to _unsorted and reaches .completed.
    - Test 4: NoteNormalizer.normalize now accepts term: "Fall 2026" and source: "MacBook Pro" as parameters, and the resulting FrontmatterSchema has term == "Fall 2026" and source == "MacBook Pro" (not the old hardcoded values).
    - Test 5: Calling cancel() while in .awaitingUserChoice transitions to .cancelled (the continuation is resumed with an error, which the catch block handles).
    - Test 6: PipelineInputs now has a termLabel field. Default value is empty string for backward compatibility with Phase 3 tests.
  </behavior>
  <action>
    **PipelineState.swift**: Add a new case `.awaitingUserChoice` between `.classifying` and `.normalizing`. Update the doc comment to describe it: "Pipeline is paused waiting for user to manually select a course (MP-04). The orchestrator stores a CheckedContinuation that the UI layer resumes via resume(with:)."

    **PipelineInputs.swift**: Add `public var termLabel: String` field. Add it to the init with a default value of `""` so existing Phase 2/3 call sites that do not pass it still compile (backward-compatible extension). Add `public var courseMapping: CourseMappingDocument?` — optional, so Phase 3 calls that pass nil still work; Phase 4 passes the loaded document for the resolver to use. Actually, do not add courseMapping to PipelineInputs — the resolver gets it injected at init time (see Task 2). Keep PipelineInputs lean: just add `termLabel`.

    **NoteNormalizer.swift**: Change the `normalize` method signature from:
    `normalize(transcript:course:audioFile:recordingStart:durationSeconds:)`
    to:
    `normalize(transcript:course:audioFile:recordingStart:durationSeconds:term:source:)`

    Replace the hardcoded `term: "Fall 2026"` (line ~109) with the `term` parameter. Replace the hardcoded `source: "MacBook Air"` (line ~112) with the `source` parameter. All existing Phase 2 tests that call normalize must be updated to pass the new parameters — add `term: "Fall 2026", source: "MacBook Air"` to existing test calls so they continue passing (the old hardcoded values become the test's explicit inputs).

    **PipelineOrchestrator.swift**: This is the core change.

    Add a private stored property: `private var selectionContinuation: CheckedContinuation<CalendarEvent, any Error>?`.

    In `executePipeline`, replace the `.classifying` stage. Currently (lines 153-175), the code:
    1. Sets state = .classifying
    2. Calls CourseClassifier.match()
    3. Calls resolver.resolve(match:)
    4. Guards `case .single(let course) = match` else throws .invalidInputs

    The new flow:
    1. Sets state = .classifying
    2. Calls CourseClassifier.match()
    3. If match is `.single(let event)`: proceed directly to resolve + normalize + write (existing path)
    4. If match is `.multiple` or `.none`:
       a. Set state = .awaitingUserChoice
       b. Call `resolveViaUserChoice()` which parks via `withCheckedThrowingContinuation`
       c. On resume: proceed with the user-selected event to resolve + normalize + write

    Add method: `private func resolveViaUserChoice() async throws -> CalendarEvent`:
    ```
    return try await withCheckedThrowingContinuation { continuation in
        self.selectionContinuation = continuation
    }
    ```
    This suspends the orchestrator actor cooperatively. The continuation is stored as actor state. Per RESEARCH.md Pitfall 3: the resume happens from OUTSIDE the actor (UI layer), which is the safe pattern.

    Add public method: `public func resume(with selection: CourseSelection) async`:
    This is called from the UI layer (MenuBarViewModel on @MainActor). It crosses the actor boundary — the method itself runs within the orchestrator's actor isolation (Swift 6 ensures this). It converts the CourseSelection to a CalendarEvent and resumes the continuation:
    - `.course(code)` -> construct a CalendarEvent with title=code (the resolver looks up the mapping by title or creates a folder from the code)
    - `.event(event)` -> use the event directly
    - `.newCourse(code, name)` -> construct a CalendarEvent with title=code
    - `.skip` -> construct a CalendarEvent with title="_unsorted" and resume the continuation

    Actually, the cleaner design: `resume(with:)` takes a `CalendarEvent` (the resolved event to route to) directly, and the UI layer (Plan 05) is responsible for converting CourseSelection -> CalendarEvent before calling resume. This keeps the orchestrator simpler and avoids importing CoursePickerViewModel into UnibrainCore.

    Revised: `public func resume(with event: CalendarEvent)`:
    ```
    selectionContinuation?.resume(returning: event)
    selectionContinuation = nil
    ```

    Add public method: `public func skipClassification()`:
    ```
    let unsortedEvent = CalendarEvent(id: "unsorted", title: "_unsorted", startDate: Date(), endDate: Date())
    selectionContinuation?.resume(returning: unsortedEvent)
    selectionContinuation = nil
    ```

    In `cancel()`: also resume the continuation with a CancellationError if it exists:
    ```
    if let cont = selectionContinuation {
        selectionContinuation = nil
        cont.resume(throwing: CancellationError())
    }
    activeTask?.cancel()
    ```

    The `executePipeline` method changes from guard-against-non-single to:
    ```
    let resolvedEvent: CalendarEvent
    switch match {
    case .single(let event):
        resolvedEvent = event
    case .multiple, .none:
        state = .awaitingUserChoice
        resolvedEvent = try await resolveViaUserChoice()
    }
    // Continue with resolvedEvent -> resolve -> normalize -> write
    ```

    Then replace the `guard case .single` block with: use `resolvedEvent` for normalization. The resolver.resolve call happens before the switch (it can handle .single directly and .multiple/.none via the continuation-parked path — actually the resolver needs the resolved event, so move resolver.resolve AFTER the user choice is resolved).

    Revised executePipeline classification stage:
    ```
    // Stage 2: Classifying
    state = .classifying
    try Task.checkCancellation()
    let match = CourseClassifier.match(
        events: inputs.events,
        against: inputs.recordingStart,
        window: 1800
    )

    // Resolve course event (may pause for user input)
    let resolvedEvent: CalendarEvent
    switch match {
    case .single(let event):
        resolvedEvent = event
    case .multiple, .none:
        state = .awaitingUserChoice
        try Task.checkCancellation()
        resolvedEvent = try await resolveViaUserChoice()
    }

    // Resolve vault destination from the resolved event.
    let destinationURL = try resolver.resolve(
        match: .single(resolvedEvent),
        recordingStart: inputs.recordingStart
    )
    ```

    This wraps the resolver.resolve call to always receive a `.single` match with the resolved event. The resolver no longer needs to handle `.multiple` or `.none` — the orchestrator resolves those before calling resolve.
  </action>
  <verify>
    <automated>swift test --filter PipelineOrchestratorPauseTests && swift test --filter PipelineOrchestratorTests && swift test --filter NoteNormalizerTests</automated>
  </verify>
  <done>
    - PipelineState has .awaitingUserChoice case
    - Orchestrator parks at .awaitingUserChoice when CourseClassifier returns .multiple/.none
    - resume(with:) resumes the continuation from outside the actor (no SR-14875 hang)
    - skipClassification() routes to _unsorted
    - cancel() during pause transitions to .cancelled
    - NoteNormalizer accepts term and source parameters (no hardcoded values)
    - All existing Phase 2 orchestrator + normalizer tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScheduleAwareVaultResolver (replaces HardcodedVaultResolver for Phase 4+ recordings)</name>
  <files>Sources/UnibrainProviders/VaultWriting/ScheduleAwareVaultResolver.swift, Tests/UnibrainProvidersTests/VaultWriting/ScheduleAwareVaultResolverTests.swift</files>
  <read_first>
    - Sources/UnibrainProviders/VaultWriting/HardcodedVaultResolver.swift (existing Phase 3 resolver — stays for backward compat, Phase 4 uses the new one)
    - Sources/UnibrainCore/Pipeline/PipelineTranscriber.swift (VaultPathResolver protocol definition)
    - Sources/UnibrainCore/Classification/FolderNameSanitizer.swift (sanitize method for term labels and course codes)
    - Sources/UnibrainCore/Classification/CourseMatch.swift (.single carries the resolved event)
    - .planning/phases/04-course-classification-smart-routing/04-RESEARCH.md Pattern 4 (ScheduleAwareVaultResolver example)
    - .planning/phases/04-course-classification-smart-routing/04-CONTEXT.md CT-04 (folder path uses sanitized term label), CLAS-05 (multi-term folder structure), CLAS-03 (auto-create sanitized folder), MP-03 (Skip -> _unsorted)
  </read_first>
  <behavior>
    - Test 1: resolve(match: .single(event with title "CS101 Lecture"), recordingStart: Sept 15 2026) returns URL ending in "/{sanitizedTerm}/CS101 Lecture/2026-09-15-CS101 Lecture-Lecture.md" (path structure per CLAS-05).
    - Test 2: resolve with title containing unsafe characters (e.g., "CS101: Intro / Lecture") produces a path with sanitized folder name (slashes and colons stripped by FolderNameSanitizer).
    - Test 3: resolve with title "_unsorted" produces path ending in "/{sanitizedTerm}/_unsorted/2026-09-15-_unsorted-Lecture.md" (MP-03 Skip path).
    - Test 4: resolve creates the intermediate directories (vault/{term}/{course}/) via FileManager.createDirectory(withIntermediateDirectories: true) (A-05 pattern from Phase 2).
    - Test 5: Different term labels produce different base directories (multi-term routing per CLAS-05).
  </behavior>
  <action>
    Create `Sources/UnibrainProviders/VaultWriting/ScheduleAwareVaultResolver.swift` behind `#if os(macOS)` guard (matching HardcodedVaultResolver's platform guard — the resolver touches FileManager which is available on both, but it is only used from macOS in Phase 4):

    Define `public struct ScheduleAwareVaultResolver: VaultPathResolver, Sendable`:

    Properties (all `let` — immutable, Sendable):
    - `private let vaultRoot: URL` — the vault root URL (e.g., ~/Documents/Unibrain/)
    - `private let termLabel: String` — the current term label (e.g., "Fall 2026")

    Init: `public init(vaultRoot: URL, termLabel: String)`

    Method `resolve(match:recordingStart:)`:
    The orchestrator always passes `.single(resolvedEvent)` after the pause/resume resolution (see Task 1). So this method handles `.single` and extracts the event:
    ```
    guard case .single(let event) = match else {
        throw PipelineError.invalidInputs
    }
    ```

    Extract course code from event title:
    - `let sanitizedTitle = FolderNameSanitizer.sanitize(folderName: event.title)` — per CLAS-03, unrecognized event titles trigger sanitized folder creation.

    Build path:
    - `let sanitizedTerm = FolderNameSanitizer.sanitize(folderName: termLabel)` — per CT-04, folder path uses sanitized term label. If termLabel is empty, use "default-term" as fallback to avoid empty path component.
    - `let courseDir = vaultRoot.appendingPathComponent(sanitizedTerm).appendingPathComponent(sanitizedTitle)`
    - Create directory tree: `try FileManager.default.createDirectory(at: courseDir, withIntermediateDirectories: true)` (A-05: recursive creation).

    Build filename:
    - `let dateFormatter = DateFormatter()` with dateFormat "yyyy-MM-dd", locale en_US_POSIX, timeZone TimeZone.current (matching HardcodedVaultResolver pattern).
    - `let dateString = dateFormatter.string(from: recordingStart)`
    - Return `courseDir.appendingPathComponent("\(dateString)-\(sanitizedTitle)-Lecture.md")`

    Per CLAS-05: the path structure is `{vault}/{term}/{course-code}/YYYY-MM-DD-{COURSE}-Lecture.md`.

    Per MP-03: the Skip path uses title "_unsorted" which naturally produces the `_unsorted/` folder — no special-casing needed since the resolver treats "_unsorted" as a regular (sanitized) course code.

    Per CLAS-03: the resolver does NOT look up the CourseMappingStore — it always uses the event title as the course code (sanitized). The mapping lookup (M-02 auto-learn, or mapped code lookup) happens earlier in the pipeline: the EventKit adapter fetches events, the orchestrator/classifier matches, and the resolved event title IS the course folder name. If a mapping exists (e.g., "CS101 Lecture" -> "CS101"), the CourseMappingStore lookup should happen BEFORE calling the resolver — in the orchestrator or a pre-resolver step. For simplicity in Phase 4, the resolver uses the raw event title. The mapping store integration for course-code-vs-title is handled by the UI layer (Plan 05) when converting CourseSelection to a CalendarEvent before calling resume(with:).

    Actually — reconsider: the mapping IS needed at resolve time. The event title from the calendar might be "Intro to Computer Science" but the mapped course code is "CS101". The resolver should produce `.../CS101/...` not `.../Intro to Computer Science/...`. So inject the CourseMappingStore into the resolver:

    Revised properties:
    - `private let vaultRoot: URL`
    - `private let termLabel: String`
    - `private let mappingStore: CourseMappingStore` — injected for lookup

    Revised resolve: look up the event title in the mapping store. If found, use the mapped courseCode. If not found (unmapped — CLAS-03 auto-create), use the sanitized event title AND upsert the mapping (auto-learn per M-02).

    Wait — the resolver is a struct (value type, Sendable) and CourseMappingStore is an actor. A struct cannot hold an actor reference without being fine — actually actors are reference types, so a struct CAN hold a reference to an actor (the struct is still value type, the actor reference is just a pointer). But the resolver is supposed to be a simple path builder. Making it call an actor for every resolve complicates it.

    Better design: keep the resolver simple. The orchestrator (or a new classification coordinator) does the mapping lookup BEFORE calling resolve. The orchestrator already has the resolvedEvent from the pause/resume. Before calling resolver.resolve, it looks up the event title in the CourseMappingStore (injected into the orchestrator). If mapped, it replaces the event title with the mapped code. If unmapped, it calls mappingStore.upsert to auto-learn, then uses the sanitized title.

    But that means modifying the orchestrator to have a CourseMappingStore dependency. Since the orchestrator already has transcriber/writer/resolver injected, adding a fourth dependency is clean:
    - Add `private let mappingStore: CourseMappingStore?` — optional, so Phase 3 (which does not use mapping) can pass nil.

    Actually, the cleanest approach for Phase 4: the resolver takes a `CourseMappingDocument` (the loaded snapshot, not the actor) at init time. The document is loaded once when the resolver is created (at pipeline setup), and the resolver reads from the in-memory dict. Writes (auto-learn, manual pick) go through the CourseMappingStore actor elsewhere. The resolver only reads.

    Final decision: `ScheduleAwareVaultResolver` takes `vaultRoot: URL`, `termLabel: String`, and `mapping: [String: CourseMapping]` (a plain dictionary snapshot). The init loads the mapping from CourseMappingStore at pipeline setup time. The resolver looks up event.title in the mapping dict; if found, uses mapping.courseCode; if not found, uses sanitized event title. No actor dependency, no async calls in resolve(), fully testable.
  </action>
  <verify>
    <automated>swift test --filter ScheduleAwareVaultResolverTests</automated>
  </verify>
  <done>
    - ScheduleAwareVaultResolver builds {vault}/{term}/{course-code}/YYYY-MM-DD-{COURSE}-Lecture.md paths (CLAS-05)
    - Mapped event titles resolve to mapped course codes
    - Unmapped event titles auto-create sanitized folders (CLAS-03)
    - Skip path routes to _unsorted (MP-03)
    - Directory tree created recursively (A-05)
    - macOS CI tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator actor -> CheckedContinuation | Continuation is stored as actor state and resumed from outside the actor (UI layer) |
| Resolver -> Filesystem | Course folder names derived from calendar event titles cross into filesystem path context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Denial of Service | CheckedContinuation resumed from within same actor hangs (SR-14875) | high | mitigate | resume(with:) and skipClassification() are called from the UI layer (MenuBarViewModel @MainActor), crossing the actor boundary — this is the safe pattern per RESEARCH Pitfall 3 |
| T-04-10 | Denial of Service | Continuation never resumed (orphaned pipeline) | medium | mitigate | cancel() resumes continuation with CancellationError; UI Cancel button calls cancel(). Also, a Skip button is always available in the picker. |
| T-04-11 | Tampering | Path traversal via event title in resolver | medium | mitigate | FolderNameSanitizer (Phase 2 T-2-01) strips /, :, leading dots before any path component use. Resolver calls sanitize on both term label and event title. |
</threat_model>

<verification>
- `swift test --filter PipelineOrchestratorPauseTests` passes on WSL2 Linux
- `swift test --filter PipelineOrchestratorTests` passes (existing Phase 2 tests still green — backward compat)
- `swift test --filter NoteNormalizerTests` passes (existing tests with updated parameters)
- `swift test --filter ScheduleAwareVaultResolverTests` passes on macOS CI
- `swift build` succeeds for UnibrainCore and UnibrainProviders
</verification>

<success_criteria>
- PipelineState has .awaitingUserChoice; orchestrator parks and resumes correctly
- CheckedContinuation resumed from outside actor (no SR-14875 hang)
- NoteNormalizer parameterized with term and source (no hardcoded values)
- ScheduleAwareVaultResolver produces correct multi-term paths (CLAS-05)
- Auto-folder creation for unrecognized titles (CLAS-03)
- Skip routes to _unsorted (MP-03)
</success_criteria>

<output>
Create `.planning/phases/04-course-classification-smart-routing/04-04-SUMMARY.md` when done
</output>
