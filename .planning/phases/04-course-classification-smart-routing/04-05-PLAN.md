---
phase: 04-course-classification-smart-routing
plan: 05
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified:
  - UnibrainApp/ViewModels/MenuBarViewModel.swift
  - UnibrainApp/Views/CoursePickerView.swift
  - UnibrainApp/Views/CoursePickerRow.swift
  - UnibrainApp/Views/CreateCourseForm.swift
  - UnibrainApp/Views/ManageCoursesView.swift
  - UnibrainApp/Views/PermissionDeniedSheet.swift
  - UnibrainApp/Views/PermissionBanner.swift
  - UnibrainApp/Views/TermExpiredBanner.swift
  - UnibrainApp/Views/TermEditorForm.swift
  - UnibrainApp/Views/ClassificationPausedView.swift
  - UnibrainApp/MenuBarPopover.swift
  - UnibrainApp/UnibrainApp.swift
  - UnibrainApp/Info.plist
  - Tests/UnibrainAppTests/MenuBarViewModelOverlayTests.swift
autonomous: false
requirements: [CLAS-04, CLAS-07, ONBD-02, ONBD-03]

must_haves:
  truths:
    - "When CourseClassifier returns .multiple or .none, the course picker appears inline in the popover (not as .sheet) and Angelica can pick a course without leaving her current app (MP-01)"
    - "Manual pick updates the mapping AND recent list in courses.json (M-03, CLAS-07)"
    - "When calendar Full Access is denied, a first-time explanation overlay appears with a Settings deep-link; subsequent recordings show a compact tappable banner (P-01)"
    - "Angelica can manage course mappings via a Manage Courses button in the idle popover (M-04)"
    - "When currentTerm.endDate has passed, a non-blocking banner prompts to set a new term (CT-03)"
    - "Term editor allows setting label, start date, and end date (CT-01)"
    - "Info.plist contains NSCalendarsUsageDescription so requestFullAccessToEvents can show the system permission dialog (per RESEARCH Open Question 3 RESOLVED)"
    - "PipelineInputs.termLabel is set from currentTermLabel on every recording (W4 fix)"
  artifacts:
    - path: "UnibrainApp/Views/CoursePickerView.swift"
      provides: "Inline course picker with search, recent, all courses, multi-match events, create new, skip"
      contains: "struct CoursePickerView"
    - path: "UnibrainApp/Views/CoursePickerRow.swift"
      provides: "Individual course row with accessibility labels"
      contains: "struct CoursePickerRow"
    - path: "UnibrainApp/Views/CreateCourseForm.swift"
      provides: "Inline form for creating a new course (code + name fields)"
      contains: "struct CreateCourseForm"
    - path: "UnibrainApp/Views/ManageCoursesView.swift"
      provides: "Editable mapping table with add/delete/edit"
      contains: "struct ManageCoursesView"
    - path: "UnibrainApp/Views/PermissionDeniedSheet.swift"
      provides: "First-time calendar permission explanation overlay"
      contains: "struct PermissionDeniedSheet"
    - path: "UnibrainApp/Views/PermissionBanner.swift"
      provides: "Ongoing compact banner for denied calendar permission"
      contains: "struct PermissionBanner"
    - path: "UnibrainApp/Views/TermExpiredBanner.swift"
      provides: "Non-blocking banner when term has ended"
      contains: "struct TermExpiredBanner"
    - path: "UnibrainApp/Views/TermEditorForm.swift"
      provides: "Term label + start/end date editor"
      contains: "struct TermEditorForm"
    - path: "UnibrainApp/Views/ClassificationPausedView.swift"
      provides: "Brief loading state between transcription done and picker/classification result"
      contains: "struct ClassificationPausedView"
    - path: "UnibrainApp/ViewModels/MenuBarViewModel.swift"
      provides: "Extended with calendar permission, overlay state, resume/skip methods, CourseMappingStore injection"
      contains: "overlayState"
    - path: "UnibrainApp/MenuBarPopover.swift"
      provides: "Extended with overlay view-state switching (replaces .sheet per Pitfall 2)"
      contains: "switch viewModel.overlayState"
    - path: "UnibrainApp/UnibrainApp.swift"
      provides: "App entry with CourseMappingStore and EventKitCalendarAdapter injection"
      contains: "CourseMappingStore"
    - path: "UnibrainApp/Info.plist"
      provides: "NSCalendarsUsageDescription key for EventKit permission dialog"
      contains: "NSCalendarsUsageDescription"
    - path: "Tests/UnibrainAppTests/MenuBarViewModelOverlayTests.swift"
      provides: "Unit tests for PopoverOverlay state transitions and selectCourse/skipClassification logic"
      contains: "@Test"
  key_links:
    - from: "MenuBarPopover body"
      to: "overlay views"
      via: "switch viewModel.overlayState (inline view-state switching, NOT .sheet)"
      pattern: "overlayState"
    - from: "CoursePickerView tap"
      to: "orchestrator.resume(with:)"
      via: "viewModel handles CourseSelection -> CalendarEvent -> resume"
      pattern: "resume"
    - from: "PermissionDeniedSheet button"
      to: "NSWorkspace.open(URL)"
      via: "x-apple.systempreferences deep-link to Privacy_Calendars"
      pattern: "NSWorkspace"
---

<objective>
Build all Phase 4 SwiftUI views and wire them into the menu-bar popover via inline view-state switching. Extend MenuBarViewModel with calendar permission handling, course picker coordination, and orchestrator resume/skip methods. Create Info.plist with NSCalendarsUsageDescription. Add unit tests for PopoverOverlay state transitions.

Purpose: This plan delivers the user-facing surfaces for schedule-aware routing — the manual course picker, permission degradation UX, manage courses view, term editor, and classification-paused state. Per RESEARCH.md Pitfall 2, `.sheet` on `MenuBarExtra` is unreliable (FB11984872), so all Phase 4 surfaces use inline view-state switching via a `PopoverOverlay` enum.
Output: 9 new SwiftUI view files + Info.plist + unit tests, extended MenuBarViewModel + MenuBarPopover + UnibrainApp, macOS device verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-course-classification-smart-routing/04-CONTEXT.md
@.planning/phases/04-course-classification-smart-routing/04-RESEARCH.md
@.planning/phases/04-course-classification-smart-routing/04-UI-SPEC.md
@UnibrainApp/ViewModels/MenuBarViewModel.swift
@UnibrainApp/MenuBarPopover.swift
@UnibrainApp/UnibrainApp.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MenuBarViewModel extensions (overlay state, calendar permission, resume/skip) + unit tests</name>
  <files>UnibrainApp/ViewModels/MenuBarViewModel.swift, Tests/UnibrainAppTests/MenuBarViewModelOverlayTests.swift</files>
  <read_first>
    - UnibrainApp/ViewModels/MenuBarViewModel.swift (full file — SessionDisplayState enum, stopRecording method, recording lifecycle, permission handling section)
    - .planning/phases/04-course-classification-smart-routing/04-RESEARCH.md Code Examples section (inline view-state switching with PopoverOverlay enum), Pitfall 2 (.sheet on MenuBarExtra fails silently), Open Questions (RESOLVED) Q1 (async variant), Q2 (deployment target), Q3 (Info.plist)
    - .planning/phases/04-course-classification-smart-routing/04-UI-SPEC.md Popover State Extensions section, Interaction Contracts section
    - .planning/phases/04-course-classification-smart-routing/04-CONTEXT.md P-01 (first-time overlay + banner), P-02 (permission request on first recording), MP-04 (picker fires at .classifying), CT-03 (term-expired nudge)
    - Sources/UnibrainCore/Classification/CoursePickerViewModel.swift (from Plan 03 — CoursePickerMode, CourseSelection, CoursePickerViewModel)
    - Sources/UnibrainCore/Classification/PermissionState.swift (from Plan 03 — PermissionState enum)
    - Sources/UnibrainCore/Protocols/CalendarEventProvider.swift (from Plan 02 — CalendarEventProvider protocol)
    - Sources/UnibrainCore/Classification/CourseMapping.swift (from Plan 01 — CourseMappingStore, CourseMappingDocument, TermDefinition)
    - Sources/UnibrainCore/Pipeline/PipelineInputs.swift (from Plan 04 — termLabel field)
  </read_first>
  <behavior>
    - Test 1: MenuBarViewModel with no course selection in flight has `overlayState == .none`.
    - Test 2: After calling handleClassificationPause(match: .none), overlayState transitions to `.coursePicker(.none)`.
    - Test 3: After calling handleClassificationPause(match: .multiple([event1, event2])), overlayState transitions to `.coursePicker(.multiple([event1, event2]))`.
    - Test 4: selectCourse(.course("CS101")) calls orchestrator.resume(with:) with a CalendarEvent whose title is "CS101", and resets overlayState to `.none`.
    - Test 5: selectCourse(.skip) calls orchestrator.skipClassification() and resets overlayState to `.none`.
    - Test 6: skipClassification() resets overlayState to `.none` and calls orchestrator.skipClassification().
    - Test 7: PopoverOverlay has exactly 5 cases: .none, .coursePicker, .manageCourses, .permissionDenied, .termEditor (verify via case iteration or mirror — or just construct one of each and assert Equatable).
  </behavior>
  <action>
    Extend `MenuBarViewModel` with the following additions (all existing Phase 3 code stays intact — only add new properties and methods):

    **Add to SessionDisplayState enum**: a new case `case awaitingCourseSelection` — distinct from `.transcribing`; the popover shows the ClassificationPausedView or CoursePickerView for this state.

    **Add PopoverOverlay enum** (at file scope or nested in MenuBarViewModel) with 5 Equatable cases: `.none`, `.coursePicker(CoursePickerMode)`, `.manageCourses`, `.permissionDenied`, `.termEditor`. Per RESEARCH.md Pitfall 2 and Code Examples: inline view-state switching replaces `.sheet`. The popover body switches on this enum.

    **New properties on MenuBarViewModel**:
    - `var overlayState: PopoverOverlay = .none` — drives the inline view switch
    - `var calendarPermission: PermissionState = .notDetermined` — tracks current calendar permission status
    - `var hasShownPermissionOverlay: Bool = false` — tracks first-time overlay display (persisted via @AppStorage("hasShownPermissionOverlay") on macOS)
    - `var currentTermLabel: String = ""` — from CourseMappingStore, displayed in idle state
    - `var termHasExpired: Bool = false` — computed when term endDate < today
    - `private var courseMappingStore: CourseMappingStore?` — injected dependency
    - `private var calendarProvider: (any CalendarEventProvider)?` — injected EventKitCalendarAdapter
    - `private var coursePickerViewModel: CoursePickerViewModel?` — created on demand when picker fires

    **Extend init** to accept optional `courseMappingStore: CourseMappingStore?` and `calendarProvider: (any CalendarEventProvider)?` parameters with nil defaults (backward-compatible with Phase 3's init).

    **New methods**:

    `func requestCalendarPermission() async`: Call `calendarProvider?.requestFullAccess()` or set permission to `.denied` on failure. Update `calendarPermission` based on result. Per P-02: fires on first recording alongside mic permission. Per RESEARCH Open Question Q1 RESOLVED: assume the async variant of `requestFullAccessToEvents()` exists on macOS 15+; if compile fails, fall back to wrapping the completion-handler variant via `withCheckedThrowingContinuation`.

    `func checkCalendarPermission() async`: Call `calendarProvider?.checkAuthorization()` and map to PermissionState. Update `calendarPermission` property.

    `func handleClassificationPause(match: CourseMatch) async`: Called when orchestrator reaches `.awaitingUserChoice`. Creates a CoursePickerViewModel with mode based on match (.multiple or .none), loaded courses from CourseMappingStore, and recent codes. Sets `overlayState = .coursePicker(mode)`.

    `func selectCourse(_ selection: CourseSelection) async`: Convert CourseSelection to a CalendarEvent for orchestrator.resume(with:). For `.course(code)`: construct CalendarEvent(title: code). For `.event(event)`: use the event directly. For `.newCourse(code, name)`: construct CalendarEvent(title: code), and call courseMappingStore.upsert. For `.skip`: call orchestrator.skipClassification(). Update CourseMappingStore: upsert mapping (M-03), addRecent (M-03). Call orchestrator.resume(with: resolvedEvent). Set overlayState = .none.

    `func skipClassification() async`: Call orchestrator.skipClassification(). Set overlayState = .none.

    `func openSystemSettings()`: macOS-only (`#if os(macOS)`): `NSWorkspace.shared.open(URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_Calendars")!)`. Per RESEARCH.md Pattern 5 and CONTEXT.md Claude's Discretion.

    `func setTerm(label:startDate:endDate:) async`: Call courseMappingStore.setCurrentTerm. Update currentTermLabel and termHasExpired.

    **Modify stopRecording()** (W4 fix — PipelineInputs.termLabel wiring):
    Before constructing PipelineInputs, check if calendar permission is granted. If so, fetch events from EventKitCalendarAdapter using the term range. If not, pass empty events array (manual picker will fire for .none). Add `requestCalendarPermission()` call before the first recording if permission is `.notDetermined`. Explicitly set `inputs.termLabel = currentTermLabel` so the pipeline carries the real term label forward to NoteNormalizer and ScheduleAwareVaultResolver (W4 fix — without this step, termLabel defaults to empty string and the resolver falls back to "default-term").

    The key change: after transcription completes and the orchestrator transitions to `.awaitingUserChoice`, the view model calls `handleClassificationPause`.

    **Create Tests/UnibrainAppTests/MenuBarViewModelOverlayTests.swift**: Unit tests covering the 7 behaviors listed above. Use a mock orchestrator (or spy) to verify resume/skipClassification calls. Tests run on macOS CI only (UnibrainApp target requires AppKit/SwiftUI). Mark the test file `@_spi(Testing) import` or use internal access levels to inject a mock orchestrator.
  </action>
  <verify>
    <automated>cd /home/gr3gg0rk/unibrain && swift build 2>&1 | tail -5 && swift test --filter MenuBarViewModelOverlayTests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - PopoverOverlay enum defined with 5 cases
    - SessionDisplayState has .awaitingCourseSelection case
    - Calendar permission check/request methods compile
    - selectCourse converts CourseSelection to CalendarEvent and resumes orchestrator
    - skipClassification resets overlayState and calls orchestrator
    - openSystemSettings uses NSWorkspace.open with privacy URL
    - stopRecording sets `inputs.termLabel = currentTermLabel` explicitly (W4 fix)
    - Unit tests for PopoverOverlay state transitions + selectCourse/skipClassification logic pass (W3 fix)
    - swift build succeeds for UnibrainApp target
  </done>
</task>

<task type="auto">
  <name>Task 2a: Classification flow views (CoursePickerView, CoursePickerRow, CreateCourseForm, ClassificationPausedView) + MenuBarPopover overlay switching</name>
  <files>UnibrainApp/Views/CoursePickerView.swift, UnibrainApp/Views/CoursePickerRow.swift, UnibrainApp/Views/CreateCourseForm.swift, UnibrainApp/Views/ClassificationPausedView.swift, UnibrainApp/MenuBarPopover.swift</files>
  <read_first>
    - UnibrainApp/MenuBarPopover.swift (full file — existing popover view with idle/recording/paused/transcribing/completed/error states)
    - .planning/phases/04-course-classification-smart-routing/04-UI-SPEC.md (Surface 1 Manual Course Picker, Popover State Extensions, Copywriting Contract)
    - .planning/phases/04-course-classification-smart-routing/04-CONTEXT.md MP-01..MP-05 (picker design), MP-04 (classification-paused state)
    - .planning/phases/04-course-classification-smart-routing/04-RESEARCH.md Pitfall 2 (.sheet on MenuBarExtra fails silently — FB11984872)
  </read_first>
  <action>
    Create `UnibrainApp/Views/` directory. Create 4 SwiftUI view files for the classification flow. All views use:
    - Native SwiftUI components (List, Section, TextField, Button, Label)
    - SF Symbols as specified in UI-SPEC
    - Semantic system colors (accentColor for primary CTAs, orange for warnings)
    - 280pt fixed width matching Phase 3 popover
    - SF Pro semantic type styles (.body, .headline, .subheadline, .caption)
    - Copy from UI-SPEC Copywriting Contract (verbatim strings)

    **CoursePickerRow.swift**: `struct CoursePickerRow: View` — single row showing course title (.subheadline, primary) + course code (.caption, secondary). Accessibility label: "{name}, {code}". Accessibility hint: "Selects this course for the current recording". Tap gesture on the row.

    **CoursePickerView.swift**: `struct CoursePickerView: View` taking a `CoursePickerMode` and a `MenuBarViewModel` (for calling selectCourse/skip). Constructs a CoursePickerViewModel internally. Renders based on mode:
    - Header: "Which lecture is this?" (multi-match) or "Pick a course for this recording" (no-match)
    - If multi-match: events list (ForEach over matchingEvents, each row shows title + time range + location)
    - Divider: "Or pick from all courses"
    - Search TextField with placeholder "Search courses..."
    - Recent section (if non-empty): header "Recent", ForEach over pickerViewModel.recentCourses using CoursePickerRow
    - All Courses section: header "All Courses ({termLabel})", ForEach over pickerViewModel.filteredCourses using CoursePickerRow
    - Create New Course button (plus.circle icon, .bordered style)
    - Skip button ("Skip (save to _unsorted)", .bordered style, NOT destructive)
    - When Create New is tapped, reveal CreateCourseForm inline within the same overlay (use @State to toggle — no .sheet transition per Pitfall 2)
    - Empty search result: show "No courses match '{query}'" inline

    **CreateCourseForm.swift**: `struct CreateCourseForm: View` with two TextFields ("Course Code" placeholder "e.g., CS101", "Course Name" placeholder "e.g., Intro to CS"). Discard button (secondary) + Create + Route button (.borderedProminent, disabled when code is empty). On Create: sanitize code via FolderNameSanitizer, call viewModel.selectCourse(.newCourse(code:name:)).

    **ClassificationPausedView.swift**: `struct ClassificationPausedView: View` — brief loading state. ProgressView (indeterminate) with "Transcription done." headline + "Picking course..." subheadline + "Analyzing calendar..." caption. Cancel button "Cancel (save to _unsorted)" calls viewModel.skipClassification(). This view shows briefly before the picker appears for .multiple/.none, or auto-transitions for .single.

    **MenuBarPopover.swift modifications**:
    Change the `body` to switch on `viewModel.overlayState` FIRST. If overlayState is not `.none`, render the overlay view. If `.none`, fall through to the existing `sessionState` switch:

    ```
    var body: some View {
        switch viewModel.overlayState {
        case .none:
            mainContent
        case .coursePicker(let mode):
            CoursePickerView(mode: mode, viewModel: viewModel)
        case .manageCourses:
            ManageCoursesView(viewModel: viewModel)
        case .permissionDenied:
            PermissionDeniedSheet(viewModel: viewModel)
        case .termEditor:
            TermEditorForm(viewModel: viewModel)
        }
    }

    @ViewBuilder
    private var mainContent: some View {
        // Add .awaitingCourseSelection to existing switch
        switch viewModel.sessionState {
        case .idle:
            idleState  // Extended in Task 2b with Manage Courses button + calendar status + term label
        case .awaitingCourseSelection:
            ClassificationPausedView(viewModel: viewModel)
        // ... existing cases unchanged ...
        }
    }
    ```
  </action>
  <verify>
    <automated>cd /home/gr3gg0rk/unibrain && swift build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - CoursePickerView, CoursePickerRow, CreateCourseForm, ClassificationPausedView created and compile
    - MenuBarPopover body switches on viewModel.overlayState FIRST (inline, not .sheet) per Pitfall 2
    - CoursePickerView handles both multi-match and no-match variants
    - CreateCourseForm sanitizes code and routes via selectCourse
    - swift build succeeds for UnibrainApp target
  </done>
</task>

<task type="auto">
  <name>Task 2b: Settings/permission views (ManageCoursesView, PermissionDeniedSheet, PermissionBanner, TermExpiredBanner, TermEditorForm) + UnibrainApp injection + Info.plist</name>
  <files>UnibrainApp/Views/ManageCoursesView.swift, UnibrainApp/Views/PermissionDeniedSheet.swift, UnibrainApp/Views/PermissionBanner.swift, UnibrainApp/Views/TermExpiredBanner.swift, UnibrainApp/Views/TermEditorForm.swift, UnibrainApp/UnibrainApp.swift, UnibrainApp/Info.plist</files>
  <read_first>
    - UnibrainApp/UnibrainApp.swift (app entry with MenuBarExtra setup)
    - .planning/phases/04-course-classification-smart-routing/04-UI-SPEC.md (Surface 2 Manage Courses, Surface 3 Permission-Degradation, Surface 4 Term-Expired, Copywriting Contract)
    - .planning/phases/04-course-classification-smart-routing/04-CONTEXT.md P-01 (permission UX), M-04 (manage courses), CT-01/CT-03 (term mechanism)
    - .planning/phases/04-course-classification-smart-routing/04-RESEARCH.md Open Questions (RESOLVED) Q3 (Info.plist NSCalendarsUsageDescription)
  </read_first>
  <action>
    Create 5 SwiftUI view files for the settings/permission surfaces, extend UnibrainApp.swift with dependency injection, and create Info.plist with NSCalendarsUsageDescription. All views use native SwiftUI components, SF Symbols, semantic system colors, 280pt fixed width, and SF Pro semantic type styles per UI-SPEC.

    **ManageCoursesView.swift**: `struct ManageCoursesView: View` showing the mapping table. Header "Manage Courses". Current term display with date range. List of mappings (event title -> course code, chevron.right separator). Tap row -> inline edit mode with two TextFields (event title, course code). Swipe to delete with confirmation alert ("Delete mapping for '{event title}'? Recordings with this event title will auto-create a new folder next time." with "Delete" destructive and "Keep Mapping" options). Add Course Mapping button (plus.circle, reveals inline form: Event Title + Course Code + Course Name fields). Done button (.borderedProminent) saves to CourseMappingStore and sets overlayState = .none.

    **PermissionDeniedSheet.swift**: `struct PermissionDeniedSheet: View` — first-time explanation overlay (NOT a .sheet). Header "Calendar Access Needed". Two body paragraphs per UI-SPEC copywriting contract ("unibrain uses your calendar to automatically route recordings to the right course folder." / "Without calendar access, you'll need to pick the course manually each time you record."). "Open System Settings" button (.borderedProminent, gear icon) calls viewModel.openSystemSettings(). "Continue with Manual Pick" button (.bordered) sets overlayState = .coursePicker(.none).

    **PermissionBanner.swift**: `struct PermissionBanner: View` — compact ongoing banner. Orange-tinted background (.orange.opacity(0.15), cornerRadius 8). Warning icon (exclamationmark.triangle.fill, .orange). Two-line text: "Calendar off — manual pick required. Tap to enable." Tap action: viewModel.openSystemSettings(). Accessibility: .isHeader trait.

    **TermExpiredBanner.swift**: `struct TermExpiredBanner: View` — same visual language as PermissionBanner. Text: "{term label} ended — set your new term?". "Set Term" button (.bordered, compact) sets viewModel.overlayState = .termEditor.

    **TermEditorForm.swift**: `struct TermEditorForm: View` — header "Set Current Term". TextField for label (placeholder "e.g., Fall 2026"). DatePicker for start date. DatePicker for end date. "Set Current Term" button (.borderedProminent) calls viewModel.setTerm and sets overlayState = .none.

    **Extend idleState in MenuBarPopover** (this requires editing MenuBarPopover.swift which was touched in Task 2a — use Edit tool to extend the idleState computed property, not Write):
    - Calendar status line: checkmark.circle.fill (.green) + "Calendar connected" when granted; exclamationmark.circle (.orange) + "Calendar off — manual pick" when denied.
    - Term label: "Term: {currentTermLabel}" in .caption/.secondary.
    - Manage Courses button: folder.badge.gearshape + "Manage Courses", .bordered, .controlSize(.small), sets overlayState = .manageCourses.
    - PermissionBanner (if calendar denied and hasShownPermissionOverlay) above the status lines.
    - TermExpiredBanner (if termHasExpired) above the status lines (mutually exclusive with permission banner — permission takes priority).

    **UnibrainApp.swift modifications**:
    In init(), create CourseMappingStore with vaultRoot from HardcodedVaultResolver.vaultRoot. Create EventKitCalendarAdapter. Inject both into MenuBarViewModel. Add `.task` to check calendar permission on launch and on `NSApplication.didBecomeActiveNotification` (to detect if user granted permission via Settings while app was backgrounded).

    **Create UnibrainApp/Info.plist** (per RESEARCH Open Question Q3 RESOLVED — B1 blocker):
    Add an Info.plist file at `UnibrainApp/Info.plist` with the following key:
    - `NSCalendarsUsageDescription`: "Unibrain reads your calendar to route lecture recordings to the correct course folder automatically."
    Without this key, `EKEventStore.requestFullAccessToEvents()` silently fails — the system permission dialog never appears and the permission flow breaks on device. This Info.plist ships with the app bundle when Xcode builds the macOS target. SPM-only tests mock `CalendarEventProvider` and do not need the key.
  </action>
  <verify>
    <automated>cd /home/gr3gg0rk/unibrain && swift build 2>&1 | tail -5 && grep -c 'NSCalendarsUsageDescription' UnibrainApp/Info.plist</automated>
  </verify>
  <done>
    - ManageCoursesView, PermissionDeniedSheet, PermissionBanner, TermExpiredBanner, TermEditorForm created and compile
    - Idle state extended with calendar status, term label, manage courses button, permission banner, term-expired banner
    - PermissionDeniedSheet is an inline overlay (NOT a .sheet) per Pitfall 2
    - UnibrainApp.swift injects CourseMappingStore and EventKitCalendarAdapter into MenuBarViewModel
    - Info.plist created with NSCalendarsUsageDescription key (B1 fix)
    - swift build succeeds for all targets
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: macOS device verification — Phase 4 full flow</name>
  <files></files>
  <action>
    Complete Phase 4 course classification + smart routing verification on a macOS device with Xcode 16+.

    Build and run the app via Xcode. Verify the following 8 scenarios:

    1. **Idle state check**: Open the menu-bar popover. Verify "Manage Courses" button is visible. Verify calendar status line shows "Calendar connected" (if previously granted) or "Calendar off" (if not).
    2. **Calendar permission flow**: If not yet granted, tap Record. Verify the system permission dialog appears for both mic and calendar (the NSCalendarsUsageDescription string from Info.plist appears in the dialog). Grant Full Access. Verify the idle status updates to "Calendar connected".
    3. **Auto-route (.single match)**: Add a calendar event overlapping the current time (e.g., "CS101 Lecture" starting now). Record for 30 seconds, stop. Verify the note appears in the correct course folder under the sanitized term label. Verify the frontmatter has real course, course_name, and term values.
    4. **Manual picker (.none match)**: Record without any overlapping calendar event. Stop. Verify the course picker appears inline in the popover (NOT a detached sheet). Tap a course or Skip. Verify the note routes to the selected course folder or _unsorted.
    5. **Permission denied degradation**: Deny calendar permission in System Settings. Record. Verify the first-time explanation overlay appears with the Settings deep-link button. Dismiss. Record again — verify the compact orange banner appears instead of the overlay.
    6. **Manage Courses**: Tap "Manage Courses" in idle state. Verify the mapping table appears with editable rows. Add a new mapping. Delete a mapping. Tap Done — verify changes persist.
    7. **Term editor**: Tap the term label or term-expired banner. Verify the term editor appears with label, start date, end date fields. Set a new term. Verify the idle state updates.
    8. **Multi-match (.multiple)**: Create two overlapping calendar events. Record. Verify the picker shows both events with time ranges and locations. Tap one. Verify the note routes correctly.
  </action>
  <verify>
    <automated>echo "Manual macOS device verification — see action steps"</automated>
    <human-check>8 scenarios in action block must all pass</human-check>
  </verify>
  <done>All 8 device verification scenarios pass: auto-route, manual picker, permission degradation, manage courses, term editor, multi-match picker all functional on macOS. NSCalendarsUsageDescription appears in the system permission dialog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Orchestrator (resume) | User selection crosses from MainActor to orchestrator actor via resume(with:) |
| macOS Settings deep-link | URL scheme opens external System Settings app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Spoofing | Malicious URL scheme replaces the Settings deep-link | low | mitigate | URL string is a compile-time constant, not derived from user input. NSWorkspace.open validates the URL scheme before launching. |
| T-04-13 | Information Disclosure | Calendar event details shown in multi-match picker are visible on screen | low | accept | Single-user device. Events are the user's own calendar data. No disclosure beyond what the OS already shows. |
| T-04-14 | Denial of Service | Overlay state gets stuck (picker never dismisses) | medium | mitigate | Skip button always available. Cancel button in ClassificationPausedView calls skipClassification. Cancel pipeline via menu-bar also clears overlay state. |
</threat_model>

<verification>
- `swift build` succeeds for all targets (UnibrainCore, UnibrainProviders, UnibrainApp)
- `swift test --filter UnibrainCoreTests` passes on WSL2 Linux (pure logic)
- macOS CI: all target tests pass including EventKitCalendarAdapterTests and ScheduleAwareVaultResolverTests
- macOS device: full Phase 4 flow verified per Task 3 checkpoint
</verification>

<success_criteria>
- Course picker appears inline (not .sheet) for .multiple and .none classification results (MP-01, CLAS-04)
- Manual pick updates courses.json mapping AND recent list (M-03, CLAS-07)
- Calendar permission denial shows first-time sheet then ongoing banner (P-01, ONBD-03)
- Manage Courses button opens editable mapping table (M-04)
- Term editor sets label + dates (CT-01)
- Term-expired banner appears when endDate < today (CT-03)
- macOS Settings deep-link opens Privacy > Calendars (P-01)
- Auto-route writes to {vault}/{term}/{course-code}/ with real frontmatter values (CLAS-05)
</success_criteria>

<output>
Create `.planning/phases/04-course-classification-smart-routing/04-05-SUMMARY.md` when done
</output>
