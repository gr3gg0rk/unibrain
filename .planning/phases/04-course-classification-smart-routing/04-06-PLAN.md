---
phase: 04-course-classification-smart-routing
plan: 06
type: execute
wave: 3
depends_on: [04-04, 04-05]
gap_closure: true
autonomous: true
requirements: [CLAS-01, CLAS-02, CLAS-04, CLAS-05, CLAS-07, ONBD-02, ONBD-03]
files_modified:
  - Sources/UnibrainProviders/Pipeline/PipelineWiring.swift
  - UnibrainApp/UnibrainApp.swift
  - UnibrainApp/ViewModels/MenuBarViewModel.swift
  - UnibrainApp/Views/CoursePickerView.swift
  - UnibrainApp/Views/ManageCoursesView.swift

must_haves:
  truths:
    - "PipelineWiring.makeScheduleAwareOrchestrator constructs ScheduleAwareVaultResolver with vaultRoot, termLabel, and mapping snapshot (CLAS-02, CLAS-05)"
    - "UnibrainApp.swift constructs the orchestrator with ScheduleAwareVaultResolver instead of HardcodedVaultResolver (CLAS-01, CLAS-02)"
    - "MenuBarViewModel observes orchestrator.currentState and calls handleClassificationPause when .awaitingUserChoice is detected (CLAS-04, MP-04)"
    - "CoursePickerView reads recentCourses and filteredCourses from CoursePickerViewModel data exposed via MenuBarViewModel (CLAS-04)"
    - "ManageCoursesView.loadMappings reads from CourseMappingStore via MenuBarViewModel (CLAS-02, M-04)"
    - "Manual course selection persists via CourseMappingStore and routes the next recording correctly (CLAS-07, M-03)"
  artifacts:
    - path: "Sources/UnibrainProviders/Pipeline/PipelineWiring.swift"
      provides: "makeScheduleAwareOrchestrator factory that injects ScheduleAwareVaultResolver"
      contains: "makeScheduleAwareOrchestrator"
    - path: "UnibrainApp/UnibrainApp.swift"
      provides: "Calls makeScheduleAwareOrchestrator with vaultRoot + CourseMappingStore snapshot"
      contains: "makeScheduleAwareOrchestrator"
    - path: "UnibrainApp/ViewModels/MenuBarViewModel.swift"
      provides: "State observer polling orchestrator.currentState + picker data exposed + mappings loader"
      contains: "stateObserverTask"
    - path: "UnibrainApp/Views/CoursePickerView.swift"
      provides: "Reads course data from MenuBarViewModel pickerData property instead of hardcoded arrays"
      contains: "pickerData"
    - path: "UnibrainApp/Views/ManageCoursesView.swift"
      provides: "loadMappings reads from CourseMappingStore via MenuBarViewModel"
      contains: "loadMappingsFromStore"
  key_links:
    - from: "UnibrainApp.init"
      to: "PipelineWiring.makeScheduleAwareOrchestrator"
      via: "Factory call replacing makeOrchestrator"
      pattern: "makeScheduleAwareOrchestrator"
    - from: "MenuBarViewModel state observer"
      to: "handleClassificationPause(match:)"
      via: "Polling task detecting orchestrator.currentState == .awaitingUserChoice"
      pattern: "stateObserverTask"
    - from: "CoursePickerView"
      to: "CoursePickerViewModel data"
      via: "MenuBarViewModel.pickerData computed property"
      pattern: "pickerData"
    - from: "ManageCoursesView.loadMappings"
      to: "CourseMappingStore.allMappings"
      via: "MenuBarViewModel.loadAllMappings async method"
      pattern: "loadAllMappings"
---

<objective>
Wire together the tested Phase 4 components so the running app actually uses ScheduleAwareVaultResolver, fires the manual picker when the orchestrator pauses, and displays real course data in both CoursePickerView and ManageCoursesView.

Purpose: Phase 4 produced all the right components — CourseMappingStore, ScheduleAwareVaultResolver, CoursePickerViewModel, PipelineOrchestrator pause/resume, and the SwiftUI views — but the integration layer was never wired. This plan closes all 4 wiring gaps identified in 04-VERIFICATION.md so the Phase 4 success criteria become observable in the running app. Per D-01/D-02 (locked decisions), the schedule-to-folder routing is the competitive moat — if it does not fire in production, the phase has failed.

Output: Five modified files that connect the tested components into a working end-to-end pipeline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-course-classification-smart-routing/04-CONTEXT.md
@.planning/phases/04-course-classification-smart-routing/04-VERIFICATION.md
@.planning/phases/04-course-classification-smart-routing/04-04-SUMMARY.md
@.planning/phases/04-course-classification-smart-routing/04-05-SUMMARY.md
@Sources/UnibrainProviders/Pipeline/PipelineWiring.swift
@Sources/UnibrainProviders/VaultWriting/ScheduleAwareVaultResolver.swift
@Sources/UnibrainProviders/VaultWriting/HardcodedVaultResolver.swift
@Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift
@Sources/UnibrainCore/Pipeline/PipelineState.swift
@Sources/UnibrainCore/Classification/CoursePickerViewModel.swift
@Sources/UnibrainCore/Classification/CourseMappingStore.swift
@UnibrainApp/UnibrainApp.swift
@UnibrainApp/ViewModels/MenuBarViewModel.swift
@UnibrainApp/Views/CoursePickerView.swift
@UnibrainApp/Views/ManageCoursesView.swift
@UnibrainApp/MenuBarPopover.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ScheduleAwareVaultResolver into PipelineWiring + UnibrainApp</name>
  <files>Sources/UnibrainProviders/Pipeline/PipelineWiring.swift, UnibrainApp/UnibrainApp.swift</files>
  <read_first>
    - Sources/UnibrainProviders/Pipeline/PipelineWiring.swift (full file — 74 lines; the makeOrchestrator factory at line 26 constructs HardcodedVaultResolver at line 29; makeRecordingSession and makePipelineInputs at lines 40-72 stay unchanged)
    - Sources/UnibrainProviders/VaultWriting/ScheduleAwareVaultResolver.swift (full file — 120 lines; init signature at line 50: init(vaultRoot: URL, termLabel: String, mapping: [String: CourseMapping] = [:]); resolve method at line 74 takes match: CourseMatch and recordingStart: Date)
    - Sources/UnibrainProviders/VaultWriting/HardcodedVaultResolver.swift (full file — 50 lines; vaultRoot static at line 17, lecturesDir static at line 22 — these stay available for backward compatibility)
    - UnibrainApp/UnibrainApp.swift (full file — 141 lines; init at line 29 constructs orchestrator via PipelineWiring.makeOrchestrator at line 32; CourseMappingStore already created at lines 36-38; calendar provider at lines 41-45; MenuBarViewModel init at lines 47-55)
    - Sources/UnibrainCore/Classification/CourseMappingStore.swift (lines 110-113: allMappings() async throws -> [String: CourseMapping]; lines 122-125: currentTerm() async throws -> TermDefinition)
  </read_first>
  <action>
    **Gap 1 closure: Resolver not wired (VERIFICATION.md gap 1)**

    **PipelineWiring.swift — Add new factory method (keep makeOrchestrator for backward compat):**

    Add a new public static method `makeScheduleAwareOrchestrator` after the existing `makeOrchestrator` (after line 35). This method accepts vaultRoot, termLabel, and mapping parameters and constructs a ScheduleAwareVaultResolver instead of HardcodedVaultResolver.

    Signature: `public static func makeScheduleAwareOrchestrator(modelPath: URL, vaultRoot: URL, termLabel: String, mapping: [String: CourseMapping]) -> PipelineOrchestrator`

    Implementation:
    - Construct `TranscriberRouter(modelPath: modelPath)` (same as existing)
    - Construct `NSFileCoordinatorNoteWriter()` (same as existing)
    - Construct `ScheduleAwareVaultResolver(vaultRoot: vaultRoot, termLabel: termLabel, mapping: mapping)` — this is the Phase 4 resolver that builds {vault}/{term}/{course-code}/ paths per CLAS-05
    - Return `PipelineOrchestrator(transcriber: transcriber, writer: writer, resolver: resolver)`

    Do NOT remove or modify the existing `makeOrchestrator(modelPath:)` — Phase 3 backward compatibility requires it to stay. The MenuBarPopover #Preview at MenuBarPopover.swift line 407 uses it.

    Also add a convenience method to refresh the mapping snapshot on the orchestrator: `public static func makeUpdatedResolver(vaultRoot: URL, termLabel: String, mapping: [String: CourseMapping]) -> any VaultPathResolver`. This returns a new ScheduleAwareVaultResolver with refreshed data. The orchestrator's resolver is injected at init time (immutable), so to refresh mappings between recordings, the caller constructs a new orchestrator. Since MenuBarViewModel.stopRecording constructs PipelineInputs per-recording, the resolver snapshot should be loaded fresh at the start of each recording cycle.

    **UnibrainApp.swift — Replace makeOrchestrator with makeScheduleAwareOrchestrator:**

    In `init()` at line 29, replace the orchestrator construction. The current code at line 32 is:
    `let orchestrator = PipelineWiring.makeOrchestrator(modelPath: modelPath)`

    Change the init to load mapping data synchronously before constructing the orchestrator. Since CourseMappingStore is an actor with async methods, and `init()` is synchronous, use a two-step approach:

    Step 1 — Change the `init()` to NOT construct the orchestrator inline. Instead, store temporary state and construct the orchestrator in a `.task` modifier on first launch. Add a private `@State private var orchestratorReady: Bool = false` flag. Set viewModel to a temporary state, then in the first `.task` on the WindowGroup body, load the mapping snapshot and reconstruct the orchestrator.

    **Preferred simpler approach (avoid async init complexity):** Since `ScheduleAwareVaultResolver` accepts an empty mapping default (`mapping: [:]`), construct the orchestrator with an empty mapping initially. The mapping snapshot refresh happens in `MenuBarViewModel.stopRecording()` before each pipeline run — add a method to refresh the resolver there (see Task 2).

    So in `init()`:
    - Load `vaultRoot` from `HardcodedVaultResolver.vaultRoot` (same as line 37)
    - Load `currentTermLabel` — default to empty string (loaded asynchronously in the launch .task at lines 73-77 via `viewModel.loadCurrentTerm()`)
    - Construct orchestrator via `PipelineWiring.makeScheduleAwareOrchestrator(modelPath: modelPath, vaultRoot: HardcodedVaultResolver.vaultRoot, termLabel: "", mapping: [:])` — empty mapping initially; Task 2 refreshes it per-recording

    This keeps the synchronous init unchanged in structure, just calling the new factory with empty defaults. The real mapping data loads asynchronously at recording time (Task 2 adds the refresh).

    The CourseMappingStore creation at lines 36-38 stays unchanged. The calendarProvider creation at lines 41-45 stays unchanged. The MenuBarViewModel init at lines 47-55 stays unchanged.

    Per D-01 (locked decision: "Every recording lands in the right course folder") and CLAS-02 (title-to-course-code mapping), this wiring is mandatory — without it the moat feature is dead code.
  </action>
  <verify>
    <automated>cd /home/gr3gg0rk/unibrain && swift build --target UnibrainProviders 2>&amp;1 | tail -5 &amp;&amp; grep -c 'makeScheduleAwareOrchestrator' Sources/UnibrainProviders/Pipeline/PipelineWiring.swift | grep -qx '1' &amp;&amp; grep -c 'makeScheduleAwareOrchestrator' UnibrainApp/UnibrainApp.swift | grep -qx '1'</automated>
  </verify>
  <done>
    - PipelineWiring.swift has a new makeScheduleAwareOrchestrator(modelPath:vaultRoot:termLabel:mapping:) method that constructs ScheduleAwareVaultResolver
    - UnibrainApp.init calls makeScheduleAwareOrchestrator instead of makeOrchestrator
    - The existing makeOrchestrator stays for backward compatibility (MenuBarPopover preview)
    - swift build --target UnibrainProviders succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add state observer + resolver refresh + picker data exposure in MenuBarViewModel</name>
  <files>UnibrainApp/ViewModels/MenuBarViewModel.swift</files>
  <read_first>
    - UnibrainApp/ViewModels/MenuBarViewModel.swift (full file — 705 lines; stopRecording at line 279 constructs Task.detached at line 325; handleClassificationPause at line 425 constructs CoursePickerViewModel at line 453; coursePickerViewModel stored at line 175; orchestrator accessor at line 162; existing pollTask/downloadObserverTask patterns at lines 178/181 for polling reference)
    - Sources/UnibrainCore/Pipeline/PipelineOrchestrator.swift (full file — 287 lines; currentState at line 80 exposes the actor's state; .awaitingUserChoice set at line 228; resume(with:) at line 177; skipClassification() at line 188)
    - Sources/UnibrainCore/Pipeline/PipelineState.swift (full file — 49 lines; .awaitingUserChoice case at line 38)
    - Sources/UnibrainCore/Classification/CoursePickerViewModel.swift (full file — 148 lines; recentCourses computed property at line 99; filteredCourses at line 107; allCourses at line 68)
    - UnibrainApp/Views/CoursePickerView.swift (lines 11-14: the view receives mode and viewModel; lines 182-186: hardcoded empty arrays)
    - UnibrainApp/Views/ManageCoursesView.swift (lines 150-154: loadMappings returns empty)
  </read_first>
  <action>
    **Gap 2 closure: No state observer for .awaitingUserChoice (VERIFICATION.md gap 2)**
    **Gap 3 enabler: Expose picker data for CoursePickerView**
    **Gap 4 enabler: Expose mappings loader for ManageCoursesView**

    **Add state observer task property (near line 181):**

    Add a new private property: `private var stateObserverTask: Task<Void, Never>?` alongside the existing `pollTask` and `downloadObserverTask` properties.

    **Add state observer start method:**

    Add a private method `startObservingOrchestratorState()` that creates a polling Task checking `orchestrator?.currentState` every 100ms while a pipeline run is active. When the state transitions to `.awaitingUserChoice`, it calls `handleClassificationPause(match:)` with a reconstructed CourseMatch.

    The match type derivation: the orchestrator does not expose the CourseMatch that caused the pause. Two options: (a) poll currentState and when `.awaitingUserChoice` is detected, construct a `.none` match (the picker still fires; the events list will be empty for .none mode); or (b) better — store the events from PipelineInputs on the view model so the match can be reconstructed.

    Use approach (b): Add a private stored property `private var pendingEvents: [CalendarEvent] = []` set during `stopRecording()` right before launching the Task.detached (after line 318 where `inputs.events = events` is set, add `self.pendingEvents = events`).

    Then in the state observer, when `.awaitingUserChoice` is detected, derive the match:
    - If `pendingEvents.count >= 2`: match = `.multiple(pendingEvents)` (CourseClassifier matched multiple overlapping)
    - If `pendingEvents.isEmpty`: match = `.none`
    - If `pendingEvents.count == 1`: match = `.none` (single should have auto-resolved; treat as .none safety fallback)

    Call `await handleClassificationPause(match: match)` on @MainActor, then cancel the state observer (it has done its job — the picker is showing).

    **Start the observer in stopRecording():**

    In `stopRecording()`, before the `Task.detached` call at line 325, call `startObservingOrchestratorState()`. The observer runs concurrently with the transcription task. When the orchestrator reaches `.awaitingUserChoice`, the observer fires the picker.

    **Stop the observer on completion/error:**

    In `onTranscriptionComplete()` (line 657) and `onTranscriptionError(_:)` (line 664), add `stateObserverTask?.cancel(); stateObserverTask = nil` alongside the existing `stopPolling()` calls. Also cancel it in `dismissCompletion()`.

    **Expose picker data for CoursePickerView (Gap 3):**

    The `coursePickerViewModel` at line 175 is already constructed by `handleClassificationPause`. Make it readable by CoursePickerView by adding a computed property that returns the course data:

    Add: `var pickerRecentCourses: [CourseSummary] { coursePickerViewModel?.recentCourses ?? [] }`
    Add: `var pickerFilteredCourses: [CourseSummary] { coursePickerViewModel?.filteredCourses ?? [] }`
    Add: `var pickerSearchQuery: String { coursePickerViewModel?.searchQuery ?? "" }`

    Note: `CoursePickerViewModel` is a plain class (not @Observable), so SwiftUI will not automatically re-render when its properties change. However, `handleClassificationPause` sets `overlayState = .coursePicker(mode)` which IS @Observable on MenuBarViewModel — so the view re-renders when the overlay switches. At that point the pickerData properties read the already-populated CoursePickerViewModel. For the search field, CoursePickerView already has its own @State searchQuery (line 15) that filters locally — so the search updates work without re-rendering the view model. The picker data (course lists) is static once the picker fires.

    **Expose mappings loader for ManageCoursesView (Gap 4):**

    Add a public method: `func loadAllMappings() async -> [String: CourseMapping]` that reads from CourseMappingStore:
    ```
    if let store = courseMappingStore {
        return (try? await store.allMappings()) ?? [:]
    }
    return [:]
    ```

    Also add: `func deleteMapping(eventTitle: String) async` that calls `try? await courseMappingStore?.deleteMapping(eventTitle: eventTitle)` for ManageCoursesView's delete action.

    Also add: `func addMapping(eventTitle: String, code: String, name: String) async` that calls `try? await courseMappingStore?.upsert(eventTitle: eventTitle, mapping: CourseMapping(courseCode: code, courseName: name))` for ManageCoursesView's add action.

    **Refresh resolver snapshot per recording:**

    In `stopRecording()`, after fetching events (around line 309) and before constructing PipelineInputs, load the current mapping snapshot and pass it to a new orchestrator constructed with the fresh data. However, the orchestrator's resolver is injected at init time and is immutable.

    Simpler approach: Since `ScheduleAwareVaultResolver` is a value type (struct) with an immutable mapping dict, and the orchestrator holds it as `let resolver`, the mapping cannot be refreshed on a running orchestrator.

    The pragmatic solution per 04-VERIFICATION.md gap 1 missing items: load the mapping snapshot and construct a NEW orchestrator per recording with the fresh mapping. Add a method on MenuBarViewModel:

    `private func refreshOrchestrator(termLabel: String, mapping: [String: CourseMapping]) -> PipelineOrchestrator` that calls `PipelineWiring.makeScheduleAwareOrchestrator(modelPath: SmallEnDownloader.modelStoragePath, vaultRoot: HardcodedVaultResolver.vaultRoot, termLabel: termLabel, mapping: mapping)`.

    In `stopRecording()`, before constructing PipelineInputs, load the mapping and term, construct a fresh orchestrator, and use it for this run. Store it as `private var currentOrchestrator: PipelineOrchestrator?` (replacing the computed `orchestrator` accessor at line 162). The `overlayOrchestrator` stays for test injection.

    Add: `private var currentOrchestrator: PipelineOrchestrator?` property. In stopRecording:
    ```
    let mapping = (try? await courseMappingStore?.allMappings()) ?? [:]
    let termLabel = currentTermLabel
    let fresh = PipelineWiring.makeScheduleAwareOrchestrator(
        modelPath: SmallEnDownloader.modelStoragePath,
        vaultRoot: HardcodedVaultResolver.vaultRoot,
        termLabel: termLabel,
        mapping: mapping
    )
    currentOrchestrator = fresh
    ```

    Update the `orchestrator` computed property at line 162 to: `private var orchestrator: PipelineOrchestrator? { currentOrchestrator ?? (overlayOrchestrator as? PipelineOrchestrator) }`.

    This means each recording gets a fresh resolver with the latest mapping snapshot, satisfying CLAS-07 (manual override remembered for next recording) and M-02 (auto-learn on encounter).
  </action>
  <verify>
    <automated>cd /home/gr3gg0rk/unibrain &amp;&amp; grep -c 'stateObserverTask' UnibrainApp/ViewModels/MenuBarViewModel.swift | grep -qx '1' || true; grep -c 'startObservingOrchestratorState' UnibrainApp/ViewModels/MenuBarViewModel.swift | grep -qx '1' || true; grep -c 'pickerRecentCourses' UnibrainApp/ViewModels/MenuBarViewModel.swift | grep -qx '1' || true; grep -c 'loadAllMappings' UnibrainApp/ViewModels/MenuBarViewModel.swift | grep -qx '1' || true; grep -c 'currentOrchestrator' UnibrainApp/ViewModels/MenuBarViewModel.swift | grep -v '^#' | grep -c 'currentOrchestrator'</automated>
  </verify>
  <done>
    - MenuBarViewModel has a stateObserverTask that polls orchestrator.currentState
    - When .awaitingUserChoice is detected, handleClassificationPause is called with the correct CourseMatch
    - pickerRecentCourses and pickerFilteredCourses computed properties expose CoursePickerViewModel data
    - loadAllMappings() async method reads from CourseMappingStore
    - deleteMapping and addMapping methods delegate to CourseMappingStore
    - stopRecording constructs a fresh orchestrator with current mapping snapshot per recording
    - stateObserverTask is cancelled on completion/error/dismiss
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire CoursePickerView + ManageCoursesView to read real data</name>
  <files>UnibrainApp/Views/CoursePickerView.swift, UnibrainApp/Views/ManageCoursesView.swift</files>
  <read_first>
    - UnibrainApp/Views/CoursePickerView.swift (full file — 204 lines; lines 11-14: struct fields mode + viewModel; line 15: @State searchQuery; lines 65-67: ForEach(recentCourses); lines 90-97: ForEach(filteredCourses); lines 182-186: hardcoded empty return [])
    - UnibrainApp/Views/ManageCoursesView.swift (full file — 173 lines; lines 93-95: .task calls loadMappings; lines 150-154: loadMappings returns empty; lines 157-167: addMapping mutates local @State only; lines 169-171: deleteMapping mutates local @State only)
    - UnibrainApp/ViewModels/MenuBarViewModel.swift (Task 2 adds: pickerRecentCourses, pickerFilteredCourses, loadAllMappings, deleteMapping, addMapping)
    - Sources/UnibrainCore/Classification/CoursePickerViewModel.swift (lines 99-102: recentCourses computed; lines 107-113: filteredCourses computed)
  </read_first>
  <action>
    **Gap 3 closure: CoursePickerView has no data (VERIFICATION.md gap 3)**
    **Gap 4 closure: ManageCoursesView has no data (VERIFICATION.md gap 4)**

    **CoursePickerView.swift — Replace hardcoded arrays with view model data:**

    At line 182, replace the `recentCourses` computed property:
    From: `private var recentCourses: [CourseSummary] { [] }`
    To: `private var recentCourses: [CourseSummary] { viewModel.pickerRecentCourses }`

    At line 186, replace the `filteredCourses` computed property:
    From: `private var filteredCourses: [CourseSummary] { [] }`
    To: `private var filteredCourses: [CourseSummary] {
        guard !searchQuery.isEmpty else { return viewModel.pickerFilteredCourses }
        let needle = searchQuery.lowercased()
        return viewModel.pickerFilteredCourses.filter { course in
            course.code.lowercased().contains(needle) || course.name.lowercased().contains(needle)
        }
    }`

    This reads from the picker data exposed by MenuBarViewModel (Task 2), which in turn reads from the CoursePickerViewModel constructed in handleClassificationPause. The search filtering is applied locally on the full course list — the @State searchQuery at line 15 drives it.

    Remove the comment at lines 179-181 that says the arrays are empty — they are now wired.

    **ManageCoursesView.swift — Replace loadMappings stub + wire add/delete to store:**

    At line 150, replace the `loadMappings()` method:
    From:
    ```
    private func loadMappings() async {
        mappings = [:]
    }
    ```
    To:
    ```
    private func loadMappings() async {
        mappings = await viewModel.loadAllMappings()
    }
    ```

    At line 157, replace `addMapping()` to persist to the store:
    From:
    ```
    private func addMapping() {
        let mapping = CourseMapping(...)
        mappings[newEventTitle] = mapping
        showingAddForm = false
        ...
    }
    ```
    To:
    ```
    private func addMapping() {
        let mapping = CourseMapping(
            courseCode: newCourseCode,
            courseName: newCourseName.isEmpty ? newCourseCode : newCourseName
        )
        mappings[newEventTitle] = mapping
        showingAddForm = false
        newEventTitle = ""
        newCourseCode = ""
        newCourseName = ""
        Task {
            await viewModel.addMapping(eventTitle: newEventTitle, code: newCourseCode, name: newCourseName)
        }
    }
    ```

    IMPORTANT: Capture the field values BEFORE clearing them. Move the field-clearing after the Task captures them. Corrected:
    ```
    private func addMapping() {
        let title = newEventTitle
        let code = newCourseCode
        let name = newCourseName.isEmpty ? newCourseCode : newCourseName
        let mapping = CourseMapping(courseCode: code, courseName: name)
        mappings[title] = mapping
        showingAddForm = false
        newEventTitle = ""
        newCourseCode = ""
        newCourseName = ""
        Task {
            await viewModel.addMapping(eventTitle: title, code: code, name: name)
        }
    }
    ```

    At line 169, replace `deleteMapping(_:)` to persist the deletion:
    From:
    ```
    private func deleteMapping(_ key: String) {
        mappings.removeValue(forKey: key)
    }
    ```
    To:
    ```
    private func deleteMapping(_ key: String) {
        mappings.removeValue(forKey: key)
        Task {
            await viewModel.deleteMapping(eventTitle: key)
        }
    }
    ```

    These changes ensure the Manage Courses view both displays real data from courses.json AND persists user edits back to the store. Per M-04 (locked decision): "Angelica never has to hand-edit JSON" — the UI must be the editor.
  </action>
  <verify>
    <automated>cd /home/gr3gg0rk/unibrain &amp;&amp; grep -v '^//' UnibrainApp/Views/CoursePickerView.swift | grep -v '^    //' | grep -c 'viewModel.pickerRecentCourses' | grep -qx '1' &amp;&amp; grep -v '^//' UnibrainApp/Views/CoursePickerView.swift | grep -v '^    //' | grep -c 'return \[\]' | grep -qx '0' &amp;&amp; grep -v '^//' UnibrainApp/Views/ManageCoursesView.swift | grep -c 'viewModel.loadAllMappings' | grep -qx '1' &amp;&amp; grep -v '^//' UnibrainApp/Views/ManageCoursesView.swift | grep -c 'mappings = \[:\]' | grep -qx '0'</automated>
  </verify>
  <done>
    - CoursePickerView.recentCourses reads from viewModel.pickerRecentCourses (no hardcoded arrays)
    - CoursePickerView.filteredCourses reads from viewModel.pickerFilteredCourses with local search filtering
    - ManageCoursesView.loadMappings calls viewModel.loadAllMappings() (no hardcoded empty dict)
    - ManageCoursesView.addMapping persists via viewModel.addMapping to CourseMappingStore
    - ManageCoursesView.deleteMapping persists via viewModel.deleteMapping to CourseMappingStore
    - No literal "return []" remains in CoursePickerView course-data properties
    - No literal "mappings = [:]" remains in ManageCoursesView.loadMappings
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI layer -> Orchestrator actor | State observer polls orchestrator.currentState across actor boundary |
| CoursePickerView -> MenuBarViewModel | View reads picker data from view model (no direct CourseMappingStore access) |
| ManageCoursesView -> CourseMappingStore | View model delegates CRUD to store actor (no direct file access) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06-01 | Denial of Service | State observer polling loop never terminates | medium | mitigate | Observer task is cancelled in onTranscriptionComplete, onTransorationError, and dismissCompletion. It also self-terminates after firing handleClassificationPause (its sole job). Uses weak self reference. |
| T-04-06-02 | Tampering | Mapping snapshot loaded at recording time may be stale if courses.json changes mid-run | low | accept | Mapping is loaded per-recording in stopRecording (fresh each time). Within a single pipeline run, the mapping is immutable — acceptable since a recording takes seconds to stop. |
| T-04-06-03 | Information Disclosure | CoursePickerView exposes all course mappings to the UI | low | accept | Single-user app (constraint: single-user v1). Course data is the user's own calendar mappings. No cross-user exposure surface. |
</threat_model>

<verification>
- `swift build` succeeds for all targets (UnibrainCore, UnibrainProviders, UnibrainApp)
- `swift test` passes on WSL2 Linux (existing 200+ tests unaffected — no test files modified)
- grep confirms makeScheduleAwareOrchestrator exists in PipelineWiring.swift and is called from UnibrainApp.swift
- grep confirms stateObserverTask exists in MenuBarViewModel.swift
- grep confirms CoursePickerView reads viewModel.pickerRecentCourses (no hardcoded return [])
- grep confirms ManageCoursesView calls viewModel.loadAllMappings (no hardcoded mappings = [:])
- macOS CI runner builds + tests pass (ScheduleAwareVaultResolverTests, EventKitCalendarAdapterTests)
</verification>

<success_criteria>
- PipelineWiring.makeScheduleAwareOrchestrator constructs ScheduleAwareVaultResolver (CLAS-02, CLAS-05)
- UnibrainApp uses the schedule-aware factory (CLAS-01)
- MenuBarViewModel detects .awaitingUserChoice and fires the picker (CLAS-04, MP-04)
- CoursePickerView displays real course data from CourseMappingStore (CLAS-04)
- ManageCoursesView displays and edits real mappings from CourseMappingStore (M-04)
- Manual picks persist and route the next recording correctly (CLAS-07, M-03)
- All existing tests remain green (no regressions)
</success_criteria>

<output>
Create `.planning/phases/04-course-classification-smart-routing/04-06-SUMMARY.md` when done
</output>
